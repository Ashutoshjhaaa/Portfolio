'use client';
import Link from 'next/link';

import React, { useEffect, useRef } from 'react';
import Head from 'next/head';

export default function NodeJSInterviewQuestions() {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
        const container = containerRef.current;
    if (!container) return;

    const applyFilters = () => {
      const activePill = container.querySelector('.pill.active') as HTMLElement;
      const activeFilter = activePill ? activePill.dataset.filter : 'all';      
      const searchInput = container.querySelector('#searchInput') as HTMLInputElement;
      const query = searchInput ? searchInput.value.toLowerCase() : '';

      const cards = container.querySelectorAll('.q-card');
      if (cards) {
        cards.forEach((card: any) => {
          const diff = card.dataset.difficulty;
          const qText = card.querySelector('.q-question')?.textContent?.toLowerCase() || '';
          const bText = card.querySelector('.q-body')?.textContent?.toLowerCase() || '';

          const matchesDiff = activeFilter === 'all' || diff === activeFilter;  
          const matchesSearch = !query || qText.includes(query) || bText.includes(query);

          if (matchesDiff && matchesSearch) {
            card.classList.remove('hidden');
          } else {
            card.classList.add('hidden');
          }
        });
      }
    };

    const clickHandler = (e: MouseEvent) => {
      const target = e.target as HTMLElement;
      const header = target.closest('.q-top');
      if (header && container.contains(header)) {
        const card = header.closest('.q-card');
        if (card) {
          card.classList.toggle('open');
        }
        return;
      }
      
      const pill = target.closest('.pill');
      if (pill && container.contains(pill)) {
        const pills = container.querySelectorAll('.pill');
        pills.forEach(b => b.classList.remove('active'));
        pill.classList.add('active');
        applyFilters();
      }
    };

    container.addEventListener('click', clickHandler as EventListener);
    
    const searchInput = container.querySelector('#searchInput');
    if (searchInput) {
      searchInput.addEventListener('input', applyFilters);
    }

    return () => {
      container.removeEventListener('click', clickHandler as EventListener);
      if (searchInput) {
        searchInput.removeEventListener('input', applyFilters);
      }
    };
  }, []);

  return (
    <>
      
      <title>Top 100 Node.js Interview Questions & Answers</title>
      <meta name="description" content="Master Node.js with this comprehensive list of 100 interview questions, designed for beginners and advanced developers preparing for technical interviews." />
      <meta property="og:title" content="Top 100 Node.js Interview Questions & Answers" />
      <meta property="og:description" content="Master Node.js with this comprehensive list of 100 interview questions, designed for beginners and advanced developers preparing for technical interviews." />
      
      <div style={{ position: 'sticky', top: 0, zIndex: 1000, background: 'var(--bg, #fdfaf5)', padding: '12px 40px', borderBottom: '2px solid var(--border, #e8e0d0)' }}>
        <Link href="/" style={{ color: 'var(--accent, #e8601c)', fontWeight: 800, textDecoration: 'none', display: 'flex', alignItems: 'center', gap: '8px' }}>
          <span>&larr;</span> Back to Portfolio
        </Link>
      </div>
  <style dangerouslySetInnerHTML={{ __html: "\r\n  :root {\r\n    --bg: #f4fbf6;\r\n    --card: #ffffff;\r\n    --border: #b8dfc8;\r\n    --text: #0d2618;\r\n    --muted: #4a8060;\r\n    --accent: #1a7a40;\r\n    --accent2: #1a6fd4;\r\n    --easy-bg: #e8f7f0; --easy-text: #1a7a50;\r\n    --medium-bg: #fff4e0; --medium-text: #b06010;\r\n    --hard-bg: #fdeae4; --hard-text: #c0351a;\r\n    --tag-bg: #d4f0e0;\r\n  }\r\n  * { margin: 0; padding: 0; box-sizing: border-box; }\r\n  body { background: var(--bg); color: var(--text); font-family: 'Nunito', sans-serif; font-size: 16px; line-height: 1.7; }\r\n\r\n  header { background: var(--accent); padding: 52px 40px 44px; position: relative; overflow: hidden; }\r\n  header::after { content: ''; position: absolute; bottom: -40px; left: 0; right: 0; height: 80px; background: var(--bg); border-radius: 50% 50% 0 0 / 100% 100% 0 0; }\r\n  .header-emoji { font-size: 48px; display: block; margin-bottom: 12px; }\r\n  header h1 { font-size: clamp(1.8rem, 4vw, 3rem); font-weight: 900; color: #fff; line-height: 1.15; margin-bottom: 10px; }\r\n  header p { color: rgba(255,255,255,0.85); font-size: 16px; font-weight: 600; max-width: 560px; }\r\n  .header-stats { display: flex; gap: 20px; margin-top: 24px; flex-wrap: wrap; }\r\n  .hstat { background: rgba(255,255,255,0.2); border-radius: 12px; padding: 8px 18px; color: #fff; font-weight: 700; font-size: 14px; }\r\n\r\n  .controls { position: sticky; top: 0; z-index: 100; background: var(--bg); border-bottom: 2px solid var(--border); padding: 14px 40px; display: flex; gap: 10px; flex-wrap: wrap; align-items: center; }\r\n  .pill { padding: 7px 18px; border-radius: 999px; border: 2px solid var(--border); background: transparent; color: var(--muted); font-family: 'Nunito', sans-serif; font-size: 14px; font-weight: 700; cursor: pointer; transition: all 0.15s; }\r\n  .pill:hover { border-color: var(--accent); color: var(--accent); }\r\n  .pill.active { background: var(--accent); color: #fff; border-color: var(--accent); }\r\n  .pill.easy.active { background: var(--easy-text); border-color: var(--easy-text); }\r\n  .pill.medium.active { background: var(--medium-text); border-color: var(--medium-text); }\r\n  .pill.hard.active { background: var(--hard-text); border-color: var(--hard-text); }\r\n  .search-box { margin-left: auto; position: relative; }\r\n  .search-box input { background: var(--card); border: 2px solid var(--border); border-radius: 999px; padding: 8px 16px 8px 38px; font-family: 'Nunito', sans-serif; font-size: 14px; font-weight: 600; color: var(--text); width: 210px; outline: none; transition: border-color 0.2s; }\r\n  .search-box input:focus { border-color: var(--accent); }\r\n  .search-box::before { content: '🔍'; position: absolute; left: 12px; top: 50%; transform: translateY(-50%); font-size: 14px; }\r\n\r\n  main { max-width: 860px; margin: 0 auto; padding: 40px 32px 80px; }\r\n\r\n  .section-head { display: flex; align-items: center; gap: 12px; margin: 40px 0 18px; }\r\n  .section-icon { font-size: 28px; width: 48px; height: 48px; display: grid; place-items: center; background: var(--tag-bg); border-radius: 12px; flex-shrink: 0; }\r\n  .section-title { font-size: 1.25rem; font-weight: 900; color: var(--text); }\r\n  .section-line { flex: 1; height: 2px; background: var(--border); border-radius: 2px; }\r\n\r\n  .q-card { background: var(--card); border: 2px solid var(--border); border-radius: 16px; margin-bottom: 10px; overflow: hidden; transition: border-color 0.2s, box-shadow 0.2s; cursor: pointer; }\r\n  .q-card:hover { border-color: #80c8a0; box-shadow: 0 4px 16px rgba(0,0,0,0.06); }\r\n  .q-card.open { border-color: var(--accent); box-shadow: 0 4px 20px rgba(26,122,64,0.12); }\r\n\r\n  .q-top { display: flex; align-items: flex-start; gap: 12px; padding: 16px 20px; }\r\n  .q-num { font-family: 'Fira Code', monospace; font-size: 12px; color: var(--muted); min-width: 30px; padding-top: 2px; }\r\n  .q-question { flex: 1; font-size: 15px; font-weight: 700; color: var(--text); line-height: 1.45; }\r\n  .q-card.open .q-question { color: var(--accent); }\r\n  .badge { font-size: 11px; font-weight: 800; padding: 3px 10px; border-radius: 999px; flex-shrink: 0; margin-top: 1px; }\r\n  .b-easy { background: var(--easy-bg); color: var(--easy-text); }\r\n  .b-medium { background: var(--medium-bg); color: var(--medium-text); }\r\n  .b-hard { background: var(--hard-bg); color: var(--hard-text); }\r\n  .chevron { color: var(--muted); font-size: 18px; flex-shrink: 0; transition: transform 0.25s; }\r\n  .q-card.open .chevron { transform: rotate(180deg); color: var(--accent); }\r\n\r\n  .q-body { display: none; padding: 16px 20px 18px 62px; border-top: 2px solid var(--border); }\r\n  .q-card.open .q-body { display: block; }\r\n  .q-body p { font-size: 14.5px; color: #1e4030; margin-bottom: 10px; }\r\n  .q-body p:last-child { margin-bottom: 0; }\r\n\r\n  .simple-box { background: #edf7f1; border-left: 4px solid var(--accent); border-radius: 0 10px 10px 0; padding: 12px 16px; margin: 10px 0; font-size: 14px; color: #0d3820; font-weight: 600; }\r\n  .simple-box .label { font-size: 11px; text-transform: uppercase; letter-spacing: 1px; color: var(--accent); font-weight: 800; margin-bottom: 4px; display: block; }\r\n  .example-box { background: #eef4ff; border-left: 4px solid var(--accent2); border-radius: 0 10px 10px 0; padding: 12px 16px; margin: 10px 0; font-size: 14px; color: #0e2860; font-weight: 600; }\r\n  .example-box .label { font-size: 11px; text-transform: uppercase; letter-spacing: 1px; color: var(--accent2); font-weight: 800; margin-bottom: 4px; display: block; }\r\n\r\n  code { font-family: 'Fira Code', monospace; background: #d4f0e0; border-radius: 5px; padding: 1px 7px; font-size: 13px; color: #0d4020; }\r\n  pre { background: #061408; border-radius: 10px; padding: 14px 16px; overflow-x: auto; margin: 12px 0; font-family: 'Fira Code', monospace; font-size: 12.5px; color: #90e8a8; line-height: 1.7; }\r\n\r\n  .q-card.hidden { display: none; }\r\n  footer { text-align: center; padding: 32px; color: var(--muted); font-size: 13px; font-weight: 600; border-top: 2px solid var(--border); }\r\n\r\n  @media (max-width: 600px) {\r\n    header, .controls, main { padding-left: 16px; padding-right: 16px; }\r\n    .search-box { margin-left: 0; width: 100%; }\r\n    .search-box input { width: 100%; }\r\n    .q-body { padding-left: 20px; }\r\n  }\r\n" }} />
      <div ref={containerRef} dangerouslySetInnerHTML={{ __html: "\r\n\r\n\r\n\r\n\r\n<header>\r\n  <span class=\"header-emoji\">🟢</span>\r\n  <h1>100 Node.js Interview Questions</h1>\r\n  <p>Every concept covered deeply — event loop, streams, modules, Express, async, security, scaling & more. Simple English!</p>\r\n  <div class=\"header-stats\">\r\n    <span class=\"hstat\">100 Questions</span>\r\n    <span class=\"hstat\">12 Deep Topics</span>\r\n    <span class=\"hstat\">Every Concept ✓</span>\r\n    <span class=\"hstat\">Simple English ✓</span>\r\n  </div>\r\n</header>\r\n\r\n<div class=\"controls\">\r\n  <button class=\"pill active\" data-filter=\"all\">All</button>\r\n  <button class=\"pill easy\" data-filter=\"easy\">🟢 Easy</button>\r\n  <button class=\"pill medium\" data-filter=\"medium\">🟡 Medium</button>\r\n  <button class=\"pill hard\" data-filter=\"hard\">🔴 Hard</button>\r\n  <div class=\"search-box\"><input type=\"text\" id=\"searchInput\" placeholder=\"Search questions…\"></div>\r\n</div>\r\n\r\n<main id=\"main\">\r\n\r\n<!-- ========== SECTION 1: CORE CONCEPTS ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🌱</span><h2 class=\"section-title\">Core Concepts</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">01</span><span class=\"q-question\">What is Node.js and why do developers use it?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Node.js is a JavaScript runtime built on Chrome's V8 engine. It lets you run JavaScript <strong>outside the browser</strong> — on a server. Before Node.js, JavaScript only ran inside web browsers. Now you can use it to build backends, APIs, tools, and real-time apps.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Analogy 🚂</span>Think of JavaScript as a train. The browser was the only track it could run on. Node.js built a whole new track — now the same JavaScript train can run on servers, read files, connect to databases, and handle thousands of users at once.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">02</span><span class=\"q-question\">What is non-blocking I/O and why is it important?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Non-blocking I/O means Node.js does NOT wait for slow operations (reading files, querying databases, calling APIs). It starts the operation, moves on to handle other things, and comes back when the result is ready.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Analogy 🍽️</span>A waiter takes orders from 10 tables, sends them all to the kitchen, and handles whoever's food is ready first. He never stands at one table waiting. Node.js is that waiter — one thread serving thousands of requests at once.</div>\r\n    <pre>// Blocking — freezes while waiting:\r\nconst data = fs.readFileSync('bigfile.txt'); // waits here!\r\nconsole.log('only runs AFTER file is done');\r\n\r\n// Non-blocking — moves on immediately:\r\nfs.readFile('bigfile.txt', (err, data) => {\r\n  console.log('file is ready now!');\r\n});\r\nconsole.log('runs IMMEDIATELY while file loads');</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">03</span><span class=\"q-question\">What is the difference between Node.js and the browser?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Both run JavaScript with V8, but they have completely different APIs. The browser has the DOM, window, localStorage. Node.js has the file system, networking, OS access — but no DOM.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Side by side 🆚</span>\r\n      <strong>Browser has</strong>: <code>window</code>, <code>document</code>, <code>localStorage</code>, <code>fetch</code>, DOM APIs — all for UI.<br><br>\r\n      <strong>Node.js has</strong>: <code>global</code> (not window), <code>process</code>, <code>__dirname</code>, <code>require()</code>, <code>fs</code> (file system), <code>http</code>, <code>os</code>, <code>crypto</code> — all for servers.<br><br>\r\n      Browser: sandboxed — cannot touch your files or system. Node.js: full system access — can read/write files, run commands, connect to databases.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">04</span><span class=\"q-question\">What is the global object in Node.js?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>In the browser, everything global lives on <code>window</code>. In Node.js, it lives on <code>global</code>. Variables added to <code>global</code> are accessible everywhere in your app — but avoid doing this, it creates messy code.</div>\r\n    <pre>// Important Node.js globals you use constantly:\r\n__dirname   // full path to the current file's folder\r\n__filename  // full path to the current file\r\nprocess     // info about the running Node.js process\r\nrequire()   // function to import other modules\r\nmodule      // info about the current module\r\nexports     // what this module shares with others\r\n\r\n// NOTE: __dirname and __filename do NOT exist in ES modules!\r\n// In ESM use: new URL('.', import.meta.url).pathname</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">05</span><span class=\"q-question\">What is the process object in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>process</code> is a global object that gives you information and control over the current Node.js process — environment variables, command-line arguments, memory, and the ability to exit cleanly.</div>\r\n    <pre>process.env.NODE_ENV      // 'production' or 'development'\r\nprocess.env.PORT          // your custom env variables\r\nprocess.argv              // command-line args array\r\nprocess.cwd()             // current working directory\r\nprocess.pid               // process ID (a number)\r\nprocess.version           // Node.js version: 'v20.5.0'\r\nprocess.platform          // 'linux', 'darwin', 'win32'\r\nprocess.memoryUsage()     // { heapUsed, heapTotal, rss, external }\r\nprocess.uptime()          // seconds since app started\r\n\r\n// Exit the process:\r\nprocess.exit(0)  // 0 = success\r\nprocess.exit(1)  // 1 = error\r\n\r\n// Catch fatal errors — prevents crash:\r\nprocess.on('uncaughtException', (err) => {\r\n  console.error('Fatal error:', err);\r\n  process.exit(1); // must exit after this!\r\n});\r\nprocess.on('unhandledRejection', (reason) => {\r\n  console.error('Unhandled promise rejection:', reason);\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">06</span><span class=\"q-question\">What is single-threaded in Node.js and is it a problem?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Node.js runs JavaScript on a <strong>single thread</strong> — only one piece of code runs at a time. This is NOT a problem for I/O (files, network, databases) because Node offloads those to the OS. It IS a problem for CPU-heavy work like image processing or math.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Why single-thread is OK for most apps 👇</span>\r\n      90% of web server work is waiting — waiting for the database, waiting for the API, waiting for the file. Node.js hands all that waiting off to the OS and serves other requests in the meantime.<br><br>\r\n      The only problem: if you do heavy math on the main thread for 2 seconds, ALL other requests are frozen for 2 seconds. Solutions: <strong>Worker Threads</strong>, <strong>Child Processes</strong>, or <strong>Cluster module</strong>.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">07</span><span class=\"q-question\">What is V8 and what role does it play in Node.js?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>V8 is Google's JavaScript engine — the same one that runs JavaScript in Chrome. Node.js uses V8 to run your JavaScript code. V8 compiles JavaScript directly to machine code (JIT compilation), which makes Node.js fast.</div>\r\n    <div class=\"example-box\"><span class=\"label\">How it all fits together 🧩</span>\r\n      <strong>V8</strong>: runs your JavaScript, compiles it to machine code.<br>\r\n      <strong>libuv</strong>: provides the event loop, thread pool, and async I/O (file, network).<br>\r\n      <strong>Node.js bindings</strong>: connects V8 + libuv and adds built-in modules (fs, http, etc.).<br><br>\r\n      When you write <code>fs.readFile()</code>, Node.js tells libuv to read the file (in a background thread), and V8 keeps running your other JavaScript while it waits.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">08</span><span class=\"q-question\">What is libuv and what does it do?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>libuv is a C library that powers Node.js's async I/O and event loop. It provides a <strong>thread pool</strong> for operations that can't be done async at the OS level — like file system, DNS lookups, and crypto.</div>\r\n    <div class=\"example-box\"><span class=\"label\">What libuv manages 👇</span>\r\n      The <strong>Event Loop</strong> — the main loop that picks up callbacks and runs them.<br>\r\n      The <strong>Thread Pool</strong> — 4 threads by default for file I/O, crypto, DNS.<br>\r\n      OS-level async APIs — epoll (Linux), kqueue (macOS), IOCP (Windows).<br><br>\r\n      When you call <code>fs.readFile()</code>: libuv hands it to a thread pool thread. That thread blocks (it reads from disk). When done, libuv pushes the callback into the event loop queue. Your JS thread picks it up and runs the callback.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 2: EVENT LOOP (DEEP DIVE) ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🔄</span><h2 class=\"section-title\">The Event Loop (Deep Dive)</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">09</span><span class=\"q-question\">What is the Event Loop in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The Event Loop is the heart of Node.js. It continuously checks: \"Is there any work to do?\" It picks up completed async operations (file reads, network responses, timers) and runs their callbacks on the single JavaScript thread.</div>\r\n    <div class=\"example-box\"><span class=\"label\">How it works step by step 🔄</span>\r\n      1. Your code runs synchronously (the call stack).<br>\r\n      2. Async operations start (setTimeout, fs.readFile, fetch) — handed off to OS/thread pool.<br>\r\n      3. Node.js is free to run other code while waiting.<br>\r\n      4. When an async operation completes, its callback is put in a queue.<br>\r\n      5. The Event Loop checks — is the call stack empty? If yes, pick a callback from the queue and run it.<br>\r\n      6. Repeat forever until there's nothing left to do.\r\n    </div>\r\n    <div class=\"simple-box\"><span class=\"label\">Why this enables handling thousands of connections 💡</span>A traditional server creates a new thread per connection (expensive — threads use ~1MB RAM each). Node.js handles all connections on one thread via the event loop. 10,000 connections = 10,000 callbacks waiting in a queue — no 10,000 threads needed!</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">10</span><span class=\"q-question\">What are the phases of the Event Loop?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The event loop runs in 6 phases, each with its own queue. It processes all callbacks in one phase before moving to the next. Between each phase, it drains the microtask queue (Promises + nextTick).</div>\r\n    <div class=\"example-box\"><span class=\"label\">The 6 phases in order 📋</span>\r\n      1. <strong>timers</strong>: runs <code>setTimeout</code> and <code>setInterval</code> callbacks whose time has expired.<br>\r\n      2. <strong>pending callbacks</strong>: I/O callbacks deferred from the previous loop iteration.<br>\r\n      3. <strong>idle, prepare</strong>: internal Node.js use only — you can ignore this.<br>\r\n      4. <strong>poll</strong>: retrieves new I/O events and runs their callbacks. If nothing to do, waits here for new events.<br>\r\n      5. <strong>check</strong>: runs <code>setImmediate()</code> callbacks.<br>\r\n      6. <strong>close callbacks</strong>: runs cleanup callbacks like <code>socket.on('close')</code>.<br><br>\r\n      <strong>Between every phase</strong>: Node.js runs the microtask queue — all <code>process.nextTick()</code> callbacks first, then all Promise <code>.then()</code> callbacks. These have the highest priority!\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">11</span><span class=\"q-question\">What is the exact execution order: sync → nextTick → Promise → setTimeout → setImmediate?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The order is: <strong>synchronous code</strong> → <strong>process.nextTick</strong> → <strong>Promises (.then)</strong> → <strong>setTimeout/setInterval</strong> → <strong>setImmediate</strong>.</div>\r\n    <pre>console.log('1 - sync start');\r\n\r\nsetTimeout(() => console.log('5 - setTimeout'), 0);\r\nsetImmediate(() => console.log('6 - setImmediate'));\r\n\r\nPromise.resolve().then(() => console.log('3 - Promise'));\r\nprocess.nextTick(() => console.log('2 - nextTick'));\r\n\r\nconsole.log('4 - sync end');\r\n\r\n// Output: 1, 4, 2, 3, 5, 6\r\n// (note: sync first, then nextTick, then Promise,\r\n//  then macrotasks: setTimeout then setImmediate)</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Why this order? 🤔</span>\r\n      Sync code runs first — it's on the call stack, nothing else can run.<br>\r\n      <code>process.nextTick</code> runs before Promises — it's a special \"micro-microtask\".<br>\r\n      Promises run — they're microtasks, cleared before returning to the event loop.<br>\r\n      <code>setTimeout(0)</code> — macrotask, runs in the timers phase.<br>\r\n      <code>setImmediate</code> — runs in the check phase (after poll), often after setTimeout.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">12</span><span class=\"q-question\">What is process.nextTick() and when should you use it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>process.nextTick()</code> schedules a callback to run <strong>at the end of the current operation</strong>, before the event loop continues — even before Promises. It's the highest priority async callback in Node.js.</div>\r\n    <pre>console.log('start');\r\nprocess.nextTick(() => console.log('nextTick'));\r\nPromise.resolve().then(() => console.log('Promise'));\r\nconsole.log('end');\r\n// Output: start → end → nextTick → Promise\r\n// nextTick runs before Promise!</pre>\r\n    <div class=\"example-box\"><span class=\"label\">When to use it 👇</span>\r\n      ✅ Emit events after a constructor returns (so listeners can be attached first).<br>\r\n      ✅ Throw errors asynchronously after setting up something synchronously.<br>\r\n      ✅ Ensure a callback runs in a consistent async manner.<br><br>\r\n      ⚠️ <strong>Warning</strong>: Don't use it recursively — if nextTick keeps scheduling new nextTick calls, the event loop starves. I/O callbacks never run and your server freezes!\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">13</span><span class=\"q-question\">What is the difference between process.nextTick() and setImmediate()?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Both schedule a callback — but at different points. <code>process.nextTick</code> runs before the event loop continues (highest priority). <code>setImmediate</code> runs in the check phase of the NEXT event loop iteration (after I/O callbacks).</div>\r\n    <div class=\"example-box\"><span class=\"label\">Which one to use? 👇</span>\r\n      Use <strong>process.nextTick</strong>: when you need something to run before any I/O. Rare — usually only for library internals or error handling patterns.<br><br>\r\n      Use <strong>setImmediate</strong>: when you want to break up CPU work or defer something to after I/O. Safer — it doesn't starve I/O the way nextTick can.<br><br>\r\n      The Node.js team recommends <code>setImmediate</code> over <code>process.nextTick</code> in most cases because it's less likely to cause issues.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">14</span><span class=\"q-question\">What is the Thread Pool in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Node.js has a background thread pool (managed by libuv) with <strong>4 threads by default</strong>. These threads handle operations that can't be done async at the OS level — like file I/O, DNS lookups, crypto, and zlib compression.</div>\r\n    <div class=\"example-box\"><span class=\"label\">What uses the thread pool 👇</span>\r\n      <code>fs.readFile</code>, <code>fs.writeFile</code> — file system operations.<br>\r\n      <code>crypto.pbkdf2</code>, <code>crypto.scrypt</code>, <code>crypto.randomBytes</code> — crypto hashing.<br>\r\n      <code>dns.lookup</code> (but NOT <code>dns.resolve</code> — that's async!).<br>\r\n      <code>zlib</code> — compression and decompression.<br><br>\r\n      <strong>Performance gotcha</strong>: If 4 bcrypt password hashes run at once, the 5th must wait. Your server has 8 CPU cores doing nothing while 1 thread pool thread is busy. Fix: increase <code>UV_THREADPOOL_SIZE=8</code> environment variable.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 3: MODULES ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">📦</span><h2 class=\"section-title\">Modules — CommonJS & ES Modules</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">15</span><span class=\"q-question\">What is the CommonJS module system (require/module.exports)?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>CommonJS is Node.js's built-in module system. Use <code>require()</code> to import code from other files. Use <code>module.exports</code> to share code from a file. This is the default in Node.js — no setup needed.</div>\r\n    <pre>// math.js — exporting:\r\nfunction add(a, b) { return a + b; }\r\nconst PI = 3.14159;\r\nmodule.exports = { add, PI };\r\n\r\n// app.js — importing:\r\nconst { add, PI } = require('./math');   // your own file\r\nconst fs = require('fs');                // built-in module\r\nconst express = require('express');      // npm package\r\n\r\n// How require resolves files:\r\n// './math'    → looks for ./math.js, ./math/index.js\r\n// 'express'   → looks in node_modules/express\r\n// 'fs', 'http' → built-in Node.js modules</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Module caching ⚡</span>Node.js caches every module after the first <code>require()</code>. So requiring the same module 10 times only runs its code once — the same object is returned every time. This makes modules act like <strong>singletons</strong> — perfect for database connections!</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">16</span><span class=\"q-question\">What is the difference between module.exports and exports?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>exports</code> is a shortcut reference that initially points to the same object as <code>module.exports</code>. If you <em>reassign</em> <code>exports</code>, it breaks the connection — <code>module.exports</code> is what actually gets exported.</div>\r\n    <pre>// Adding to the object — both work the same:\r\nexports.add = (a, b) => a + b;    // ✅\r\nmodule.exports.add = (a, b) => a + b; // ✅ same thing\r\n\r\n// Reassigning — only module.exports works:\r\nmodule.exports = { add, PI };    // ✅ works\r\nexports = { add, PI };           // ❌ breaks the link!\r\n// exports now points to a NEW object\r\n// module.exports is still {} — nothing gets exported!</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Simple rule 📏</span>\r\n      Adding properties → use <code>exports.thing = value</code> (shortcut is fine).<br>\r\n      Replacing entirely → use <code>module.exports = value</code>.<br>\r\n      When in doubt → <strong>always use module.exports</strong>. It always works.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">17</span><span class=\"q-question\">What are ES Modules (import/export) in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>ES Modules are the modern JavaScript standard — using <code>import</code>/<code>export</code> syntax. Node.js supports them from v12+. They're the same syntax used in the browser. Enable them with <code>.mjs</code> extension or <code>\"type\": \"module\"</code> in package.json.</div>\r\n    <pre>// math.mjs — named exports:\r\nexport const add = (a, b) => a + b;\r\nexport const PI = 3.14159;\r\n\r\n// default export (one per file):\r\nexport default function multiply(a, b) { return a * b; }\r\n\r\n// importing:\r\nimport multiply, { add, PI } from './math.js';\r\nimport * as MathUtils from './math.js'; // import all as object\r\nimport { add as sum } from './math.js'; // rename on import</pre>\r\n    <div class=\"example-box\"><span class=\"label\">CJS vs ESM key differences 🆚</span>\r\n      <code>__dirname</code> / <code>__filename</code>: available in CJS, NOT in ESM (use <code>import.meta.url</code>).<br>\r\n      <code>require()</code>: CJS only. ESM uses <code>import()</code> (dynamic import).<br>\r\n      Top-level <code>await</code>: ESM only — you can await at the top level without async!<br>\r\n      Loading: CJS = synchronous. ESM = asynchronous (allows static analysis, tree-shaking).<br>\r\n      Exports: CJS exports a copy. ESM exports live bindings (the importer sees changes).\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">18</span><span class=\"q-question\">What are the built-in (core) modules in Node.js?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Node.js ships with built-in modules you can use without installing anything from npm. They cover file system, networking, crypto, path handling, and more.</div>\r\n    <div class=\"example-box\"><span class=\"label\">The most important core modules 👇</span>\r\n      <code>fs</code> / <code>fs/promises</code> — read, write, delete files and folders.<br>\r\n      <code>path</code> — join and resolve file paths cross-platform.<br>\r\n      <code>http</code> / <code>https</code> — create HTTP servers, make HTTP requests.<br>\r\n      <code>events</code> — EventEmitter class (base for most of Node.js).<br>\r\n      <code>stream</code> — handle streaming data (Readable, Writable, Transform).<br>\r\n      <code>crypto</code> — hashing, encryption, random values.<br>\r\n      <code>os</code> — CPU count, memory, hostname, platform.<br>\r\n      <code>child_process</code> — run shell commands, other programs.<br>\r\n      <code>worker_threads</code> — true multi-threading.<br>\r\n      <code>cluster</code> — run multiple processes on multiple CPU cores.<br>\r\n      <code>url</code> — parse and format URLs.<br>\r\n      <code>util</code> — promisify, inspect, and other helpers.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 4: STREAMS ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🌊</span><h2 class=\"section-title\">Streams (Deep Dive)</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">19</span><span class=\"q-question\">What are streams in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Streams let you process data piece by piece (in <strong>chunks</strong>) instead of loading all of it into memory at once. This is crucial for large files, video, or real-time data — you process data as it arrives.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Analogy 🚿</span>Watching Netflix — you don't download the entire 4GB movie before watching. It streams to you chunk by chunk as you watch. Node.js streams work the same: process a 10GB log file chunk by chunk instead of loading 10GB into RAM at once.</div>\r\n    <div class=\"example-box\"><span class=\"label\">The four types of streams 👇</span>\r\n      <strong>Readable</strong>: source you read FROM. Example: reading a file, incoming HTTP request body.<br>\r\n      <strong>Writable</strong>: destination you write TO. Example: writing a file, HTTP response.<br>\r\n      <strong>Duplex</strong>: both readable and writable. Example: TCP socket — sends and receives.<br>\r\n      <strong>Transform</strong>: duplex that transforms data passing through. Example: gzip compression, encryption.\r\n    </div>\r\n    <pre>const fs = require('fs');\r\n\r\n// Without streams — loads entire 2GB file into RAM!\r\nconst data = fs.readFileSync('2gb-log.txt'); // ❌ crashes!\r\n\r\n// With streams — uses fixed small amount of memory:\r\nconst readStream = fs.createReadStream('2gb-log.txt');\r\nconst writeStream = fs.createWriteStream('output.txt');\r\nreadStream.pipe(writeStream); // ✅ handles any file size!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">20</span><span class=\"q-question\">What is pipe() in streams and how does it work?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>pipe()</code> connects a Readable stream to a Writable stream — data flows automatically from one to the other. It also handles <strong>backpressure</strong> for you: if the writer is slow, it pauses the reader so memory doesn't overflow.</div>\r\n    <pre>const fs = require('fs');\r\nconst zlib = require('zlib');\r\n\r\n// Chain multiple pipes — compress a file:\r\nfs.createReadStream('large-file.txt')   // read file\r\n  .pipe(zlib.createGzip())              // compress it\r\n  .pipe(fs.createWriteStream('large-file.txt.gz')); // write compressed\r\n\r\n// Handle errors in pipe chain:\r\nconst { pipeline } = require('stream/promises');\r\nawait pipeline(\r\n  fs.createReadStream('input.txt'),\r\n  zlib.createGzip(),\r\n  fs.createWriteStream('output.gz')\r\n);\r\n// pipeline() is better than pipe() — handles errors properly!</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Use pipeline() over pipe() 📏</span><code>pipe()</code> doesn't forward errors — if the source errors, the destination keeps open and leaks. <code>pipeline()</code> from <code>stream/promises</code> properly closes all streams when any one errors. Always prefer <code>pipeline()</code> in production code.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">21</span><span class=\"q-question\">What is backpressure in streams and why does it matter?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Backpressure happens when data is being produced faster than it can be consumed — the buffer fills up and memory spikes. Streams handle this by signaling \"slow down\" to the producer. <code>pipe()</code> does this automatically.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Analogy 🚰</span>A fire hose filling a bathtub faster than the drain can empty it — eventually the tub overflows (memory crash). Backpressure is the drain saying \"slow down the hose.\" <code>pipe()</code> handles this automatically: when the write buffer is full, it pauses reading.</div>\r\n    <pre>// Manual backpressure — what pipe() does internally:\r\nreadStream.on('data', (chunk) => {\r\n  const canContinue = writeStream.write(chunk);\r\n  if (!canContinue) {\r\n    readStream.pause(); // stop reading — write buffer full!\r\n  }\r\n});\r\nwriteStream.on('drain', () => {\r\n  readStream.resume(); // write buffer cleared — keep reading!\r\n});\r\n\r\n// Just use pipe() — it handles all of this for you:\r\nreadStream.pipe(writeStream);</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">22</span><span class=\"q-question\">How do you create a custom Transform stream?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Extend the <code>Transform</code> class and implement <code>_transform(chunk, encoding, callback)</code>. This lets you process data as it flows through — like filtering lines, converting formats, or encrypting on the fly.</div>\r\n    <pre>const { Transform } = require('stream');\r\n\r\n// Custom transform: uppercase every line:\r\nclass UpperCaseTransform extends Transform {\r\n  _transform(chunk, encoding, callback) {\r\n    // chunk is a Buffer — convert to string, process, push result:\r\n    const upperCased = chunk.toString().toUpperCase();\r\n    this.push(upperCased); // send to the next stream\r\n    callback(); // signal we're done with this chunk\r\n  }\r\n}\r\n\r\n// Use it in a pipeline:\r\nconst { pipeline } = require('stream/promises');\r\nawait pipeline(\r\n  fs.createReadStream('input.txt'),\r\n  new UpperCaseTransform(), // transform in the middle\r\n  fs.createWriteStream('output.txt')\r\n);</pre>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 5: EVENTS ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">📡</span><h2 class=\"section-title\">Events & EventEmitter</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">23</span><span class=\"q-question\">What is EventEmitter in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>EventEmitter is the core pub/sub system in Node.js. Objects can <strong>emit</strong> named events. Other code can <strong>listen</strong> to those events. Almost everything in Node.js (streams, HTTP server, process) is built on EventEmitter.</div>\r\n    <pre>const EventEmitter = require('events');\r\n\r\nclass OrderService extends EventEmitter {\r\n  async placeOrder(item) {\r\n    const order = await saveToDatabase(item);\r\n    this.emit('orderPlaced', order); // fire the event!\r\n    return order;\r\n  }\r\n}\r\n\r\nconst service = new OrderService();\r\n\r\n// Subscribe — multiple listeners can listen to same event:\r\nservice.on('orderPlaced', (order) => sendConfirmationEmail(order));\r\nservice.on('orderPlaced', (order) => updateInventory(order));\r\nservice.on('orderPlaced', (order) => notifyWarehouse(order));\r\n\r\n// Run only once then auto-removes:\r\nservice.once('orderPlaced', sendWelcomeBonus);\r\n\r\n// Unsubscribe:\r\nservice.off('orderPlaced', handler);  // modern (Node 10+)\r\nservice.removeListener('orderPlaced', handler); // older way\r\n\r\nawait service.placeOrder('Pizza'); // all 3 listeners fire!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">24</span><span class=\"q-question\">What is the 'error' event and why is it special?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The <code>'error'</code> event is special in Node.js. If an EventEmitter emits <code>'error'</code> and nobody is listening — Node.js <strong>throws the error and crashes the process</strong>. Always add an error listener on EventEmitters!</div>\r\n    <pre>const emitter = new EventEmitter();\r\n\r\n// DANGEROUS — no error listener:\r\nemitter.emit('error', new Error('boom'));\r\n// ❌ Uncaught Error: process crashes!\r\n\r\n// SAFE — always handle errors:\r\nemitter.on('error', (err) => {\r\n  console.error('Handled:', err.message); // logs, doesn't crash!\r\n});\r\nemitter.emit('error', new Error('boom')); // ✅ handled\r\n\r\n// This is why you always see:\r\nserver.on('error', handler);\r\nstream.on('error', handler);\r\nsocket.on('error', handler);</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Common gotcha ⚠️</span>Streams are EventEmitters too. If you forget <code>.on('error', handler)</code> on a stream and a read fails (file not found, network drop), Node.js crashes. With <code>pipeline()</code> this is handled automatically — one more reason to prefer pipeline over manual piping.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">25</span><span class=\"q-question\">What is the maximum number of listeners and memory leak warning?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>By default, EventEmitter warns if more than <strong>10 listeners</strong> are added to the same event. This is a memory leak detection feature — you probably forgot to remove old listeners in a loop.</div>\r\n    <pre>const emitter = new EventEmitter();\r\n\r\n// Node.js warns after 10 listeners:\r\nfor (let i = 0; i < 15; i++) {\r\n  emitter.on('data', handler); // after 10: MaxListenersExceededWarning!\r\n}\r\n\r\n// Fix 1 — increase the limit if intentional:\r\nemitter.setMaxListeners(20);\r\nEventEmitter.defaultMaxListeners = 20; // for all emitters\r\n\r\n// Fix 2 — remove listeners when done:\r\nfunction handleData(data) { /* ... */ }\r\nemitter.on('data', handleData);\r\n// Later when done:\r\nemitter.off('data', handleData); // removes THIS specific listener\r\n\r\n// Check listener count:\r\nemitter.listenerCount('data'); // number of listeners for 'data'\r\nemitter.eventNames();          // ['data', 'error', ...]</pre>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 6: ASYNC PATTERNS ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">⏳</span><h2 class=\"section-title\">Async Patterns (Deep Dive)</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">26</span><span class=\"q-question\">What are callbacks and what is callback hell?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A callback is a function you pass to another function, to be called when an async operation is done. Callback hell is when you nest callbacks inside callbacks inside callbacks — creating a hard-to-read \"pyramid of doom.\"</div>\r\n    <pre>// Callback hell — nesting makes this unreadable:\r\ngetUser(id, function(err, user) {\r\n  if (err) return handleError(err);\r\n  getPosts(user.id, function(err, posts) {\r\n    if (err) return handleError(err);\r\n    getComments(posts[0].id, function(err, comments) {\r\n      if (err) return handleError(err);\r\n      // 4 levels deep, 3 error handlers, total mess!\r\n    });\r\n  });\r\n});</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Problems with callbacks 👇</span>\r\n      ❌ Deep nesting — hard to read and maintain.<br>\r\n      ❌ Error handling on every callback — repeated code.<br>\r\n      ❌ Can't use try/catch — errors don't propagate.<br>\r\n      ❌ Hard to run things in parallel.<br>\r\n      ✅ Solution: <strong>Promises</strong> and <strong>async/await</strong>.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">27</span><span class=\"q-question\">What is util.promisify() and when do you use it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>util.promisify()</code> converts an old-style callback function into a Promise-returning function, so you can use it with async/await. Works on any function that follows the Node.js callback convention: <code>(err, result)</code> as the last argument.</div>\r\n    <pre>const util = require('util');\r\nconst fs = require('fs');\r\n\r\n// Old callback style — can't use await:\r\nfs.readFile('file.txt', 'utf8', (err, data) => {\r\n  if (err) throw err;\r\n  console.log(data);\r\n});\r\n\r\n// Convert to Promise:\r\nconst readFile = util.promisify(fs.readFile);\r\n\r\n// Now use async/await:\r\nconst data = await readFile('file.txt', 'utf8');\r\nconsole.log(data);\r\n\r\n// Tip: most Node.js APIs now have a promises version built in:\r\nconst { readFile } = require('fs/promises');    // CJS\r\nimport { readFile } from 'node:fs/promises';    // ESM\r\n// So you often don't even need util.promisify anymore!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">28</span><span class=\"q-question\">What is the difference between sequential and parallel async operations?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><strong>Sequential</strong>: operations run one after another — next starts only after previous finishes. <strong>Parallel</strong>: all operations start at the same time — you wait for all to complete. Parallel is much faster when operations don't depend on each other.</div>\r\n    <pre>// Sequential — slow! Each waits for the previous (600ms total):\r\nasync function sequential() {\r\n  const user  = await fetchUser(1);   // 200ms\r\n  const posts = await fetchPosts(2);  // 200ms\r\n  const tags  = await fetchTags(3);   // 200ms\r\n}\r\n\r\n// Parallel — fast! All start at the same time (200ms total):\r\nasync function parallel() {\r\n  const [user, posts, tags] = await Promise.all([\r\n    fetchUser(1),   // starts immediately\r\n    fetchPosts(2),  // starts immediately\r\n    fetchTags(3),   // starts immediately\r\n  ]);\r\n}\r\n\r\n// Parallel with concurrency limit:\r\n// Don't hammer an API with 1000 requests at once!\r\nasync function limitedParallel(ids) {\r\n  const chunks = [];\r\n  for (let i = 0; i < ids.length; i += 5) {\r\n    chunks.push(ids.slice(i, i + 5));\r\n  }\r\n  const results = [];\r\n  for (const chunk of chunks) {\r\n    const chunkResults = await Promise.all(chunk.map(fetchItem));\r\n    results.push(...chunkResults);\r\n  }\r\n  return results;\r\n}</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">29</span><span class=\"q-question\">How do you handle errors properly with async/await in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Wrap <code>await</code> calls in <code>try/catch</code>. Async errors behave like synchronous errors inside async functions — catch handles both. The most common mistake is forgetting try/catch and getting unhandled Promise rejections.</div>\r\n    <pre>// ✅ Correct — try/catch catches async errors:\r\nasync function getUser(id) {\r\n  try {\r\n    const user = await User.findById(id);\r\n    return user;\r\n  } catch (err) {\r\n    throw new Error(`Failed to get user: ${err.message}`);\r\n  }\r\n}\r\n\r\n// ❌ Common mistake — try/catch doesn't catch async:\r\nfunction badHandler(req, res) {\r\n  try {\r\n    fetchData().then(data => res.json(data)); // .then is async!\r\n    // if fetchData rejects, try/catch MISSES it!\r\n  } catch (err) {} // never runs for Promise errors!\r\n}\r\n\r\n// ✅ Fix — await the Promise:\r\nasync function goodHandler(req, res, next) {\r\n  try {\r\n    const data = await fetchData(); // await makes it catchable!\r\n    res.json(data);\r\n  } catch (err) {\r\n    next(err); // pass to Express error handler\r\n  }\r\n}\r\n\r\n// Useful pattern — wrap to avoid repetitive try/catch:\r\nconst safe = async (promise) => {\r\n  try { return [null, await promise]; }\r\n  catch (e) { return [e, null]; }\r\n};\r\nconst [err, data] = await safe(fetchData());\r\nif (err) return handleError(err);</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">30</span><span class=\"q-question\">What is the race condition problem in async code and how do you fix it?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A race condition happens when multiple async operations are in flight and responses arrive out of order — the UI or data can show stale/wrong results. The most common example: typing in a search box, older results arriving after newer ones.</div>\r\n    <pre>// RACE CONDITION — older query result overwrites newer:\r\nlet currentResults = null;\r\n\r\nasync function search(query) {\r\n  const results = await fetchResults(query); // async!\r\n  currentResults = results; // what if an older fetch finishes LAST?\r\n}\r\n\r\n// user types: 're' then 'react'\r\n// Both fetches start. 'react' resolves first (maybe cached).\r\n// Then 're' resolves and OVERWRITES with wrong data!\r\n\r\n// FIX 1 — cancel old requests with AbortController:\r\nlet currentController = null;\r\nasync function search(query) {\r\n  if (currentController) currentController.abort(); // cancel previous\r\n  currentController = new AbortController();\r\n  try {\r\n    const results = await fetchResults(query, currentController.signal);\r\n    currentResults = results;\r\n  } catch (err) {\r\n    if (err.name === 'AbortError') return; // expected — ignore\r\n    throw err;\r\n  }\r\n}\r\n\r\n// FIX 2 — ignore stale responses with a sequence counter:\r\nlet requestId = 0;\r\nasync function search(query) {\r\n  const id = ++requestId;\r\n  const results = await fetchResults(query);\r\n  if (id !== requestId) return; // a newer request was made — ignore this\r\n  currentResults = results;\r\n}</pre>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 7: HTTP & EXPRESS ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🌐</span><h2 class=\"section-title\">HTTP Server & Express.js</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n  <div class=\"q-top\"><span class=\"q-num\">31</span><span class=\"q-question\">How does Express.js middleware work?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Middleware is a function with three parameters — <code>(req, res, next)</code>. It runs between the request arriving and the response being sent. Each middleware can modify the request, send a response, or call <code>next()</code> to pass control to the next middleware.</div>\r\n    <pre>// Middleware signature:\r\nfunction logger(req, res, next) {\r\n  console.log(`${req.method} ${req.url}`);\r\n  next(); // MUST call next() or the request hangs forever!\r\n}\r\n\r\nfunction auth(req, res, next) {\r\n  const token = req.headers.authorization;\r\n  if (!token) return res.status(401).json({ error: 'No token' });\r\n  req.user = verifyToken(token); // attach data to request\r\n  next();\r\n}\r\n\r\n// Apply globally — runs for every request:\r\napp.use(express.json()); // parse JSON bodies\r\napp.use(logger);\r\n\r\n// Apply to specific routes only:\r\napp.get('/profile', auth, (req, res) => {\r\n  res.json(req.user); // auth middleware added this!\r\n});\r\n\r\n// Error-handling middleware — MUST have 4 parameters:\r\napp.use((err, req, res, next) => {\r\n  console.error(err.stack);\r\n  res.status(err.status || 500).json({ error: err.message });\r\n});</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Middleware order matters! 📋</span>Express runs middleware in the order you call <code>app.use()</code>. Error handlers MUST go last. Auth middleware must be placed BEFORE the routes it protects. If you define a route before adding body parsing middleware, the body won't be available.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">32</span><span class=\"q-question\">What is the exact order Express executes middleware and routes?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Express runs code in the exact order it's registered. Every call to <code>app.use()</code>, <code>app.get()</code>, etc. adds to the stack. The request travels through them one by one until a response is sent or error handler is reached.</div>\r\n    <pre>// Request travels top to bottom:\r\napp.use(express.json());    // 1. parse body\r\napp.use(cors());            // 2. CORS headers\r\napp.use(logger);            // 3. log the request\r\napp.use(authenticate);      // 4. verify JWT token\r\n\r\napp.get('/users', handler); // 5. handle GET /users\r\napp.post('/users', handler);// 5. handle POST /users\r\n\r\n// Error handler — LAST, after all routes:\r\napp.use((err, req, res, next) => res.status(500).json({ err }));\r\n\r\n// If no route matches — 404 handler (also last):\r\napp.use((req, res) => res.status(404).json({ error: 'Not Found' }));\r\n\r\n// Chain stops when:\r\n// - res.send/json/end is called (response sent)\r\n// - next(err) is called (jumps to error handler)\r\n// - next() is never called (request hangs!)</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">33</span><span class=\"q-question\">How do you handle errors globally in an Express app?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Express has a special error-handling middleware with 4 parameters <code>(err, req, res, next)</code>. Any call to <code>next(err)</code> or an unhandled error in async code jumps straight to this handler. Place it LAST after all routes.</div>\r\n    <pre>// Async route wrapper — avoids try/catch in every route:\r\nconst asyncHandler = fn => (req, res, next) =>\r\n  Promise.resolve(fn(req, res, next)).catch(next);\r\n\r\n// Clean routes — no try/catch needed:\r\napp.get('/users/:id', asyncHandler(async (req, res) => {\r\n  const user = await User.findById(req.params.id);\r\n  if (!user) {\r\n    const err = new Error('User not found');\r\n    err.status = 404;\r\n    throw err; // asyncHandler catches and calls next(err)\r\n  }\r\n  res.json(user);\r\n}));\r\n\r\n// Custom error class for structured errors:\r\nclass AppError extends Error {\r\n  constructor(message, statusCode) {\r\n    super(message);\r\n    this.status = statusCode;\r\n    this.isOperational = true; // mark as handled error\r\n  }\r\n}\r\n\r\n// Global error handler — MUST be last:\r\napp.use((err, req, res, next) => {\r\n  const status = err.status || 500;\r\n  // Don't leak stack traces to production:\r\n  const message = process.env.NODE_ENV === 'production' && status === 500\r\n    ? 'Internal Server Error'\r\n    : err.message;\r\n  console.error(err); // log full error with stack\r\n  res.status(status).json({ error: message });\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">34</span><span class=\"q-question\">What is the difference between app.use() and app.get()/post()/put()/delete()?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>app.use()</code> matches <strong>any HTTP method</strong> and <strong>any path starting with</strong> the prefix. <code>app.get/post/put/delete()</code> match only <strong>that specific method</strong> and an <strong>exact path</strong>.</div>\r\n    <pre>app.use('/api', router);     // matches ALL methods: GET /api, POST /api/users...\r\napp.use(logger);             // matches everything (no path = all paths)\r\n\r\napp.get('/users', handler);  // only GET /users exactly\r\napp.post('/users', handler); // only POST /users exactly\r\n\r\n// Router — a mini Express app for grouping routes:\r\nconst router = express.Router();\r\nrouter.get('/', getAll);      // GET /api/users (when mounted at /api/users)\r\nrouter.post('/', create);     // POST /api/users\r\nrouter.get('/:id', getOne);   // GET /api/users/42\r\nrouter.put('/:id', update);   // PUT /api/users/42\r\nrouter.delete('/:id', remove);// DELETE /api/users/42\r\n\r\napp.use('/api/users', router);</pre>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 8: STREAMS & BUFFERS ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🗂️</span><h2 class=\"section-title\">File System, Buffers & Path</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">35</span><span class=\"q-question\">What is the Buffer class and when do you use it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A Buffer is a fixed-size chunk of raw binary data (bytes). Node.js uses it to handle binary data — like image files, network packets, or encrypted data — before converting to strings.</div>\r\n    <pre>// Create buffers:\r\nconst buf1 = Buffer.from('Hello', 'utf8');     // from string\r\nconst buf2 = Buffer.from([0x48, 0x65, 0x6c]);  // from byte array\r\nconst buf3 = Buffer.alloc(10);                 // 10 zero bytes\r\nconst buf4 = Buffer.allocUnsafe(10);           // 10 uninitialized bytes (faster)\r\n\r\n// Convert buffer to string:\r\nbuf1.toString('utf8');   // \"Hello\"\r\nbuf1.toString('hex');    // \"48656c6c6f\"\r\nbuf1.toString('base64'); // \"SGVsbG8=\"\r\n\r\n// Info:\r\nbuf1.length;  // 5 bytes\r\nbuf1[0];      // 72 (ASCII code of 'H')\r\n\r\n// Buffers are what you get from streams by default:\r\nfs.createReadStream('photo.jpg').on('data', (chunk) => {\r\n  // chunk is a Buffer of raw bytes!\r\n  console.log(chunk instanceof Buffer); // true\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">36</span><span class=\"q-question\">What is the path module and why do you always need it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The <code>path</code> module builds file paths correctly for every operating system. Windows uses backslashes (<code>\\</code>), Mac/Linux use forward slashes (<code>/</code>). <code>path</code> handles this automatically — never manually concatenate paths with string concatenation!</div>\r\n    <pre>const path = require('path');\r\n\r\n// ALWAYS use path.join — handles OS differences:\r\npath.join(__dirname, 'public', 'images', 'logo.png');\r\n// \"/home/user/app/public/images/logo.png\" (Linux)\r\n// \"C:\\Users\\user\\app\\public\\images\\logo.png\" (Windows)\r\n\r\n// ❌ Never do this — breaks on Windows!\r\n__dirname + '/public/' + 'logo.png'; // breaks!\r\n\r\n// Resolve — creates absolute path from cwd:\r\npath.resolve('uploads', 'photo.jpg');\r\n// \"/home/user/app/uploads/photo.jpg\"\r\n\r\n// Parse parts:\r\npath.basename('/home/user/photo.jpg'); // \"photo.jpg\"\r\npath.dirname('/home/user/photo.jpg');  // \"/home/user\"\r\npath.extname('/home/user/photo.jpg');  // \".jpg\"\r\npath.parse('/home/user/photo.jpg');\r\n// { root:'/', dir:'/home/user', base:'photo.jpg', ext:'.jpg', name:'photo' }</pre>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 9: PERFORMANCE & SCALING ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🚀</span><h2 class=\"section-title\">Performance, Scaling & Clustering</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">37</span><span class=\"q-question\">What is the Cluster module and how does it use multiple CPU cores?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The Cluster module lets you run <strong>multiple Node.js processes</strong> — one per CPU core. A master process manages worker processes. Each worker runs your full app and handles requests independently. This multiplies throughput linearly with core count.</div>\r\n    <pre>const cluster = require('cluster');\r\nconst os = require('os');\r\n\r\nif (cluster.isPrimary) {\r\n  const numCPUs = os.cpus().length;\r\n  console.log(`Master ${process.pid} — spawning ${numCPUs} workers`);\r\n\r\n  // One worker per CPU core:\r\n  for (let i = 0; i < numCPUs; i++) cluster.fork();\r\n\r\n  // Auto-restart crashed workers:\r\n  cluster.on('exit', (worker, code) => {\r\n    console.log(`Worker ${worker.pid} died — restarting`);\r\n    cluster.fork();\r\n  });\r\n} else {\r\n  // Each worker is a fully independent Express app:\r\n  const app = require('./app');\r\n  app.listen(3000);\r\n  console.log(`Worker ${process.pid} started`);\r\n}</pre>\r\n    <div class=\"example-box\"><span class=\"label\">In production — use PM2 instead 🏭</span><code>pm2 start app.js -i max</code> — starts one worker per CPU automatically, with auto-restart, logging, zero-downtime reload. PM2 is much easier than managing raw Cluster code. Use Cluster only if you need custom logic between master and workers.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">38</span><span class=\"q-question\">What are Worker Threads and how are they different from Cluster?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Worker Threads run JavaScript in <strong>true separate threads</strong> within the same process — perfect for CPU-intensive tasks that would block the main thread. Cluster creates separate <strong>processes</strong>.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Cluster vs Worker Threads 🆚</span>\r\n      <strong>Cluster</strong>: Multiple processes. Separate memory. For scaling HTTP servers — each process handles requests independently. No shared memory.<br><br>\r\n      <strong>Worker Threads</strong>: Multiple threads. Can share memory (SharedArrayBuffer). For CPU-heavy work within one process — image processing, crypto, data analysis.\r\n    </div>\r\n    <pre>// main.js — offload heavy work to a thread:\r\nconst { Worker } = require('worker_threads');\r\n\r\napp.get('/calculate', async (req, res) => {\r\n  const result = await new Promise((resolve, reject) => {\r\n    const worker = new Worker('./heavy.js', {\r\n      workerData: { input: req.query.n }\r\n    });\r\n    worker.on('message', resolve);\r\n    worker.on('error', reject);\r\n  });\r\n  res.json({ result }); // main thread stayed responsive!\r\n});\r\n\r\n// heavy.js — runs in a separate thread:\r\nconst { workerData, parentPort } = require('worker_threads');\r\nlet sum = 0;\r\nfor (let i = 0; i < workerData.input; i++) sum += i; // heavy CPU work!\r\nparentPort.postMessage(sum); // send back to main thread</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">39</span><span class=\"q-question\">What causes memory leaks in Node.js and how do you prevent them?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A memory leak is when objects that are no longer needed stay in memory — because something still holds a reference to them. Over time, the process uses more RAM until it slows or crashes.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Top 5 causes of memory leaks in Node.js 👇</span>\r\n      1. <strong>Event listeners not removed</strong>: Adding listeners in a loop without removing. Fix: always <code>emitter.off(event, handler)</code> when done.<br>\r\n      2. <strong>Timers not cleared</strong>: <code>setInterval</code> keeps running after its component is gone. Fix: <code>clearInterval(id)</code> in cleanup.<br>\r\n      3. <strong>Growing in-memory caches</strong>: unbounded <code>Map</code> or <code>Array</code> that grows forever. Fix: use size limits + eviction, or WeakMap.<br>\r\n      4. <strong>Closures holding large data</strong>: function captures a huge object it no longer needs. Fix: null out large references when done.<br>\r\n      5. <strong>Global variables accumulating data</strong>: accidental global variables. Fix: strict mode, proper scoping.\r\n    </div>\r\n    <pre>// Detect leaks:\r\nconsole.log(process.memoryUsage());\r\n// { heapUsed: 12MB, heapTotal: 20MB, rss: 45MB, external: 1MB }\r\n\r\n// Monitor over time — if heapUsed keeps growing → leak!\r\n\r\n// Bounded cache — prevents unbounded growth:\r\nconst cache = new Map();\r\nconst MAX = 1000;\r\nfunction addToCache(key, value) {\r\n  if (cache.size >= MAX) {\r\n    const firstKey = cache.keys().next().value;\r\n    cache.delete(firstKey); // evict oldest\r\n  }\r\n  cache.set(key, value);\r\n}</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">40</span><span class=\"q-question\">What is graceful shutdown and why is it critical?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Graceful shutdown means stopping the server cleanly — finishing all in-progress requests, closing database connections, and releasing resources — before the process exits. Without it, active requests get dropped and data can be corrupted.</div>\r\n    <pre>const server = app.listen(3000);\r\n\r\nasync function shutdown(signal) {\r\n  console.log(`${signal} received — shutting down...`);\r\n\r\n  // Step 1: Stop accepting new requests:\r\n  server.close(async () => {\r\n    console.log('HTTP server closed');\r\n    // Step 2: Close database connections:\r\n    await mongoose.connection.close();\r\n    await pgPool.end();\r\n    // Step 3: Close other connections:\r\n    await redisClient.quit();\r\n    await bullWorker.close();\r\n    console.log('All connections closed');\r\n    process.exit(0);\r\n  });\r\n\r\n  // Step 4: Force exit if shutdown takes too long:\r\n  setTimeout(() => {\r\n    console.error('Forcing exit after 30s timeout');\r\n    process.exit(1);\r\n  }, 30_000);\r\n}\r\n\r\n// Listen for shutdown signals:\r\nprocess.on('SIGTERM', () => shutdown('SIGTERM')); // Docker/k8s stop\r\nprocess.on('SIGINT', () => shutdown('SIGINT'));   // Ctrl+C</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Why this matters in production 🏭</span>Kubernetes/Docker sends SIGTERM before force-killing a process. Without graceful shutdown: ongoing database transactions are aborted, active HTTP requests get connection reset errors, file writes are incomplete. With graceful shutdown: users get their responses, data stays consistent.</div>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 10: SECURITY ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🔐</span><h2 class=\"section-title\">Security & Authentication</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">41</span><span class=\"q-question\">How does JWT authentication work in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A JWT (JSON Web Token) is a signed token containing user info. After login, the server creates a JWT and sends it to the client. The client sends it back with every request. The server verifies the signature — no database lookup needed.</div>\r\n    <pre>const jwt = require('jsonwebtoken');\r\nconst SECRET = process.env.JWT_SECRET; // keep this secret!\r\n\r\n// LOGIN — create the token:\r\napp.post('/login', async (req, res) => {\r\n  const user = await User.findOne({ email: req.body.email });\r\n  const ok = await bcrypt.compare(req.body.password, user.password);\r\n  if (!ok) return res.status(401).json({ error: 'Wrong credentials' });\r\n\r\n  const token = jwt.sign(\r\n    { userId: user._id, email: user.email, role: user.role },\r\n    SECRET,\r\n    { expiresIn: '7d' }\r\n  );\r\n  res.json({ token });\r\n});\r\n\r\n// AUTH MIDDLEWARE — verify on every protected request:\r\nfunction authenticate(req, res, next) {\r\n  const token = req.headers.authorization?.replace('Bearer ', '');\r\n  if (!token) return res.status(401).json({ error: 'No token' });\r\n  try {\r\n    req.user = jwt.verify(token, SECRET); // throws if invalid/expired!\r\n    next();\r\n  } catch (err) {\r\n    res.status(401).json({ error: 'Invalid or expired token' });\r\n  }\r\n}\r\n\r\n// PROTECTED ROUTE:\r\napp.get('/profile', authenticate, (req, res) => {\r\n  res.json({ user: req.user }); // jwt.verify decoded payload\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">42</span><span class=\"q-question\">How do you hash passwords correctly in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Use <strong>bcrypt</strong>. Never store plain text passwords. bcrypt hashes with salt (prevents rainbow tables) and is intentionally slow (prevents brute force). Always hash on register, compare on login.</div>\r\n    <pre>const bcrypt = require('bcrypt');\r\nconst SALT_ROUNDS = 12; // 10-12 is standard — higher = slower\r\n\r\n// REGISTER — hash before saving:\r\nasync function register(email, plainPassword) {\r\n  const hash = await bcrypt.hash(plainPassword, SALT_ROUNDS);\r\n  // \"$2b$12$...\" — this is what goes in the database\r\n  await User.create({ email, password: hash });\r\n}\r\n\r\n// LOGIN — compare, never dehash:\r\nasync function login(email, plainPassword) {\r\n  const user = await User.findOne({ email });\r\n  if (!user) throw new Error('User not found');\r\n  const match = await bcrypt.compare(plainPassword, user.password);\r\n  if (!match) throw new Error('Wrong password');\r\n  return user;\r\n}</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Why bcrypt and not SHA256? 🤔</span>SHA256 is designed to be fast (hashes billions per second). An attacker can try every word in a dictionary instantly. bcrypt is deliberately slow — 100ms per hash. Even with GPUs, brute force takes centuries. Cost factor 12 means: 2^12 = 4096 iterations. If hardware gets faster, increase the rounds!</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">43</span><span class=\"q-question\">What are the most important security practices for Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Security in Node.js is about layering defenses — input validation, proper headers, rate limiting, secrets management, and keeping dependencies updated.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Top 8 security practices 👇</span>\r\n      1. <strong>Validate all input</strong>: Use Joi or Zod. Never trust client data.<br>\r\n      2. <strong>Helmet.js</strong>: Sets 15+ security HTTP headers in one line.<br>\r\n      3. <strong>Rate limiting</strong>: 5 login attempts per 15 min. Use express-rate-limit.<br>\r\n      4. <strong>HTTPS only</strong>: Never serve over plain HTTP in production.<br>\r\n      5. <strong>Parameterized queries</strong>: Never build SQL/queries with string concatenation — use ORM or placeholders.<br>\r\n      6. <strong>Secrets in environment variables</strong>: Never commit .env or API keys. Use secret managers in production.<br>\r\n      7. <strong>npm audit</strong>: Run weekly. Fix vulnerabilities in dependencies.<br>\r\n      8. <strong>Don't run as root</strong>: Create a non-root user for your Node.js process in production.\r\n    </div>\r\n    <pre>const helmet = require('helmet');\r\nconst rateLimit = require('express-rate-limit');\r\n\r\napp.use(helmet()); // sets 15 security headers instantly!\r\n\r\napp.use('/api/auth', rateLimit({\r\n  windowMs: 15 * 60 * 1000, // 15 minutes\r\n  max: 5,                    // 5 attempts max\r\n  message: { error: 'Too many login attempts' }\r\n}));</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">44</span><span class=\"q-question\">What is CORS and how do you configure it in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>CORS (Cross-Origin Resource Sharing) is a browser security rule that blocks your frontend from calling an API on a different domain — unless the API explicitly says it's allowed. You configure this on the Node.js server.</div>\r\n    <pre>const cors = require('cors');\r\n\r\n// Allow specific origins only (production):\r\napp.use(cors({\r\n  origin: ['https://myapp.com', 'https://admin.myapp.com'],\r\n  methods: ['GET', 'POST', 'PUT', 'DELETE', 'PATCH'],\r\n  allowedHeaders: ['Content-Type', 'Authorization'],\r\n  credentials: true, // allow cookies / Authorization header\r\n}));\r\n\r\n// Dynamic origin check:\r\napp.use(cors({\r\n  origin: (origin, callback) => {\r\n    const allowed = ['https://myapp.com', 'https://app2.com'];\r\n    if (!origin || allowed.includes(origin)) {\r\n      callback(null, true); // allow\r\n    } else {\r\n      callback(new Error('Not allowed by CORS'));\r\n    }\r\n  }\r\n}));</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Never do this in production ⚠️</span><code>app.use(cors())</code> with no options allows ALL origins — any website on the internet can call your API! Always specify exactly which origins you trust.</div>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 11: DATABASES ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🗄️</span><h2 class=\"section-title\">Databases & Caching</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">45</span><span class=\"q-question\">What is connection pooling and why is it critical?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Opening a fresh database connection for every request is slow (50-200ms) and resource-intensive. <strong>Connection pooling</strong> keeps a set of open connections ready to reuse — no wait time, much faster.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Analogy 🚕</span>Summoning an Uber for every trip vs having 10 taxis sitting outside your building. The taxis (pool) are always ready — no 5-minute wait for a new car. Connection pooling is the fleet of pre-warmed database connections.</div>\r\n    <pre>const { Pool } = require('pg');\r\n\r\nconst pool = new Pool({\r\n  connectionString: process.env.DATABASE_URL,\r\n  max: 10,          // max 10 connections in the pool\r\n  min: 2,           // keep at least 2 warm at all times\r\n  idleTimeoutMillis: 30000, // close idle connections after 30s\r\n  connectionTimeoutMillis: 2000, // error if can't get connection in 2s\r\n});\r\n\r\n// Uses a connection from pool, returns it when done:\r\nconst result = await pool.query('SELECT * FROM users WHERE id = $1', [1]);\r\n\r\n// Mongoose also pools connections — the default is 5:\r\nawait mongoose.connect(uri, { maxPoolSize: 10 });\r\n\r\n// ALWAYS close pool on shutdown:\r\nprocess.on('SIGTERM', async () => {\r\n  await pool.end();\r\n  process.exit(0);\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">46</span><span class=\"q-question\">What is the N+1 query problem and how do you fix it?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The N+1 problem: you fetch N records, then make 1 extra database query for EACH record to get related data. 100 users = 101 queries instead of 2. This is a silent performance killer in Node.js apps.</div>\r\n    <pre>// N+1 PROBLEM — 1 + N queries:\r\nconst users = await User.find();   // 1 query\r\nfor (const user of users) {\r\n  user.posts = await Post.find({ userId: user._id }); // N queries!\r\n}\r\n// 100 users = 101 queries! Slow!\r\n\r\n// FIX 1 — Mongoose populate (2 queries total):\r\nconst users = await User.find().populate('posts');\r\n\r\n// FIX 2 — batch load manually (2 queries total):\r\nconst users = await User.find();\r\nconst ids = users.map(u => u._id);\r\nconst posts = await Post.find({ userId: { $in: ids } }); // 1 query for all!\r\n\r\n// Group posts by userId:\r\nconst postMap = posts.reduce((map, post) => {\r\n  (map[post.userId] ??= []).push(post);\r\n  return map;\r\n}, {});\r\n\r\nusers.forEach(user => { user.posts = postMap[user._id] || []; });\r\n// 2 queries total ✅</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">47</span><span class=\"q-question\">What is caching and how do you implement it with Redis?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Caching stores frequently accessed data in fast memory so you don't hit the database every time. Redis is the standard caching layer in Node.js — it's in-memory (fast!) and supports TTL (automatic expiry).</div>\r\n    <pre>const Redis = require('ioredis');\r\nconst redis = new Redis(process.env.REDIS_URL);\r\n\r\n// Cache-aside pattern (most common):\r\nasync function getUser(id) {\r\n  // 1. Check cache first:\r\n  const cached = await redis.get(`user:${id}`);\r\n  if (cached) return JSON.parse(cached); // cache hit — fast!\r\n\r\n  // 2. Cache miss — fetch from database:\r\n  const user = await User.findById(id);\r\n  if (!user) return null;\r\n\r\n  // 3. Store in cache with 5-minute expiry:\r\n  await redis.setex(`user:${id}`, 300, JSON.stringify(user));\r\n  return user;\r\n}\r\n\r\n// When user is updated — invalidate the cache!\r\nasync function updateUser(id, data) {\r\n  const user = await User.findByIdAndUpdate(id, data, { new: true });\r\n  await redis.del(`user:${id}`); // ❌ stale cache removed!\r\n  return user;\r\n}\r\n\r\n// Useful Redis commands:\r\nawait redis.set('key', 'value');          // set\r\nawait redis.get('key');                   // get\r\nawait redis.setex('key', 300, 'value');   // set with 5-min TTL\r\nawait redis.del('key');                   // delete\r\nawait redis.ttl('key');                   // remaining lifetime in seconds</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">48</span><span class=\"q-question\">What are database transactions and when do you need them?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A transaction groups multiple database operations so they either ALL succeed or ALL fail (rollback). Critical for operations that must be atomic — like transferring money, where deducting and crediting must both succeed or neither should.</div>\r\n    <pre>// MongoDB transaction with Mongoose:\r\nconst session = await mongoose.startSession();\r\nsession.startTransaction();\r\n\r\ntry {\r\n  // Both operations are part of the SAME transaction:\r\n  await Account.updateOne(\r\n    { userId: senderId },\r\n    { $inc: { balance: -amount } },\r\n    { session }\r\n  );\r\n  await Account.updateOne(\r\n    { userId: receiverId },\r\n    { $inc: { balance: +amount } },\r\n    { session }\r\n  );\r\n  await session.commitTransaction(); // both succeed — commit!\r\n  console.log('Transfer complete');\r\n} catch (error) {\r\n  await session.abortTransaction(); // any failure — rollback!\r\n  console.log('Transfer failed — rolled back');\r\n  throw error;\r\n} finally {\r\n  session.endSession();\r\n}\r\n// Without transaction: if server crashes after deducting but before\r\n// crediting → money disappears! Transaction prevents this.</pre>\r\n  </div>\r\n</div>\r\n\r\n<!-- ========== SECTION 12: ADVANCED TOPICS ========== -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🔬</span><h2 class=\"section-title\">Advanced Node.js Topics</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">49</span><span class=\"q-question\">What is the child_process module and when do you use it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The <code>child_process</code> module lets you spawn other programs from Node.js — run shell commands, execute Python scripts, or start other Node processes. It's how Node.js reaches outside its own environment.</div>\r\n    <pre>const { exec, spawn, fork } = require('child_process');\r\n\r\n// exec — run command, buffer all output (for small output):\r\nconst { promisify } = require('util');\r\nconst execAsync = promisify(exec);\r\nconst { stdout } = await execAsync('git log --oneline -5');\r\nconsole.log(stdout);\r\n\r\n// spawn — stream output (for large output or long-running):\r\nconst ls = spawn('ls', ['-la', '/home']);\r\nls.stdout.on('data', d => process.stdout.write(d));\r\nls.on('close', code => console.log(`Exited with code ${code}`));\r\n\r\n// fork — specifically for Node.js child processes, has IPC:\r\nconst child = fork('./worker.js');\r\nchild.send({ task: 'heavy-compute', data: bigArray });\r\nchild.on('message', (result) => console.log('Done:', result));</pre>\r\n    <div class=\"example-box\"><span class=\"label\">exec vs spawn vs fork 🆚</span>\r\n      <code>exec</code>: runs shell command, buffers output (good for small commands, vulnerable to shell injection with user input!).<br>\r\n      <code>spawn</code>: runs command directly, streams output (good for long output, no shell injection risk).<br>\r\n      <code>fork</code>: only for Node.js files, has built-in 2-way messaging via <code>process.send()</code>.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">50</span><span class=\"q-question\">What is AsyncLocalStorage and why is it useful?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>AsyncLocalStorage</code> stores data that follows an async chain automatically — like a request ID or user session — without passing it through every function call. It's how you associate data with a specific request across the entire async chain.</div>\r\n    <pre>const { AsyncLocalStorage } = require('async_hooks');\r\nconst requestStorage = new AsyncLocalStorage();\r\n\r\n// Middleware — attach a request ID to the context:\r\napp.use((req, res, next) => {\r\n  const requestId = crypto.randomUUID();\r\n  requestStorage.run({ requestId, userId: req.user?.id }, () => {\r\n    next(); // everything async inside this runs with this context!\r\n  });\r\n});\r\n\r\n// Deep inside a service or utility — no need to pass requestId!\r\nfunction logMessage(message) {\r\n  const ctx = requestStorage.getStore(); // get the current context\r\n  console.log(`[${ctx?.requestId}] [user:${ctx?.userId}] ${message}`);\r\n}\r\n\r\n// Without AsyncLocalStorage, you'd pass requestId to every function:\r\n// fetchUser(id, requestId) → getProfile(userId, requestId) → log(requestId)\r\n// Messy! AsyncLocalStorage eliminates this prop-drilling for async context.</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">51</span><span class=\"q-question\">What are job queues and why are they essential?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A job queue offloads slow or heavy tasks (emails, image resizing, PDF generation) to background workers — so your API responds instantly without waiting. The API adds a job to the queue and returns immediately. Workers process jobs in the background.</div>\r\n    <pre>// Using BullMQ (backed by Redis):\r\nconst { Queue, Worker } = require('bullmq');\r\nconst connection = { host: 'localhost', port: 6379 };\r\n\r\n// In your API route — add job and respond immediately:\r\nconst emailQueue = new Queue('emails', { connection });\r\n\r\napp.post('/register', async (req, res) => {\r\n  const user = await User.create(req.body);\r\n\r\n  // Don't wait for email — just queue it!\r\n  await emailQueue.add('welcome', {\r\n    to: user.email,\r\n    name: user.name\r\n  });\r\n\r\n  res.status(201).json({ user }); // responds in <10ms!\r\n});\r\n\r\n// Separate worker process — runs in background:\r\nconst worker = new Worker('emails', async (job) => {\r\n  if (job.name === 'welcome') {\r\n    await sendWelcomeEmail(job.data.to, job.data.name);\r\n  }\r\n}, { connection });\r\n\r\nworker.on('completed', job => console.log(`Email sent: ${job.id}`));\r\nworker.on('failed', (job, err) => console.error(`Failed: ${err.message}`));</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Common use cases for queues 👇</span>Welcome/notification emails, password reset emails, image/video processing, PDF generation, data imports, payment webhooks, sending bulk notifications.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">52</span><span class=\"q-question\">What is WebSocket and how do you implement real-time features?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>WebSocket is a protocol for two-way real-time communication — both client and server can send messages anytime without re-requesting. Unlike HTTP (client asks, server answers), WebSocket keeps the connection permanently open.</div>\r\n    <pre>// Using Socket.IO (easier, handles fallbacks):\r\nconst { Server } = require('socket.io');\r\nconst httpServer = require('http').createServer(app);\r\nconst io = new Server(httpServer, { cors: { origin: '*' } });\r\n\r\nio.on('connection', (socket) => {\r\n  console.log(`Connected: ${socket.id}`);\r\n\r\n  // Receive messages from THIS client:\r\n  socket.on('chat:send', ({ room, message }) => {\r\n    // Send to everyone in the room:\r\n    io.to(room).emit('chat:message', {\r\n      from: socket.id, message, time: Date.now()\r\n    });\r\n  });\r\n\r\n  // Joining rooms:\r\n  socket.on('room:join', (room) => {\r\n    socket.join(room);\r\n    socket.to(room).emit('user:joined', socket.id);\r\n  });\r\n\r\n  socket.on('disconnect', () => {\r\n    console.log(`Disconnected: ${socket.id}`);\r\n  });\r\n});</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Use cases for WebSocket 👇</span>Live chat apps, collaborative document editing (Google Docs style), live dashboards (stock prices, sports scores), multiplayer games, notifications, live code collaboration.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">53</span><span class=\"q-question\">What is rate limiting and how do you implement it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Rate limiting restricts how many requests a client can make in a time window. It protects against DDoS attacks, API abuse, and brute-force login attempts.</div>\r\n    <pre>const rateLimit = require('express-rate-limit');\r\n\r\n// General API rate limit:\r\napp.use('/api/', rateLimit({\r\n  windowMs: 15 * 60 * 1000, // 15 minutes\r\n  max: 100,\r\n  standardHeaders: true, // send RateLimit-* headers\r\n  message: { error: 'Too many requests, try again in 15 minutes' }\r\n}));\r\n\r\n// Strict limit for auth endpoints:\r\napp.use('/api/auth', rateLimit({\r\n  windowMs: 15 * 60 * 1000,\r\n  max: 5, // only 5 login attempts per 15 min!\r\n  skipSuccessfulRequests: true, // don't count successful logins\r\n}));\r\n\r\n// For multiple server instances — use Redis store:\r\nconst RedisStore = require('rate-limit-redis');\r\napp.use(rateLimit({\r\n  store: new RedisStore({ sendCommand: (...args) => redis.call(...args) }),\r\n  windowMs: 15 * 60 * 1000,\r\n  max: 100\r\n  // This counts requests across ALL server instances!\r\n}));</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">54</span><span class=\"q-question\">How do you schedule recurring tasks (cron jobs) in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Use <strong>node-cron</strong> to schedule tasks — clean up expired sessions, send daily reports, sync data every hour. The cron syntax defines when the task runs.</div>\r\n    <pre>const cron = require('node-cron');\r\n\r\n// Cron syntax: second  minute  hour  day  month  weekday\r\n//                 *      *      *     *     *       *\r\n\r\n// Every day at midnight:\r\ncron.schedule('0 0 * * *', async () => {\r\n  await deleteExpiredSessions();\r\n  await cleanupOldLogs();\r\n});\r\n\r\n// Every Monday at 9am:\r\ncron.schedule('0 9 * * 1', async () => {\r\n  await generateWeeklyReport();\r\n  await sendReportEmails();\r\n});\r\n\r\n// Every 30 seconds:\r\ncron.schedule('*/30 * * * * *', () => {\r\n  checkSystemHealth();\r\n});</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Multi-instance problem ⚠️</span>If you have 4 server instances (Cluster), your cron job runs on ALL 4 — sending the email 4 times! Fix: use BullMQ with <code>repeat</code> option (Redis ensures only one instance runs it), or use a distributed lock with Redis.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">55</span><span class=\"q-question\">What is input validation and how do you do it properly?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Always validate user input before using it — never trust what comes from the client. Use <strong>Joi</strong> or <strong>Zod</strong> to define a schema and validate against it. Invalid data returns a 400 error before it touches your business logic or database.</div>\r\n    <pre>const Joi = require('joi');\r\n\r\nconst createUserSchema = Joi.object({\r\n  name: Joi.string().min(2).max(50).required(),\r\n  email: Joi.string().email().required(),\r\n  age: Joi.number().integer().min(0).max(120),\r\n  role: Joi.string().valid('user', 'admin').default('user')\r\n});\r\n\r\n// Validation middleware factory:\r\nfunction validate(schema) {\r\n  return (req, res, next) => {\r\n    const { error, value } = schema.validate(req.body, {\r\n      abortEarly: false,  // return ALL errors, not just the first\r\n      stripUnknown: true  // remove any fields not in schema!\r\n    });\r\n    if (error) {\r\n      return res.status(400).json({\r\n        error: 'Validation failed',\r\n        details: error.details.map(d => d.message)\r\n      });\r\n    }\r\n    req.body = value; // use the cleaned, validated data\r\n    next();\r\n  };\r\n}\r\n\r\napp.post('/users', validate(createUserSchema), createUser);</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">56</span><span class=\"q-question\">What is the Repository Pattern and why use it?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The Repository Pattern separates your business logic from data access code. Instead of calling the ORM/database directly in your service, you go through a repository class. This makes testing easy (inject a fake repository) and swapping databases painless.</div>\r\n    <pre>// Repository — all database operations for User:\r\nclass UserRepository {\r\n  async findById(id)          { return User.findById(id).lean(); }\r\n  async findByEmail(email)    { return User.findOne({ email }).lean(); }\r\n  async create(data)          { return User.create(data); }\r\n  async update(id, data)      { return User.findByIdAndUpdate(id, data, { new: true }); }\r\n  async delete(id)            { return User.findByIdAndDelete(id); }\r\n}\r\n\r\n// Service — business logic, uses repository:\r\nclass UserService {\r\n  constructor(userRepo) {\r\n    this.userRepo = userRepo; // injected — easy to mock in tests!\r\n  }\r\n  async register(email, password) {\r\n    const exists = await this.userRepo.findByEmail(email);\r\n    if (exists) throw new AppError('Email already taken', 409);\r\n    const hashed = await bcrypt.hash(password, 12);\r\n    return this.userRepo.create({ email, password: hashed });\r\n  }\r\n}\r\n\r\n// Production:\r\nconst service = new UserService(new UserRepository());\r\n\r\n// Testing — inject a fake:\r\nconst fakeRepo = { create: async (d) => d, findByEmail: async () => null };\r\nconst service = new UserService(fakeRepo); // no real database!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">57</span><span class=\"q-question\">What is dependency injection and how does it help in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Dependency injection means passing dependencies (database, email service, logger) into a class or function instead of creating them inside. This makes code testable — you can inject mocks instead of real services during tests.</div>\r\n    <pre>// WITHOUT DI — hard to test (can't swap DB):\r\nclass UserService {\r\n  async getUser(id) {\r\n    return await User.findById(id); // directly uses Mongoose!\r\n  }\r\n}\r\n\r\n// WITH DI — easy to test (inject anything):\r\nclass UserService {\r\n  constructor(db, emailService, logger) {\r\n    this.db = db;\r\n    this.email = emailService;\r\n    this.logger = logger;\r\n  }\r\n  async registerUser(data) {\r\n    const user = await this.db.users.create(data);\r\n    await this.email.sendWelcome(user.email);\r\n    this.logger.info(`User registered: ${user.id}`);\r\n    return user;\r\n  }\r\n}\r\n\r\n// Production:\r\nconst service = new UserService(mongoDb, sendGridEmail, winstonLogger);\r\n\r\n// Tests — inject fakes:\r\nconst fakeDb = { users: { create: async d => ({ ...d, id: '1' }) } };\r\nconst fakeEmail = { sendWelcome: async () => {} };\r\nconst fakeLogger = { info: () => {} };\r\nconst service = new UserService(fakeDb, fakeEmail, fakeLogger);\r\n// Fast tests, no network calls, no database needed!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">58</span><span class=\"q-question\">What is the Circuit Breaker pattern in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A circuit breaker stops calling a failing external service after too many errors — instead of every request failing and waiting for timeout, it \"opens the circuit\" and returns a fast error. After a timeout, it carefully tests if the service recovered.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Three states 🔌</span>\r\n      <strong>Closed</strong> (normal): All requests go through. Failures are tracked.<br>\r\n      <strong>Open</strong> (failing): After too many failures, reject requests immediately. Don't even try.<br>\r\n      <strong>Half-Open</strong> (testing): After timeout, let 1 request through. If it succeeds → Closed. If it fails → back to Open.\r\n    </div>\r\n    <pre>const Opossum = require('opossum');\r\n\r\nconst options = {\r\n  timeout: 3000,                 // 3s timeout = failure\r\n  errorThresholdPercentage: 50,  // 50% failures = open circuit\r\n  resetTimeout: 30000            // try again after 30s\r\n};\r\nconst breaker = new Opossum(callPaymentService, options);\r\n\r\nbreaker.on('open',     () => console.warn('Circuit OPEN!'));\r\nbreaker.on('halfOpen', () => console.log('Testing recovery...'));\r\nbreaker.on('close',    () => console.log('Circuit CLOSED — normal!'));\r\nbreaker.fallback(() => ({ status: 'payment queued for retry' }));\r\n\r\n// Use instead of calling directly:\r\nconst result = await breaker.fire({ amount: 99.99, card: '****1234' });\r\n// If service is down — returns fallback instantly!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">59</span><span class=\"q-question\">What is PM2 and why use it in production?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>PM2 is a process manager for Node.js. It keeps your app alive (auto-restarts on crash), runs multiple instances (cluster mode), handles logs, and lets you deploy with zero downtime. The standard tool for production Node.js deployments.</div>\r\n    <pre># Install globally:\r\nnpm install pm2 -g\r\n\r\n# Start in cluster mode — one worker per CPU core:\r\npm2 start app.js -i max --name \"my-api\"\r\n\r\n# Daily restart (prevents memory leaks accumulating):\r\npm2 start app.js --cron-restart=\"0 3 * * *\"\r\n\r\n# Useful PM2 commands:\r\npm2 list                # see all processes and status\r\npm2 logs my-api         # view live logs\r\npm2 monit               # live monitoring dashboard\r\npm2 restart my-api      # restart gracefully\r\npm2 reload my-api       # zero-downtime reload!\r\npm2 stop my-api         # stop\r\npm2 delete my-api       # remove from PM2\r\n\r\n# Auto-start on server reboot:\r\npm2 save               # save current process list\r\npm2 startup            # generate startup script\r\n# Follow the output instructions!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">60</span><span class=\"q-question\">What is the Observer pattern with EventEmitter — practical patterns?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The Observer pattern with EventEmitter lets you <strong>decouple services</strong>. When something happens (order placed, user registered), emit an event. Other services react without the emitter knowing about them. New behavior = just add a new listener — don't touch the emitter.</div>\r\n    <pre>// This is the Open/Closed Principle in action:\r\nclass OrderService extends EventEmitter {\r\n  async placeOrder(orderData) {\r\n    const order = await Order.create(orderData);\r\n    this.emit('order:placed', order); // fire and forget!\r\n    return order;\r\n    // OrderService has NO idea who's listening or what they do!\r\n  }\r\n}\r\n\r\nconst orders = new OrderService();\r\n\r\n// Add behaviors independently — no changes to OrderService:\r\norders.on('order:placed', async o => await emailService.sendConfirmation(o));\r\norders.on('order:placed', async o => await inventory.reserve(o.items));\r\norders.on('order:placed', async o => await analytics.track('purchase', o));\r\n\r\n// 6 months later — add a new behavior:\r\norders.on('order:placed', async o => await loyaltyService.addPoints(o));\r\n// Zero changes to OrderService! Completely decoupled.</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">61</span><span class=\"q-question\">What is logging in production Node.js and what should you use?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Use a proper logging library like <strong>Winston</strong> or <strong>Pino</strong> instead of <code>console.log</code>. They support log levels, structured JSON output (great for log aggregators), and multiple destinations.</div>\r\n    <pre>const winston = require('winston');\r\n\r\nconst logger = winston.createLogger({\r\n  level: process.env.LOG_LEVEL || 'info',\r\n  format: winston.format.combine(\r\n    winston.format.timestamp(),\r\n    winston.format.errors({ stack: true }),\r\n    winston.format.json() // structured JSON — searchable in Datadog/Splunk!\r\n  ),\r\n  transports: [\r\n    new winston.transports.Console({\r\n      format: process.env.NODE_ENV === 'development'\r\n        ? winston.format.simple() // human-readable in dev\r\n        : winston.format.json()   // JSON in production\r\n    }),\r\n    new winston.transports.File({ filename: 'error.log', level: 'error' })\r\n  ]\r\n});\r\n\r\n// Use log levels appropriately:\r\nlogger.error('DB connection failed', { error: err.message, stack: err.stack });\r\nlogger.warn('Rate limit almost reached', { ip: req.ip, count: 95 });\r\nlogger.info('User registered', { userId: user.id, email: user.email });\r\nlogger.debug('Cache hit', { key, ttl }); // only shows in dev</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">62</span><span class=\"q-question\">What is the difference between synchronous and asynchronous error handling patterns?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Synchronous errors are caught with try/catch. Async errors (rejected Promises) need try/catch inside async functions or .catch() on Promises. The most common Node.js bug is mixing them up and letting async errors go unhandled.</div>\r\n    <pre>// Sync error — caught by try/catch:\r\ntry { JSON.parse(\"{invalid}\"); }\r\ncatch (err) { console.log(\"caught:\", err.message); }\r\n\r\n// Async error — try/catch with await:\r\ntry { await failingOperation(); }\r\ncatch (err) { console.log(\"caught:\", err.message); }\r\n\r\n// CLASSIC MISTAKE — try/catch can't catch callback errors:\r\ntry {\r\n  someCallbackAPI('data', function(err, result) {\r\n    if (err) throw err; // ❌ This throw is NOT caught by outer try/catch!\r\n    // It becomes an uncaughtException!\r\n  });\r\n} catch (err) {} // never runs!\r\n\r\n// FIX:\r\nsomeCallbackAPI('data', function(err, result) {\r\n  if (err) return handleError(err); // handle inside the callback!\r\n});\r\n\r\n// Properly catching Promise errors — ALWAYS do one of these:\r\nmyPromise.catch(err => handleError(err));          // .catch()\r\nconst result = await myPromise.catch(handleError);  // inline catch\r\ntry { await myPromise; } catch (err) { ... }        // try/catch</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">63</span><span class=\"q-question\">What is pagination and how do you implement it properly?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Never return all records at once — always paginate. Two approaches: <strong>offset-based</strong> (simple, but slow for large datasets) and <strong>cursor-based</strong> (fast, scales to billions of records).</div>\r\n    <pre>// Offset-based pagination:\r\napp.get('/api/users', async (req, res) => {\r\n  const page  = parseInt(req.query.page)  || 1;\r\n  const limit = parseInt(req.query.limit) || 20;\r\n  const skip  = (page - 1) * limit;\r\n\r\n  const [users, total] = await Promise.all([\r\n    User.find().skip(skip).limit(limit).sort({ createdAt: -1 }),\r\n    User.countDocuments()\r\n  ]);\r\n\r\n  res.json({\r\n    data: users,\r\n    pagination: {\r\n      page, limit, total,\r\n      totalPages: Math.ceil(total / limit),\r\n      hasNext: page * limit < total,\r\n      hasPrev: page > 1\r\n    }\r\n  });\r\n});\r\n\r\n// Cursor-based (better for large/real-time data):\r\napp.get('/api/feed', async (req, res) => {\r\n  const cursor = req.query.cursor; // last seen item's ID\r\n  const limit  = 20;\r\n  const query  = cursor ? { _id: { $lt: cursor } } : {};\r\n\r\n  const items = await Post.find(query).limit(limit).sort({ _id: -1 });\r\n  res.json({\r\n    data: items,\r\n    nextCursor: items.length === limit ? items.at(-1)._id : null\r\n  });\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">64</span><span class=\"q-question\">How do you test Node.js/Express APIs?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Use <strong>Jest</strong> as the test framework and <strong>Supertest</strong> to make real HTTP requests to your Express app in tests — without running an actual server on a port.</div>\r\n    <pre>// app.js — export app WITHOUT calling listen():\r\nconst express = require('express');\r\nconst app = express();\r\n// ... routes ...\r\nmodule.exports = app;\r\n\r\n// server.js — entry point:\r\nconst app = require('./app');\r\napp.listen(3000);\r\n\r\n// users.test.js — test the app:\r\nconst request = require('supertest');\r\nconst app = require('./app');\r\n\r\ndescribe('Users API', () => {\r\n  it('GET /api/users returns 200 with array', async () => {\r\n    const res = await request(app).get('/api/users');\r\n    expect(res.status).toBe(200);\r\n    expect(Array.isArray(res.body.data)).toBe(true);\r\n  });\r\n\r\n  it('POST /api/users creates user', async () => {\r\n    const res = await request(app)\r\n      .post('/api/users')\r\n      .set('Authorization', 'Bearer valid-token')\r\n      .send({ name: 'Alice', email: 'alice@test.com' });\r\n    expect(res.status).toBe(201);\r\n    expect(res.body.name).toBe('Alice');\r\n  });\r\n\r\n  it('POST /api/users returns 400 for missing email', async () => {\r\n    const res = await request(app).post('/api/users').send({ name: 'Bob' });\r\n    expect(res.status).toBe(400);\r\n  });\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">65</span><span class=\"q-question\">What is mocking in tests and how do you mock database calls?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Mocking replaces real dependencies (database, external APIs) with fake ones during tests. This makes tests fast (no real network/DB), reliable (no flakiness), and isolated (one thing fails = one test fails).</div>\r\n    <pre>// users.service.js:\r\nconst User = require('./models/User');\r\nexports.getUserById = async (id) => {\r\n  return User.findById(id);\r\n};\r\n\r\n// users.service.test.js — mock the User model:\r\njest.mock('./models/User');\r\n\r\nconst userService = require('./users.service');\r\n\r\ndescribe('getUserById', () => {\r\n  it('returns user when found', async () => {\r\n    const fakeUser = { id: '123', name: 'Alice' };\r\n    User.findById.mockResolvedValue(fakeUser); // fake DB response!\r\n\r\n    const result = await userService.getUserById('123');\r\n    expect(result).toEqual(fakeUser);\r\n    expect(User.findById).toHaveBeenCalledWith('123');\r\n  });\r\n\r\n  it('returns null when not found', async () => {\r\n    User.findById.mockResolvedValue(null);\r\n    const result = await userService.getUserById('999');\r\n    expect(result).toBeNull();\r\n  });\r\n\r\n  it('propagates DB errors', async () => {\r\n    User.findById.mockRejectedValue(new Error('DB Error'));\r\n    await expect(userService.getUserById('1')).rejects.toThrow('DB Error');\r\n  });\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">66</span><span class=\"q-question\">What are environment variables and how do you manage them?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Environment variables store configuration and secrets outside your code. Database URLs, API keys, JWT secrets — these change between environments (dev/staging/production) and should NEVER be committed to git.</div>\r\n    <pre># .env file (local dev only — in .gitignore!):\r\nPORT=3000\r\nNODE_ENV=development\r\nDATABASE_URL=mongodb://localhost:27017/myapp\r\nJWT_SECRET=dev_secret_change_in_production\r\nSENDGRID_API_KEY=SG.xxxxx\r\n\r\n# Load in your app:\r\nrequire('dotenv').config();\r\n\r\nconst port = process.env.PORT || 3000;\r\nconst db   = process.env.DATABASE_URL;\r\n\r\n// Validate required vars on startup — fail fast!\r\nconst required = ['DATABASE_URL', 'JWT_SECRET', 'SENDGRID_API_KEY'];\r\nrequired.forEach(key => {\r\n  if (!process.env[key]) {\r\n    console.error(`❌ Missing required env var: ${key}`);\r\n    process.exit(1); // fail loudly before serving any traffic!\r\n  }\r\n});</pre>\r\n    <div class=\"example-box\"><span class=\"label\">In production — don't use .env files 🔐</span>Use: AWS Secrets Manager, GCP Secret Manager, Azure Key Vault, Vercel/Railway environment UI, Kubernetes Secrets, or Docker secrets. These are encrypted, audited, and rotatable. A .env file on disk is a security risk.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">67</span><span class=\"q-question\">What is the proper folder structure for a Node.js project?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A clean structure separates concerns — routes define endpoints, controllers handle requests, services contain business logic, and repositories manage database access. This makes testing, debugging, and scaling much easier.</div>\r\n    <pre>project/\r\n├── src/\r\n│   ├── config/          # DB setup, env validation\r\n│   │   ├── database.js  # mongoose.connect or pg Pool\r\n│   │   └── env.js       # validate required env vars\r\n│   ├── controllers/     # thin — receive req, call service, send res\r\n│   │   └── users.controller.js\r\n│   ├── middleware/      # auth, validation, rate limit, logger\r\n│   │   ├── auth.middleware.js\r\n│   │   └── validate.middleware.js\r\n│   ├── models/          # Mongoose/Prisma schemas\r\n│   │   └── User.js\r\n│   ├── repositories/    # all database operations\r\n│   │   └── users.repository.js\r\n│   ├── routes/          # route definitions — map URL to controller\r\n│   │   └── users.routes.js\r\n│   ├── services/        # business logic — fat, calls repositories\r\n│   │   └── users.service.js\r\n│   ├── utils/           # helpers: email, crypto, formatters\r\n│   └── app.js           # Express setup — NO app.listen() here!\r\n├── tests/               # mirrors src/ structure\r\n├── .env.example         # template (no real values)\r\n├── .gitignore           # includes node_modules, .env\r\n├── package.json\r\n└── server.js            # entry point: require('./src/app').listen(PORT)</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Why separate app.js from server.js? 🎯</span>Your tests import <code>app.js</code> directly and Supertest binds it to a random port. If you call <code>listen()</code> inside <code>app.js</code>, every test starts the server — port conflicts, slow tests. Separating them keeps tests clean and fast.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">68</span><span class=\"q-question\">What is the difference between horizontal and vertical scaling in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><strong>Vertical scaling</strong>: make the server bigger (more RAM, faster CPU). Hit a ceiling. <strong>Horizontal scaling</strong>: add more servers running the same app behind a load balancer. Node.js is excellent for horizontal scaling — it's lightweight (30-100MB RAM) and starts in seconds.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Scaling ladder for Node.js 🪜</span>\r\n      <strong>Level 1 — PM2 Cluster</strong>: Use all CPU cores on one server. Free. Works for moderate traffic.<br>\r\n      <strong>Level 2 — Multiple servers + Nginx</strong>: Nginx load-balances traffic across 3 Node.js servers. Need shared state (Redis for sessions, DB for data).<br>\r\n      <strong>Level 3 — Container orchestration (Kubernetes)</strong>: Auto-scale based on CPU/memory metrics. Add pods automatically under load. Remove when quiet. Self-healing (restarts crashed pods).<br>\r\n      <strong>Level 4 — Microservices</strong>: Scale each service independently. User service getting hammered? Scale it from 3 to 10 instances — without touching the payment service.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">69</span><span class=\"q-question\">What is Server-Sent Events (SSE) and how is it different from WebSockets?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>SSE is a one-way real-time channel — server pushes updates to the client over a regular HTTP connection. WebSockets are two-way. SSE is simpler to implement, auto-reconnects, and works over HTTP/2 natively.</div>\r\n    <div class=\"example-box\"><span class=\"label\">SSE vs WebSocket 🆚</span>\r\n      <strong>SSE</strong>: server → client only. HTTP-based. Auto-reconnect. Works through proxies. Great for: live feeds, notifications, progress updates.<br>\r\n      <strong>WebSocket</strong>: two-way. Separate protocol (ws://). Must handle reconnects. Great for: chat, gaming, collaborative editing.\r\n    </div>\r\n    <pre>// SSE server — stream events to client:\r\napp.get('/events', (req, res) => {\r\n  res.setHeader('Content-Type', 'text/event-stream');\r\n  res.setHeader('Cache-Control', 'no-cache');\r\n  res.setHeader('Connection', 'keep-alive');\r\n  res.flushHeaders(); // send headers immediately\r\n\r\n  // Push an event every second:\r\n  const interval = setInterval(() => {\r\n    const data = JSON.stringify({ time: new Date(), count: ++counter });\r\n    res.write(`event: update\\n`);    // event name\r\n    res.write(`data: ${data}\\n\\n`); // data (double newline = end of event!)\r\n  }, 1000);\r\n\r\n  // Clean up when client disconnects:\r\n  req.on('close', () => {\r\n    clearInterval(interval);\r\n    res.end();\r\n  });\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">70</span><span class=\"q-question\">What are common Node.js performance anti-patterns to avoid?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Things that kill Node.js performance 💡</span></div>\r\n    <div class=\"example-box\"><span class=\"label\">Top anti-patterns & fixes 👇</span>\r\n      ❌ <strong>Blocking the event loop</strong>: CPU-heavy sync code in route handlers. Fix: Worker Threads, offload to queue.<br>\r\n      ❌ <strong>readFileSync/writeFileSync in routes</strong>: Blocks ALL requests while reading. Fix: always use async fs.promises.<br>\r\n      ❌ <strong>No database indexes</strong>: Full table scans on every query. Fix: index queried fields — can be 1000x faster.<br>\r\n      ❌ <strong>N+1 queries</strong>: 101 queries instead of 2. Fix: populate, joins, batch loading.<br>\r\n      ❌ <strong>Loading entire datasets into memory</strong>: <code>User.find()</code> returns 1 million records. Fix: pagination, streams, cursors.<br>\r\n      ❌ <strong>No connection pooling</strong>: New DB connection per request. Fix: pg Pool, Mongoose pools automatically.<br>\r\n      ❌ <strong>Unhandled promise rejections</strong>: Silent failures, undefined behavior. Fix: always catch, set up global handler.<br>\r\n      ❌ <strong>No graceful shutdown</strong>: Requests dropped on deploy. Fix: handle SIGTERM, close server first.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n  <div class=\"q-top\"><span class=\"q-num\">71</span><span class=\"q-question\">What is TypeScript with Node.js and why should you use it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>TypeScript adds static types to JavaScript. Your editor catches type errors before you run the code. In Node.js, server-side bugs affect all users — TypeScript prevents an entire class of runtime errors before they reach production.</div>\r\n    <pre>// TypeScript catches errors at write time:\r\ninterface User {\r\n  id: number;\r\n  name: string;\r\n  email: string;\r\n  createdAt: Date;\r\n}\r\n\r\nasync function getUser(id: number): Promise&lt;User | null&gt; {\r\n  return db.findById(id); // TypeScript verifies return type!\r\n}\r\n\r\n// Express with types:\r\nimport { Request, Response, NextFunction } from 'express';\r\n\r\napp.get('/users/:id', async (req: Request, res: Response, next: NextFunction) => {\r\n  const id = parseInt(req.params.id); // TypeScript: params are string\r\n  const user: User | null = await getUser(id);\r\n  if (!user) return res.status(404).json({ error: 'Not found' });\r\n  res.json(user); // TypeScript confirms user matches User interface\r\n});</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Benefits in Node.js 👇</span>Catch null reference crashes before production. Autocomplete for your API models. Safe refactoring — find all breaking changes instantly. Self-documenting code — types ARE the documentation. Industry standard for serious backends in 2024+.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">72</span><span class=\"q-question\">What is npm audit and how do you handle security vulnerabilities?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span><code>npm audit</code> scans your dependencies for known security vulnerabilities and reports them with severity levels. Run it regularly — a transitive dependency (a package your package uses) could have a critical CVE.</div>\r\n    <pre># Check for vulnerabilities:\r\nnpm audit\r\n\r\n# Auto-fix where possible:\r\nnpm audit fix\r\n\r\n# Fix even breaking changes (major versions):\r\nnpm audit fix --force  # careful — may break things!\r\n\r\n# See detailed report:\r\nnpm audit --json | jq '.vulnerabilities'\r\n\r\n# In CI/CD — fail the build if high vulnerabilities:\r\nnpm audit --audit-level=high\r\n\r\n# Check specific package:\r\nnpm why lodash    # why is this package in my dependencies?\r\n\r\n# Keep dependencies updated (use a tool like Dependabot):\r\n# GitHub → Settings → Security → Dependabot alerts\r\n# Creates PRs automatically when vulnerabilities are found!</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Workflow 🔄</span>Add <code>npm audit --audit-level=moderate</code> to your CI pipeline. This blocks deployment if medium+ severity vulnerabilities are found. Dependabot or Renovate automatically creates pull requests when new vulnerable versions are discovered and fixed versions are available.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">73</span><span class=\"q-question\">What is distributed tracing and how do you add it to Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>In a microservices system, one user request might touch 10 services. Distributed tracing follows that request's entire journey — how long each service took, where errors occurred, which database calls were slow.</div>\r\n    <pre>// OpenTelemetry — the standard for distributed tracing:\r\nconst { NodeSDK } = require('@opentelemetry/sdk-node');\r\nconst { getNodeAutoInstrumentations } = require('@opentelemetry/auto-instrumentations-node');\r\nconst { OTLPTraceExporter } = require('@opentelemetry/exporter-otlp-http');\r\n\r\nconst sdk = new NodeSDK({\r\n  traceExporter: new OTLPTraceExporter({\r\n    url: process.env.OTLP_ENDPOINT // Jaeger, Datadog, Honeycomb, etc.\r\n  }),\r\n  instrumentations: [\r\n    getNodeAutoInstrumentations() // auto-instruments: http, express, mongodb!\r\n  ]\r\n});\r\nsdk.start(); // MUST start before importing other modules!\r\n\r\n// Now every request automatically gets a trace ID and spans!\r\n// View complete request journey in your APM tool.</pre>\r\n    <div class=\"example-box\"><span class=\"label\">What you see with tracing 🔍</span>Request → API Gateway (5ms) → User Service (12ms) → MongoDB query (45ms) → Redis cache write (2ms) → Response (total: 64ms). Without tracing, debugging slow requests is guesswork. With tracing, you see exactly which step was slow.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">74</span><span class=\"q-question\">What is the health check endpoint and why is it essential?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A health check endpoint (<code>GET /health</code>) lets load balancers, Kubernetes, and monitoring tools check if your app is working. If it returns an error, the orchestrator routes traffic away from that instance automatically.</div>\r\n    <pre>app.get('/health', async (req, res) => {\r\n  const status = { status: 'ok', uptime: process.uptime(), checks: {} };\r\n\r\n  // Check database connectivity:\r\n  try {\r\n    await mongoose.connection.db.admin().ping();\r\n    status.checks.database = 'ok';\r\n  } catch (err) {\r\n    status.checks.database = 'error';\r\n    status.status = 'degraded';\r\n  }\r\n\r\n  // Check Redis:\r\n  try {\r\n    await redis.ping();\r\n    status.checks.redis = 'ok';\r\n  } catch (err) {\r\n    status.checks.redis = 'error';\r\n    status.status = 'degraded';\r\n  }\r\n\r\n  // Add memory check:\r\n  const { heapUsed, heapTotal } = process.memoryUsage();\r\n  status.checks.memory = `${Math.round(heapUsed/1024/1024)}MB / ${Math.round(heapTotal/1024/1024)}MB`;\r\n\r\n  const code = status.status === 'ok' ? 200 : 503;\r\n  res.status(code).json(status);\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">75</span><span class=\"q-question\">What are API versioning strategies in Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>API versioning lets you change your API without breaking existing clients. Old clients use v1, new clients use v2 — both work simultaneously. The most common approach: version in the URL path.</div>\r\n    <pre>// URL versioning (most common, most explicit):\r\napp.use('/api/v1/users', v1UserRoutes);\r\napp.use('/api/v2/users', v2UserRoutes);\r\n\r\n// Header versioning (cleaner URLs, less discoverable):\r\napp.use((req, res, next) => {\r\n  req.apiVersion = req.headers['accept-version'] || '1';\r\n  next();\r\n});\r\napp.get('/api/users', (req, res) => {\r\n  return req.apiVersion === '2' ? v2Handler(req, res) : v1Handler(req, res);\r\n});\r\n\r\n// Deprecation headers (warn clients before removing old version):\r\napp.use('/api/v1', (req, res, next) => {\r\n  res.set('Deprecation', 'true');\r\n  res.set('Sunset', 'Sat, 31 Dec 2025 23:59:59 GMT');\r\n  res.set('Link', '&lt;https://api.example.com/api/v2&gt;; rel=\"successor-version\"');\r\n  next();\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">76</span><span class=\"q-question\">What is session management vs JWT and which should you use?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Sessions store user state on the server, browser gets a session ID. JWTs store everything in the token, server is stateless. Each has real trade-offs — choosing depends on your architecture.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Sessions 🆚 JWT detailed comparison 👇</span>\r\n      <strong>Sessions</strong>:<br>\r\n      ✅ Easy to invalidate instantly — delete from Redis/DB.<br>\r\n      ✅ Small cookie — just a session ID (not the user's data).<br>\r\n      ❌ Requires server-side storage — needs Redis for multi-server setups.<br>\r\n      ❌ Every request requires a Redis lookup.<br><br>\r\n      <strong>JWT</strong>:<br>\r\n      ✅ Stateless — no server storage needed, works across microservices.<br>\r\n      ✅ Contains user info — no DB lookup per request (faster!).<br>\r\n      ❌ Hard to invalidate — can't revoke a token before it expires (need a blocklist!).<br>\r\n      ❌ Grows with payload size. If secret leaks, ALL tokens are compromised.<br><br>\r\n      <strong>Use sessions</strong>: traditional web apps, when you need instant logout.<br>\r\n      <strong>Use JWT</strong>: stateless APIs, microservices, mobile apps, third-party token sharing.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">77</span><span class=\"q-question\">What is file upload handling and what are the security considerations?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Use <strong>Multer</strong> for handling file uploads in Express. Always validate file type, size, and name — attackers will try to upload malicious files.</div>\r\n    <pre>const multer = require('multer');\r\nconst path = require('path');\r\n\r\nconst storage = multer.diskStorage({\r\n  destination: 'uploads/',\r\n  filename: (req, file, cb) => {\r\n    // NEVER use the original filename — path traversal attack!\r\n    const safeFilename = `${Date.now()}-${Math.random().toString(36)}`\r\n      + path.extname(file.originalname).toLowerCase();\r\n    cb(null, safeFilename);\r\n  }\r\n});\r\n\r\nconst upload = multer({\r\n  storage,\r\n  limits: { fileSize: 5 * 1024 * 1024 }, // 5MB max\r\n  fileFilter: (req, file, cb) => {\r\n    // Validate MIME type — check mimetype AND extension:\r\n    const allowedTypes = ['image/jpeg', 'image/png', 'image/webp'];\r\n    const allowedExts  = ['.jpg', '.jpeg', '.png', '.webp'];\r\n    const ext = path.extname(file.originalname).toLowerCase();\r\n\r\n    if (allowedTypes.includes(file.mimetype) && allowedExts.includes(ext)) {\r\n      cb(null, true);\r\n    } else {\r\n      cb(new Error('Only JPEG, PNG, WebP images allowed'));\r\n    }\r\n  }\r\n});\r\n\r\napp.post('/upload', upload.single('photo'), (req, res) => {\r\n  res.json({ filename: req.file.filename });\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">78</span><span class=\"q-question\">What is database indexing and why does it matter for Node.js apps?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>A database index is like a book's index — instead of scanning every page to find a topic, you jump straight to it. Without indexes, the database scans every row (O(n)). With indexes, it finds records in O(log n). The difference: 2ms vs 2000ms for a million-row table.</div>\r\n    <pre>// MongoDB — add indexes in Mongoose schema:\r\nconst userSchema = new mongoose.Schema({\r\n  email: { type: String, unique: true }, // unique creates an index!\r\n  name: String,\r\n  age: Number,\r\n  role: String,\r\n  createdAt: { type: Date, default: Date.now }\r\n});\r\n\r\nuserSchema.index({ email: 1 });           // single field index\r\nuserSchema.index({ name: 1, age: -1 });   // compound index\r\nuserSchema.index({ name: 'text' });       // full-text search index\r\nuserSchema.index({ createdAt: -1 });      // sort index for newest first\r\n\r\n// PostgreSQL:\r\n// CREATE INDEX CONCURRENTLY idx_users_email ON users(email);\r\n// CREATE INDEX idx_users_name_age ON users(name, age DESC);\r\n\r\n// When to index:\r\n// ✅ Fields in WHERE clauses: WHERE email = '...'\r\n// ✅ Fields you ORDER BY: ORDER BY created_at DESC\r\n// ✅ Fields in JOIN conditions\r\n// ❌ Don't index every field — indexes slow down writes and use disk space!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">79</span><span class=\"q-question\">What are microservices and how does Node.js fit in?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Microservices architecture splits an app into many small, independent services — each responsible for one feature, deployed independently, scaling independently. Node.js is ideal: lightweight, fast startup, excellent I/O performance.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Example microservices breakdown 🏗️</span>\r\n      Instead of ONE large monolith:<br>\r\n      ✅ <strong>user-service</strong>: registration, login, profile.<br>\r\n      ✅ <strong>product-service</strong>: catalog, inventory, pricing.<br>\r\n      ✅ <strong>order-service</strong>: checkout, order history.<br>\r\n      ✅ <strong>notification-service</strong>: emails, push notifications, SMS.<br>\r\n      ✅ <strong>api-gateway</strong>: routes requests to correct service, handles auth.<br><br>\r\n      Services communicate via: REST HTTP (simple), gRPC (fast, typed), or message queues (Kafka, RabbitMQ — async, decoupled).<br><br>\r\n      Node.js per service: 30-100MB RAM, starts in <1 second. Run hundreds of instances cheaply. Perfect for microservices!\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">80</span><span class=\"q-question\">What is compression and how does it improve API performance?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Compression reduces HTTP response sizes — a 100KB JSON response becomes 20KB after gzip. This saves bandwidth and speeds up responses, especially on slow connections. One middleware line adds it to your entire Express app.</div>\r\n    <pre>const compression = require('compression');\r\n\r\n// Enable gzip/brotli for all responses:\r\napp.use(compression({\r\n  level: 6,       // compression level 1-9 (6 is the sweet spot)\r\n  threshold: 1024 // only compress responses > 1KB (small responses not worth it)\r\n}));\r\n\r\n// Effect: 100KB JSON → ~15-25KB (75-85% reduction!)\r\n// Most impactful on: large JSON responses, HTML, CSS, JS files\r\n\r\n// DON'T compress: images (already compressed), videos, zip files\r\n// The compression middleware automatically skips these MIME types\r\n\r\n// In production with Nginx as reverse proxy:\r\n// Let Nginx handle compression instead — it's more efficient:\r\n// gzip on;\r\n// gzip_types text/plain application/json application/javascript text/css;\r\n// gzip_min_length 1000;</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">81</span><span class=\"q-question\">What is request context and how do you pass it through your app?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Request context is data tied to a specific request — user ID, request ID, tenant ID. You need it available throughout the entire async chain (controllers, services, repositories) without passing it as a parameter to every function.</div>\r\n    <pre>const { AsyncLocalStorage } = require('async_hooks');\r\nconst als = new AsyncLocalStorage();\r\n\r\n// Middleware — create context for each request:\r\napp.use((req, res, next) => {\r\n  als.run({\r\n    requestId: crypto.randomUUID(),\r\n    userId: req.user?.id,\r\n    tenantId: req.headers['x-tenant-id']\r\n  }, next);\r\n});\r\n\r\n// Anywhere in your codebase — no prop drilling!\r\nfunction getContext() { return als.getStore(); }\r\n\r\n// Service (no need to receive requestId as parameter!):\r\nclass UserService {\r\n  async createUser(data) {\r\n    const { userId, requestId } = getContext();\r\n    logger.info('Creating user', { requestId, createdBy: userId });\r\n    const user = await this.repo.create(data);\r\n    logger.info('User created', { requestId, newUserId: user.id });\r\n    return user;\r\n  }\r\n}</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">82</span><span class=\"q-question\">What is the SOLID principle applied to Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>SOLID is 5 design principles that make Node.js code easier to test, extend, and maintain as your app grows.</div>\r\n    <div class=\"example-box\"><span class=\"label\">SOLID in Node.js 👇</span>\r\n      <strong>S — Single Responsibility</strong>: UserController only handles HTTP. UserService only handles business logic. UserRepository only handles DB. One job each.<br><br>\r\n      <strong>O — Open/Closed</strong>: Add new behavior by adding new event listeners — don't modify OrderService. \"Open for extension, closed for modification.\"<br><br>\r\n      <strong>L — Liskov Substitution</strong>: <code>MongoUserRepository</code> and <code>PostgresUserRepository</code> implement the same interface. Either can replace the other in UserService.<br><br>\r\n      <strong>I — Interface Segregation</strong>: Don't make a service implement 20 methods it doesn't need. Split into focused interfaces.<br><br>\r\n      <strong>D — Dependency Inversion</strong>: UserService depends on <code>IUserRepository</code> (interface), not <code>MongoUserRepository</code> (concrete). Inject the implementation. Easy to swap, easy to test.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">83</span><span class=\"q-question\">What is the difference between 'require' caching and singleton pattern?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Node.js caches every module after the first <code>require()</code>. The same module object is returned every time — even from different files. This makes modules naturally behave as singletons.</div>\r\n    <pre>// db.js — database connection module:\r\nconst mongoose = require('mongoose');\r\nlet connection = null;\r\n\r\nasync function connect() {\r\n  if (!connection) {\r\n    connection = await mongoose.connect(process.env.MONGODB_URI);\r\n    console.log('DB connected'); // only runs ONCE!\r\n  }\r\n  return connection;\r\n}\r\n\r\nmodule.exports = { connect };\r\n\r\n// app.js:\r\nconst db = require('./db');\r\nawait db.connect();\r\n\r\n// controllers/users.js — requires the same module:\r\nconst db = require('./db');\r\nawait db.connect(); // returns the cached connection — no new connection!\r\n\r\n// This is why database connection modules work as singletons:\r\n// require() returns the exact same object every time!</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Beware of singleton problems ⚠️</span>In tests, the cached module persists between test files! If one test modifies the singleton, other tests see the change. Fix: use <code>jest.resetModules()</code> or <code>jest.isolateModules()</code> between tests, or design modules to be resettable.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">84</span><span class=\"q-question\">What is the difference between REST and GraphQL APIs in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>REST uses multiple endpoints, each returning a fixed data shape. GraphQL uses one endpoint where the client specifies exactly what data it wants. GraphQL solves over-fetching and under-fetching problems.</div>\r\n    <div class=\"example-box\"><span class=\"label\">REST vs GraphQL 🆚</span>\r\n      <strong>REST</strong>: <code>GET /users/1</code> returns ALL user fields even if you need only the name. Need the user's posts? Another request: <code>GET /users/1/posts</code>. Two round trips.<br><br>\r\n      <strong>GraphQL</strong>: One request, ask for exactly what you need:\r\n      <pre>query {\r\n  user(id: 1) {\r\n    name        # only name\r\n    posts {     # and their posts\r\n      title     # only title\r\n    }\r\n  }\r\n}</pre>\r\n      <strong>When REST is better</strong>: simple CRUD, file uploads, caching is critical (REST responses are cacheable by URL).<br>\r\n      <strong>When GraphQL is better</strong>: complex related data, many different client types (mobile vs web want different data), rapid frontend development.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">85</span><span class=\"q-question\">What is debounce and throttle in the context of Node.js?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>In Node.js backends, debounce and throttle prevent functions from running too frequently — useful for batch processing, preventing duplicate API calls, and protecting external services from being overwhelmed.</div>\r\n    <pre>// Debounce — batch rapid events into one call:\r\nfunction debounce(fn, delay) {\r\n  let timer;\r\n  return (...args) => {\r\n    clearTimeout(timer);\r\n    timer = setTimeout(() => fn(...args), delay);\r\n  };\r\n}\r\n\r\n// Example: user editing triggers save — only save 500ms after they stop:\r\nconst debouncedSave = debounce(async (docId, content) => {\r\n  await Document.findByIdAndUpdate(docId, { content });\r\n  console.log('Saved!'); // only fires after user stops typing for 500ms\r\n}, 500);\r\n\r\n// Throttle — run at most once per interval:\r\nfunction throttle(fn, limit) {\r\n  let lastRun = 0;\r\n  return (...args) => {\r\n    const now = Date.now();\r\n    if (now - lastRun >= limit) {\r\n      lastRun = now;\r\n      return fn(...args);\r\n    }\r\n  };\r\n}\r\n\r\n// Throttle a webhook handler — process at most once per second:\r\nconst throttledWebhook = throttle(processPaymentWebhook, 1000);</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">86</span><span class=\"q-question\">What is the difference between OAuth 2.0 and JWT authentication?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>OAuth 2.0 is an authorization <strong>framework</strong> (a set of rules for getting permission). JWT is a token <strong>format</strong> (a way to carry permission). They're different things — OAuth often uses JWT as its token format.</div>\r\n    <div class=\"example-box\"><span class=\"label\">How they relate 👇</span>\r\n      <strong>OAuth 2.0</strong> defines the flow: \"Redirect to Google → User approves → Google gives token → Use token to call Google APIs.\" It's about the handshake protocol.<br><br>\r\n      <strong>JWT</strong> defines the token shape: a base64-encoded JSON payload with a signature. It's about what the token looks like and how to verify it.<br><br>\r\n      Combined: You implement OAuth to let users \"Login with Google.\" Google gives back a JWT access token. You decode and verify the JWT to know who the user is.<br><br>\r\n      Your own auth doesn't need OAuth — you can issue your own JWTs directly after verifying username/password. OAuth is for when you need to let users grant access to third-party apps.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">87</span><span class=\"q-question\">What are database migrations and how do you use them?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Migrations are versioned scripts that change your database schema over time. They run in order and track which have already been applied. Never change the production database schema manually — always use migrations.</div>\r\n    <pre>// Prisma migrations (recommended for new projects):\r\n// 1. Change your schema.prisma:\r\n// model User {\r\n//   id    Int    @id @default(autoincrement())\r\n//   email String @unique\r\n//   name  String  // ← new field\r\n// }\r\n\r\n// 2. Create migration:\r\n// npx prisma migrate dev --name \"add_name_to_users\"\r\n// → Creates: migrations/20240101_add_name_to_users/migration.sql\r\n\r\n// 3. Deploy to production:\r\n// npx prisma migrate deploy\r\n\r\n// Knex migrations (framework-agnostic):\r\n// exports.up = knex => knex.schema.table('users', t => {\r\n//   t.string('name').notNullable().defaultTo('');\r\n//   t.index('email');\r\n// });\r\n// exports.down = knex => knex.schema.table('users', t => {\r\n//   t.dropColumn('name');\r\n// });</pre>\r\n    <div class=\"example-box\"><span class=\"label\">Why migrations matter 🏗️</span>Without migrations: \"Add a phone_number column to production\" — SSH in, run ALTER TABLE manually, pray nothing breaks, have no history of what changed. With migrations: commit the file, CI/CD runs it automatically, it's tracked in version control, reversible, and anyone can reproduce the database state exactly.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">88</span><span class=\"q-question\">How do you implement retries with exponential backoff?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>When calling external services that might fail temporarily (network blip, rate limit), retry with increasing delays between attempts. Exponential backoff prevents hammering an already struggling service.</div>\r\n    <pre>// Retry with exponential backoff:\r\nasync function withRetry(fn, { maxAttempts = 3, initialDelay = 100 } = {}) {\r\n  let lastError;\r\n  for (let attempt = 1; attempt <= maxAttempts; attempt++) {\r\n    try {\r\n      return await fn(); // try the operation\r\n    } catch (err) {\r\n      lastError = err;\r\n      if (attempt === maxAttempts) break; // last attempt failed\r\n      // Don't retry client errors (4xx):\r\n      if (err.status >= 400 && err.status < 500) throw err;\r\n\r\n      const delay = initialDelay * 2 ** (attempt - 1) // exponential\r\n        + Math.random() * 100; // jitter — prevents thundering herd!\r\n      console.log(`Attempt ${attempt} failed — retrying in ${delay}ms`);\r\n      await new Promise(r => setTimeout(r, delay));\r\n    }\r\n  }\r\n  throw lastError;\r\n}\r\n\r\n// Usage:\r\nconst data = await withRetry(\r\n  () => callUnreliableAPI(),\r\n  { maxAttempts: 4, initialDelay: 200 }\r\n);\r\n// Delays: 200ms, 400ms, 800ms — then gives up</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">89</span><span class=\"q-question\">What is the difference between optimistic and pessimistic locking in databases?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>When multiple requests might update the same data simultaneously, you need concurrency control. <strong>Pessimistic locking</strong>: lock the record before reading it — nobody else can touch it until you're done. <strong>Optimistic locking</strong>: no lock, but detect if someone else changed it before you save.</div>\r\n    <pre>// Optimistic locking with MongoDB (version field):\r\nconst productSchema = new mongoose.Schema({\r\n  name: String,\r\n  stock: Number,\r\n  __v: Number // Mongoose uses __v for optimistic locking!\r\n});\r\n\r\n// Update only if version matches — fails if someone else updated first:\r\nconst result = await Product.findOneAndUpdate(\r\n  { _id: productId, __v: currentVersion }, // version check!\r\n  { $inc: { stock: -quantity }, $inc: { __v: 1 } },\r\n  { new: true }\r\n);\r\nif (!result) {\r\n  // Another request updated this product first — retry!\r\n  throw new Error('Concurrent update conflict — please try again');\r\n}\r\n\r\n// Pessimistic locking with PostgreSQL (SELECT FOR UPDATE):\r\n// BEGIN;\r\n// SELECT * FROM products WHERE id = $1 FOR UPDATE; -- locks the row!\r\n// UPDATE products SET stock = stock - $2 WHERE id = $1;\r\n// COMMIT; -- releases the lock</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">90</span><span class=\"q-question\">How do you handle multi-tenancy in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Multi-tenancy means one app instance serves multiple customers (tenants) with their data kept separate. The three strategies are: separate databases, shared database with tenant column, or schema isolation.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Three strategies 🏢</span>\r\n      <strong>Database per tenant</strong>: Each customer gets their own database. Most isolation, most expensive. Good for: enterprise SaaS, GDPR compliance.<br>\r\n      <strong>Shared DB, tenant_id column</strong>: All tenants' data in one DB, every table has a <code>tenant_id</code>. Cheapest, must be very careful to filter by tenant_id everywhere.<br>\r\n      <strong>Schema per tenant</strong>: One database, separate schema per tenant (PostgreSQL). Middle ground.\r\n    </div>\r\n    <pre>// Shared DB approach — middleware to set tenant context:\r\napp.use(async (req, res, next) => {\r\n  const tenantId = req.headers['x-tenant-id'] || req.user?.tenantId;\r\n  if (!tenantId) return res.status(400).json({ error: 'Tenant required' });\r\n  req.tenantId = tenantId;\r\n  next();\r\n});\r\n\r\n// Always filter by tenantId in queries!\r\napp.get('/api/products', async (req, res) => {\r\n  const products = await Product.find({ tenantId: req.tenantId }); // ✅\r\n  res.json(products);\r\n});\r\n// Forgetting tenantId in one query = data leak! Use middleware/repository\r\n// that automatically adds tenantId to every query.</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">91</span><span class=\"q-question\">What is the event-driven architecture in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Event-driven architecture (EDA) means your app reacts to events instead of calling services directly. Services publish events, other services subscribe and react. This creates a loosely coupled system where services don't need to know about each other.</div>\r\n    <div class=\"example-box\"><span class=\"label\">EDA vs Direct calls 🆚</span>\r\n      <strong>Direct call</strong>: UserService → EmailService → InventoryService → AnalyticsService. All must work. If Analytics is down, the whole flow fails. Changes require updating UserService.<br><br>\r\n      <strong>EDA</strong>: UserService emits <code>user.registered</code>. EmailService, InventoryService, and AnalyticsService each subscribe independently. If Analytics is down, it misses the event (or replays from queue). Adding new behavior means adding a new subscriber — zero changes to UserService.\r\n    </div>\r\n    <pre>// Using a message queue (BullMQ) as the event bus:\r\nconst { Queue, Worker } = require('bullmq');\r\nconst eventBus = new Queue('events', { connection });\r\n\r\n// Publisher — UserService:\r\nawait eventBus.add('user.registered', { userId, email, name });\r\n\r\n// Subscriber 1 — EmailService (separate process):\r\nnew Worker('events', async job => {\r\n  if (job.name === 'user.registered') await sendWelcomeEmail(job.data);\r\n}, { connection });\r\n\r\n// Subscriber 2 — AnalyticsService (separate process):\r\nnew Worker('events', async job => {\r\n  if (job.name === 'user.registered') await trackNewUser(job.data);\r\n}, { connection });</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">92</span><span class=\"q-question\">What is idempotency and why is it important in APIs?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>An idempotent operation produces the same result no matter how many times you repeat it. GET, PUT, DELETE are idempotent. POST is NOT — calling it twice creates two records. In Node.js APIs, you should make critical operations idempotent to handle retries safely.</div>\r\n    <pre>// Non-idempotent POST — dangerous!\r\napp.post('/api/payments', async (req, res) => {\r\n  const payment = await processPayment(req.body.amount);\r\n  // If client retries (network timeout), the user gets charged TWICE!\r\n  res.json(payment);\r\n});\r\n\r\n// Idempotent version — safe to retry:\r\napp.post('/api/payments', async (req, res) => {\r\n  const idempotencyKey = req.headers['idempotency-key'];\r\n  if (!idempotencyKey) return res.status(400).json({ error: 'Idempotency-Key required' });\r\n\r\n  // Check if we've seen this key before:\r\n  const existing = await redis.get(`payment:${idempotencyKey}`);\r\n  if (existing) return res.json(JSON.parse(existing)); // return cached result!\r\n\r\n  // First time — process the payment:\r\n  const payment = await processPayment(req.body.amount);\r\n\r\n  // Cache result with 24h TTL:\r\n  await redis.setex(`payment:${idempotencyKey}`, 86400, JSON.stringify(payment));\r\n  res.json(payment);\r\n  // Now the client can safely retry — gets same result, no double charge!\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">93</span><span class=\"q-question\">How do you profile Node.js applications to find bottlenecks?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Profiling helps you find WHERE your app is slow and WHY. Node.js has built-in profiling via <code>--prof</code> flag, the Inspector, and tools like Clinic.js and 0x for flame graphs.</div>\r\n    <pre># Method 1 — V8 built-in profiler:\r\nnode --prof app.js\r\n# ... run some requests ...\r\nnode --prof-process isolate-*.log > processed.txt\r\n\r\n# Method 2 — Node.js Inspector (Chrome DevTools):\r\nnode --inspect app.js\r\n# Open Chrome → chrome://inspect → Click \"inspect\" on your app\r\n# Go to \"Performance\" tab → Record → Profile CPU/Memory\r\n\r\n# Method 3 — clinic.js (easiest):\r\nnpm install -g clinic\r\nclinic doctor -- node app.js  # finds event loop delays\r\nclinic flame  -- node app.js  # flame graph of CPU usage\r\nclinic bubbleprof -- node app.js  # async bottleneck visualization\r\n\r\n# Method 4 — 0x (flame graphs):\r\nnpm install -g 0x\r\n0x app.js\r\n# Generates an interactive flame graph showing where time is spent</pre>\r\n    <div class=\"example-box\"><span class=\"label\">What to look for 🔍</span>Wide boxes in flame graphs = slow functions. Event loop delay > 10ms = something is blocking. Growing heap in memory profiler = potential leak. Many small gaps in async profiler = N+1 queries or too many sequential awaits.</div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">94</span><span class=\"q-question\">What are the most common Node.js interview mistakes to avoid?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Common mistakes that fail interviews 💡</span></div>\r\n    <div class=\"example-box\"><span class=\"label\">Top mistakes 👇</span>\r\n      ❌ <strong>Blocking the event loop</strong>: CPU-heavy sync code in route handlers. Show you know: Worker Threads and job queues.<br><br>\r\n      ❌ <strong>Uncaught Promise rejections</strong>: Missing .catch() or try/catch in async code. Always handle async errors.<br><br>\r\n      ❌ <strong>Not understanding the event loop phases</strong>: Confusing nextTick and setImmediate. Know the 6 phases and microtask priority.<br><br>\r\n      ❌ <strong>require() in wrong places</strong>: require() inside a function or loop — runs every call, defeats caching. Always require at top level.<br><br>\r\n      ❌ <strong>Storing secrets in code</strong>: Hardcoded API keys. Always use environment variables.<br><br>\r\n      ❌ <strong>No input validation</strong>: Trusting client data. Always validate with Joi/Zod.<br><br>\r\n      ❌ <strong>Missing error handling middleware</strong>: Errors leak stack traces to users. Always add a catch-all error handler in Express.<br><br>\r\n      ❌ <strong>N+1 queries</strong>: Fetching related data in a loop. Use populate, joins, or batch loading.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">95</span><span class=\"q-question\">What is the 'You might not need a framework' consideration?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Node.js's built-in <code>http</code> module can handle simple use cases. Knowing when to use Express vs Fastify vs bare http shows deep understanding. More framework = more overhead. Sometimes raw Node.js is the right answer.</div>\r\n    <pre>// Raw Node.js HTTP — no framework, maximum performance:\r\nconst http = require('http');\r\n\r\nconst server = http.createServer(async (req, res) => {\r\n  if (req.method === 'GET' && req.url === '/health') {\r\n    res.writeHead(200, { 'Content-Type': 'application/json' });\r\n    return res.end(JSON.stringify({ status: 'ok' }));\r\n  }\r\n  if (req.method === 'GET' && req.url === '/users') {\r\n    const users = await getUsers();\r\n    res.writeHead(200, { 'Content-Type': 'application/json' });\r\n    return res.end(JSON.stringify(users));\r\n  }\r\n  res.writeHead(404);\r\n  res.end('Not Found');\r\n});\r\nserver.listen(3000);\r\n\r\n// When to use raw http: webhooks, health check servers, internal tools\r\n// When to use Express: most API projects (mature, vast ecosystem)\r\n// When to use Fastify: performance-critical APIs (2x faster than Express)</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">96</span><span class=\"q-question\">What is Fastify and how does it compare to Express?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>Fastify is a newer Node.js web framework that is significantly faster than Express — about 2x higher throughput in benchmarks. It has JSON schema validation built in, TypeScript support, and a modern plugin system.</div>\r\n    <div class=\"example-box\"><span class=\"label\">Express vs Fastify 🆚</span>\r\n      <strong>Express</strong>: huge ecosystem, massive community, millions of tutorials, very mature. Slower. Minimal built-in features. Flexible.<br>\r\n      <strong>Fastify</strong>: much faster (uses faster JSON serialization, pipelining), built-in schema validation, TypeScript-first, structured plugin system, newer.<br><br>\r\n      <strong>Choose Express</strong>: team already knows it, lots of middleware needed, stability over performance, maximum ecosystem compatibility.<br>\r\n      <strong>Choose Fastify</strong>: new project, performance matters, TypeScript project, want built-in validation.\r\n    </div>\r\n    <pre>// Fastify routes look similar to Express but with schema:\r\nconst fastify = require('fastify')({ logger: true });\r\n\r\nfastify.post('/users', {\r\n  schema: {\r\n    body: {\r\n      type: 'object',\r\n      required: ['email', 'name'],\r\n      properties: {\r\n        email: { type: 'string', format: 'email' },\r\n        name:  { type: 'string', minLength: 2 }\r\n      }\r\n    }\r\n  }\r\n}, async (request, reply) => {\r\n  const user = await User.create(request.body);\r\n  return reply.status(201).send(user);\r\n});</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">97</span><span class=\"q-question\">What is streaming JSON responses and why would you use it?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>When returning very large JSON responses, streaming lets you send data to the client as it's produced — instead of building the entire response in memory first. The client starts receiving data immediately.</div>\r\n    <pre>const { Readable } = require('stream');\r\n\r\napp.get('/api/export', async (req, res) => {\r\n  res.setHeader('Content-Type', 'application/json');\r\n\r\n  // Stream a large dataset as newline-delimited JSON:\r\n  res.write('['); // start JSON array\r\n  let first = true;\r\n\r\n  // Use a database cursor — doesn't load all records into memory!\r\n  const cursor = User.find().lean().cursor();\r\n  for await (const user of cursor) {\r\n    if (!first) res.write(',');\r\n    res.write(JSON.stringify(user));\r\n    first = false;\r\n  }\r\n\r\n  res.write(']');\r\n  res.end();\r\n});\r\n\r\n// Or use a Transform stream for clean code:\r\nconst { pipeline } = require('stream/promises');\r\nconst { stringify } = require('JSONStream');\r\n\r\nawait pipeline(\r\n  User.find().lean().cursor(),\r\n  stringify('[', ',', ']'), // wraps in JSON array\r\n  res\r\n);</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">98</span><span class=\"q-question\">What is the Singleton pattern in Node.js and how does module caching help?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>The Singleton pattern ensures only one instance of a class/object exists. In Node.js, module caching automatically makes module-level objects singletons — the first <code>require()</code> runs the module code, subsequent requires return the cached export.</div>\r\n    <pre>// logger.js — automatically a singleton due to module caching:\r\nconst winston = require('winston');\r\n\r\nconst logger = winston.createLogger({\r\n  level: 'info',\r\n  format: winston.format.json(),\r\n  transports: [new winston.transports.Console()]\r\n});\r\n\r\nmodule.exports = logger;\r\n// First require() → creates logger. All subsequent requires → same instance!\r\n\r\n// redis-client.js — singleton Redis connection:\r\nconst Redis = require('ioredis');\r\nconst client = new Redis(process.env.REDIS_URL);\r\n\r\nclient.on('connect', () => console.log('Redis connected'));\r\nclient.on('error', err => console.error('Redis error:', err));\r\n\r\nmodule.exports = client;\r\n// Every file that requires this gets the SAME Redis connection!\r\n// No need for connection management — Node.js handles it!\r\n\r\n// Usage in multiple files:\r\nconst redis = require('./redis-client'); // same connection everywhere!</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">99</span><span class=\"q-question\">How do you implement a simple API gateway in Node.js?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Simple answer 💡</span>An API gateway is a single entry point that routes requests to the right microservice. It handles cross-cutting concerns: auth, rate limiting, logging, and request routing — so individual services don't need to.</div>\r\n    <pre>const express = require('express');\r\nconst httpProxy = require('http-proxy-middleware');\r\n\r\nconst app = express();\r\n\r\n// Cross-cutting concerns applied to ALL routes:\r\napp.use(helmet());\r\napp.use(cors({ origin: process.env.ALLOWED_ORIGINS }));\r\napp.use(rateLimiter);\r\napp.use(authenticate); // verify JWT once at the gateway!\r\n\r\n// Route to microservices:\r\napp.use('/api/users',    httpProxy.createProxyMiddleware({\r\n  target: process.env.USER_SERVICE_URL,\r\n  changeOrigin: true,\r\n  pathRewrite: { '^/api/users': '' } // remove prefix before forwarding\r\n}));\r\napp.use('/api/products', httpProxy.createProxyMiddleware({\r\n  target: process.env.PRODUCT_SERVICE_URL,\r\n  changeOrigin: true,\r\n  pathRewrite: { '^/api/products': '' }\r\n}));\r\napp.use('/api/orders',   httpProxy.createProxyMiddleware({\r\n  target: process.env.ORDER_SERVICE_URL,\r\n  changeOrigin: true\r\n}));\r\n\r\napp.listen(3000);</pre>\r\n  </div>\r\n</div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n  <div class=\"q-top\"><span class=\"q-num\">100</span><span class=\"q-question\">What are the most important Node.js concepts to master?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n  <div class=\"q-body\">\r\n    <div class=\"simple-box\"><span class=\"label\">Master these and you're ready for any Node.js interview 💡</span></div>\r\n    <div class=\"example-box\"><span class=\"label\">The core fundamentals 👇</span>\r\n      🔄 <strong>Event Loop</strong> — phases, microtasks vs macrotasks, nextTick vs setImmediate, why it enables high concurrency with one thread.<br>\r\n      📦 <strong>Modules</strong> — CommonJS caching (singletons!), module.exports vs exports, ES Modules differences.<br>\r\n      🌊 <strong>Streams</strong> — 4 types, pipe, backpressure, pipeline(), when to use over loading into memory.<br>\r\n      📡 <strong>EventEmitter</strong> — on, emit, once, off, the special 'error' event, memory leak warning.<br>\r\n      ⏳ <strong>Async patterns</strong> — callbacks → Promises → async/await, sequential vs parallel, race conditions, error handling.<br>\r\n      🌐 <strong>Express middleware</strong> — execution order, next(), error handler (4 params), routing.<br>\r\n      🔐 <strong>Security</strong> — JWT, bcrypt, CORS, helmet, rate limiting, input validation, never trust client input.<br>\r\n      🚀 <strong>Performance</strong> — Cluster, Worker Threads, connection pooling, caching, indexes, N+1 queries.<br>\r\n      🏗️ <strong>Architecture</strong> — Repository pattern, DI, graceful shutdown, project structure.<br>\r\n      🧪 <strong>Testing</strong> — Jest, Supertest, mocking database calls, unit vs integration tests.\r\n    </div>\r\n  </div>\r\n</div>\r\n\r\n</main>\r\n\r\n<footer>\r\n  <p>🎉 All 100 Node.js concepts covered deeply — event loop, streams, Express, async, security, scaling & everything in between. Go ace that interview! 🟢</p>\r\n</footer>\r\n\r\n" }} />
    </>
  );
}