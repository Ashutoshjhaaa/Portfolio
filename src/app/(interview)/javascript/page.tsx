'use client';
import Link from 'next/link';

import React, { useEffect, useRef } from 'react';
import Head from 'next/head';

export default function JavascriptInterviewQuestions() {
  const containerRef = useRef<HTMLDivElement>(null);

  useEffect(() => {
        const container = containerRef.current;
    if (!container) return;

    const applyFilters = () => {
      const activePill = container.querySelector('.pill.active') as HTMLElement;
      const activeFilter = activePill ? activePill.dataset.filter : 'all';      
      const searchInput = container.querySelector('#searchInput') as HTMLInputElement;
      const query = searchInput ? searchInput.value.toLowerCase() : '';

      const cards = container.querySelectorAll('.q-card');
      if (cards) {
        cards.forEach((card: any) => {
          const diff = card.dataset.difficulty;
          const qText = card.querySelector('.q-question')?.textContent?.toLowerCase() || '';
          const bText = card.querySelector('.q-body')?.textContent?.toLowerCase() || '';

          const matchesDiff = activeFilter === 'all' || diff === activeFilter;  
          const matchesSearch = !query || qText.includes(query) || bText.includes(query);

          if (matchesDiff && matchesSearch) {
            card.classList.remove('hidden');
          } else {
            card.classList.add('hidden');
          }
        });
      }
    };

    const clickHandler = (e: MouseEvent) => {
      const target = e.target as HTMLElement;
      const header = target.closest('.q-top');
      if (header && container.contains(header)) {
        const card = header.closest('.q-card');
        if (card) {
          card.classList.toggle('open');
        }
        return;
      }
      
      const pill = target.closest('.pill');
      if (pill && container.contains(pill)) {
        const pills = container.querySelectorAll('.pill');
        pills.forEach(b => b.classList.remove('active'));
        pill.classList.add('active');
        applyFilters();
      }
    };

    container.addEventListener('click', clickHandler as EventListener);
    
    const searchInput = container.querySelector('#searchInput');
    if (searchInput) {
      searchInput.addEventListener('input', applyFilters);
    }

    return () => {
      container.removeEventListener('click', clickHandler as EventListener);
      if (searchInput) {
        searchInput.removeEventListener('input', applyFilters);
      }
    };
  }, []);

  return (
    <>
      
      <title>Top 100 JavaScript Interview Questions & Answers</title>
      <meta name="description" content="Master JavaScript with this comprehensive list of 100 interview questions, designed for beginners and advanced developers preparing for technical interviews." />
      <meta property="og:title" content="Top 100 JavaScript Interview Questions & Answers" />
      <meta property="og:description" content="Master JavaScript with this comprehensive list of 100 interview questions, designed for beginners and advanced developers preparing for technical interviews." />
      
      <div style={{ position: 'sticky', top: 0, zIndex: 1000, background: 'var(--bg, #fdfaf5)', padding: '12px 40px', borderBottom: '2px solid var(--border, #e8e0d0)' }}>
        <Link href="/" style={{ color: 'var(--accent, #e8601c)', fontWeight: 800, textDecoration: 'none', display: 'flex', alignItems: 'center', gap: '8px' }}>
          <span>&larr;</span> Back to Portfolio
        </Link>
      </div>
  <style dangerouslySetInnerHTML={{ __html: "\r\n:root {\r\n  --bg: #f0f4ff;\r\n  --card: #ffffff;\r\n  --border: #d0d9f0;\r\n  --text: #0f172a;\r\n  --muted: #64748b;\r\n  --accent: #2563eb;\r\n  --accent2: #059669;\r\n  --accent3: #7c3aed;\r\n  --easy-bg: #dcfce7; --easy-text: #166534;\r\n  --medium-bg: #fef9c3; --medium-text: #854d0e;\r\n  --hard-bg: #fee2e2; --hard-text: #991b1b;\r\n  --tag-bg: #e8edf8;\r\n  --shadow: 0 2px 12px rgba(37,99,235,0.07);\r\n}\r\n*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}\r\nhtml{scroll-behavior:smooth}\r\nbody{background:var(--bg);color:var(--text);font-family:'Plus Jakarta Sans',sans-serif;font-size:15px;line-height:1.7}\r\n\r\nheader{background:linear-gradient(135deg,#1d4ed8 0%,#2563eb 50%,#1e40af 100%);padding:52px 40px 52px;position:relative;overflow:hidden}\r\nheader::before{content:'';position:absolute;inset:0;background:url(\"data:image/svg+xml,%3Csvg width='80' height='80' viewBox='0 0 80 80' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%23ffffff' fill-opacity='0.04'%3E%3Cpath d='M0 0h40v40H0V0zm40 40h40v40H40V40zm0-40h2l-2 2V0zm0 4l4-4h2l-6 6V4zm0 4l8-8h2L40 10V8zm0 4L52 0h2L40 14v-2zm0 4L56 0h2L40 18v-2zm0 4L60 0h2L40 22v-2zm0 4L64 0h2L40 26v-2zm0 4L68 0h2L40 30v-2zm0 4L72 0h2L40 34v-2zm0 4L76 0h2L40 38v-2zm0 4L80 0v2L42 40h-2zm4 0L80 4v2L46 40h-2zm4 0L80 8v2L50 40h-2zm4 0L80 12v2L54 40h-2zm4 0L80 16v2L58 40h-2zm4 0L80 20v2L62 40h-2zm4 0L80 24v2L66 40h-2zm4 0L80 28v2L70 40h-2zm4 0L80 32v2L74 40h-2zm4 0L80 36v2L78 40h-2zm4 0L80 40v-2h-2z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E\")}\r\nheader::after{content:'';position:absolute;bottom:-44px;left:0;right:0;height:88px;background:var(--bg);border-radius:50% 50% 0 0/100% 100% 0 0}\r\n.header-icon{font-size:52px;display:block;margin-bottom:14px;position:relative;z-index:1}\r\nheader h1{font-size:clamp(1.9rem,4.5vw,3.2rem);font-weight:800;color:#fff;line-height:1.1;margin-bottom:10px;position:relative;z-index:1;letter-spacing:-0.5px}\r\nheader p{color:rgba(255,255,255,0.88);font-size:15px;font-weight:500;max-width:580px;position:relative;z-index:1}\r\n.header-stats{display:flex;gap:14px;margin-top:22px;flex-wrap:wrap;position:relative;z-index:1}\r\n.hstat{background:rgba(255,255,255,0.15);border:1px solid rgba(255,255,255,0.25);border-radius:20px;padding:6px 16px;color:#fff;font-weight:700;font-size:13px}\r\n\r\n.controls{position:sticky;top:0;z-index:100;background:rgba(240,244,255,0.96);backdrop-filter:blur(12px);border-bottom:2px solid var(--border);padding:13px 40px;display:flex;gap:8px;flex-wrap:wrap;align-items:center}\r\n.pill{padding:6px 16px;border-radius:999px;border:2px solid var(--border);background:transparent;color:var(--muted);font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;font-weight:700;cursor:pointer;transition:all 0.15s;white-space:nowrap}\r\n.pill:hover{border-color:var(--accent);color:var(--accent)}\r\n.pill.active{background:var(--accent);color:#fff;border-color:var(--accent)}\r\n.pill.easy.active{background:var(--easy-text);border-color:var(--easy-text)}\r\n.pill.medium.active{background:var(--medium-text);border-color:var(--medium-text)}\r\n.pill.hard.active{background:var(--hard-text);border-color:var(--hard-text)}\r\n.search-wrap{margin-left:auto;position:relative}\r\n.search-wrap input{background:var(--card);border:2px solid var(--border);border-radius:999px;padding:7px 16px 7px 36px;font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;font-weight:500;color:var(--text);width:220px;outline:none;transition:border-color 0.2s}\r\n.search-wrap input:focus{border-color:var(--accent)}\r\n.search-wrap input::placeholder{color:var(--muted)}\r\n.search-wrap::before{content:'🔍';position:absolute;left:12px;top:50%;transform:translateY(-50%);font-size:13px}\r\n\r\nmain{max-width:860px;margin:0 auto;padding:40px 32px 80px}\r\n\r\n.section-head{display:flex;align-items:center;gap:12px;margin:44px 0 18px}\r\n.section-icon{font-size:24px;width:46px;height:46px;display:grid;place-items:center;background:var(--tag-bg);border:1.5px solid var(--border);border-radius:12px;flex-shrink:0}\r\n.section-title{font-size:1.15rem;font-weight:800;color:var(--text);letter-spacing:-0.3px}\r\n.section-line{flex:1;height:2px;background:var(--border);border-radius:2px}\r\n\r\n.q-card{background:var(--card);border:2px solid var(--border);border-radius:16px;margin-bottom:10px;overflow:hidden;transition:border-color 0.2s,box-shadow 0.2s;box-shadow:var(--shadow)}\r\n.q-card:hover{border-color:#a0b4e8;box-shadow:0 4px 20px rgba(37,99,235,0.1)}\r\n.q-card.open{border-color:var(--accent);box-shadow:0 4px 24px rgba(37,99,235,0.15)}\r\n.q-card.hidden{display:none}\r\n\r\n.q-top{display:flex;align-items:flex-start;gap:12px;padding:15px 20px;cursor:pointer;user-select:none}\r\n.q-num{font-family:'JetBrains Mono',monospace;font-size:11px;color:var(--muted);min-width:28px;padding-top:3px;flex-shrink:0}\r\n.q-question{flex:1;font-size:14.5px;font-weight:700;color:var(--text);line-height:1.45}\r\n.q-card.open .q-question{color:var(--accent)}\r\n.badge{font-size:11px;font-weight:800;padding:3px 9px;border-radius:999px;flex-shrink:0;margin-top:2px;text-transform:uppercase;letter-spacing:0.3px}\r\n.b-easy{background:var(--easy-bg);color:var(--easy-text)}\r\n.b-medium{background:var(--medium-bg);color:var(--medium-text)}\r\n.b-hard{background:var(--hard-bg);color:var(--hard-text)}\r\n.chevron{color:var(--muted);font-size:16px;flex-shrink:0;transition:transform 0.25s;margin-top:3px}\r\n.q-card.open .chevron{transform:rotate(180deg);color:var(--accent)}\r\n\r\n.q-body{display:none;padding:0 20px 20px 60px;border-top:2px solid var(--border)}\r\n.q-card.open .q-body{display:block}\r\n\r\n.simple-box{background:#eff6ff;border-left:4px solid var(--accent);border-radius:0 10px 10px 0;padding:12px 16px;margin:14px 0 10px;font-size:14px;color:#1e3a6e}\r\n.simple-box .lbl{font-size:10px;text-transform:uppercase;letter-spacing:1.2px;color:var(--accent);font-weight:800;margin-bottom:5px;display:block}\r\n.example-box{background:#f0fdf4;border-left:4px solid var(--accent2);border-radius:0 10px 10px 0;padding:12px 16px;margin:10px 0;font-size:14px;color:#052e16}\r\n.example-box .lbl{font-size:10px;text-transform:uppercase;letter-spacing:1.2px;color:var(--accent2);font-weight:800;margin-bottom:5px;display:block}\r\n.note-box{background:#f5f3ff;border-left:4px solid var(--accent3);border-radius:0 10px 10px 0;padding:12px 16px;margin:10px 0;font-size:14px;color:#2e1065}\r\n.note-box .lbl{font-size:10px;text-transform:uppercase;letter-spacing:1.2px;color:var(--accent3);font-weight:800;margin-bottom:5px;display:block}\r\n\r\ncode{font-family:'JetBrains Mono',monospace;background:#e8edf8;border-radius:5px;padding:1px 6px;font-size:12.5px;color:#1d4ed8}\r\npre{background:#0f172a;border-radius:10px;padding:14px 16px;overflow-x:auto;margin:12px 0;font-family:'JetBrains Mono',monospace;font-size:12px;color:#e2e8f0;line-height:1.75;border:1.5px solid #1e293b}\r\n\r\nfooter{text-align:center;padding:32px;color:var(--muted);font-size:13px;font-weight:600;border-top:2px solid var(--border)}\r\n\r\n@media(max-width:640px){\r\n  header,.controls,main{padding-left:16px;padding-right:16px}\r\n  .search-wrap{margin-left:0;width:100%}\r\n  .search-wrap input{width:100%}\r\n  .q-body{padding-left:20px}\r\n}\r\n" }} />
      <div ref={containerRef} dangerouslySetInnerHTML={{ __html: "\r\n\r\n\r\n\r\n\r\n<header>\r\n  <span class=\"header-icon\">⚡</span>\r\n  <h1>100 JavaScript Interview Questions</h1>\r\n  <p>Every JS concept explained in simple English — variables, closures, async/await, prototypes, ES6+, event loop, DOM, design patterns & modern JavaScript.</p>\r\n  <div class=\"header-stats\">\r\n    <span class=\"hstat\">100 Questions</span>\r\n    <span class=\"hstat\">12 Deep Topics</span>\r\n    <span class=\"hstat\">Code Examples ✓</span>\r\n    <span class=\"hstat\">Simple English ✓</span>\r\n    <span class=\"hstat\">ES2024 Ready ✓</span>\r\n  </div>\r\n</header>\r\n\r\n<div class=\"controls\">\r\n  <button class=\"pill active\" data-filter=\"all\">All</button>\r\n  <button class=\"pill easy\" data-filter=\"easy\">🟢 Easy</button>\r\n  <button class=\"pill medium\" data-filter=\"medium\">🟡 Medium</button>\r\n  <button class=\"pill hard\" data-filter=\"hard\">🔴 Hard</button>\r\n  <div class=\"search-wrap\"><input type=\"text\" id=\"searchInput\" placeholder=\"Search questions…\"></div>\r\n</div>\r\n\r\n<main id=\"main\">\r\n\r\n<!-- ══════════ SECTION 1: JS BASICS ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🧱</span><h2 class=\"section-title\">JavaScript Basics</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">01</span><span class=\"q-question\">What is JavaScript and what is it used for?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>JavaScript is a programming language that makes web pages interactive and dynamic. It runs in the browser and controls behavior — like showing a popup, validating a form, fetching data, or animating elements. It also runs on servers via Node.js.</div>\r\n<div class=\"example-box\"><span class=\"lbl\">🧩 HTML vs CSS vs JS</span>HTML = structure (the skeleton). CSS = style (the appearance). JavaScript = behavior (the brain). Without JS, websites are just static pages. With JS, they become apps that react to users.</div>\r\n<pre>// JavaScript runs in the browser and can:\r\ndocument.getElementById('btn').addEventListener('click', () => {\r\n  alert('You clicked the button!');\r\n});\r\n\r\n// Or on the server with Node.js:\r\nconst http = require('http');\r\nhttp.createServer((req, res) => res.end('Hello World')).listen(3000);</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">02</span><span class=\"q-question\">What is the difference between var, let, and const?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>var</code> is old — function-scoped, hoisted, can be re-declared (avoid it). <code>let</code> is block-scoped, can be reassigned. <code>const</code> is block-scoped, cannot be reassigned. Use <code>const</code> by default, <code>let</code> when you need to reassign, never <code>var</code>.</div>\r\n<pre>// var — function-scoped, hoisted, old (avoid)\r\nvar x = 1;\r\nvar x = 2;     // ✅ can re-declare — confusing!\r\nif (true) { var y = 5; }\r\nconsole.log(y); // 5 — leaks out of the if block!\r\n\r\n// let — block-scoped, can reassign\r\nlet count = 0;\r\ncount = 1;     // ✅ reassign is fine\r\n// let count = 2; // ❌ cannot re-declare in same scope\r\n\r\n// const — block-scoped, cannot reassign\r\nconst PI = 3.14;\r\n// PI = 3;      // ❌ TypeError — cannot reassign const\r\n\r\n// const with objects/arrays — the REFERENCE is constant, not the value\r\nconst arr = [1, 2, 3];\r\narr.push(4);   // ✅ you CAN mutate the contents\r\n// arr = [];   // ❌ cannot reassign the variable itself</pre>\r\n<div class=\"note-box\"><span class=\"lbl\">⭐ Best Practice</span>Always use <code>const</code> unless you know the value will change, then use <code>let</code>. Never use <code>var</code> in modern JavaScript. This makes your code more predictable and easier to understand.</div>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">03</span><span class=\"q-question\">What are the JavaScript data types?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>JavaScript has 8 data types. 7 are <strong>primitive</strong> (stored by value): <code>string</code>, <code>number</code>, <code>boolean</code>, <code>undefined</code>, <code>null</code>, <code>symbol</code>, <code>bigint</code>. 1 is <strong>non-primitive</strong> (stored by reference): <code>object</code> (includes arrays and functions).</div>\r\n<pre>// Primitives — stored by value\r\nlet str  = \"hello\";          // string\r\nlet num  = 42;               // number (integers + decimals)\r\nlet bool = true;             // boolean\r\nlet undef;                   // undefined (declared but no value)\r\nlet empty = null;            // null (intentionally empty)\r\nlet sym  = Symbol('id');     // symbol (unique identifier)\r\nlet big  = 9007199254740991n;// bigint (very large integers)\r\n\r\n// Non-primitive — stored by reference\r\nlet obj  = { name: \"Alice\" };// object\r\nlet arr  = [1, 2, 3];        // array (type of object)\r\nlet fn   = () => {};         // function (type of object)\r\n\r\n// Check type:\r\ntypeof \"hello\"   // \"string\"\r\ntypeof 42        // \"number\"\r\ntypeof true      // \"boolean\"\r\ntypeof undefined // \"undefined\"\r\ntypeof null      // \"object\" ← famous JS bug!\r\ntypeof []        // \"object\"\r\ntypeof {}        // \"object\"\r\ntypeof function(){}  // \"function\"</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">04</span><span class=\"q-question\">What is the difference between == and ===?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>==</code> is loose equality — it converts types before comparing (type coercion). <code>===</code> is strict equality — it checks both value AND type, no conversion. Always use <code>===</code> in real code to avoid confusing bugs.</div>\r\n<pre>// == (loose) — converts types first, then compares\r\n0 == false      // true  (false → 0)\r\n\"\" == false     // true  (\"\" → 0, false → 0)\r\nnull == undefined // true  (special rule)\r\n1 == \"1\"        // true  (\"1\" → 1)\r\n[] == false     // true  (complex conversion)\r\n\r\n// === (strict) — no conversion, must match type AND value\r\n0 === false     // false (different types: number vs boolean)\r\n\"\" === false    // false (different types)\r\nnull === undefined // false (different types)\r\n1 === \"1\"       // false (different types: number vs string)\r\n1 === 1         // true  ✅\r\n\r\n// Real-world consequence:\r\nif (userInput == 0) {  // ❌ triggers for 0, \"\", false, null, []\r\nif (userInput === 0) { // ✅ only triggers for the number 0</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">05</span><span class=\"q-question\">What is type coercion in JavaScript?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Type coercion is when JavaScript automatically converts one data type to another. It happens with <code>==</code>, arithmetic operators, and string concatenation. It often produces surprising results — understanding it prevents bugs.</div>\r\n<pre>// String + Number = String concatenation (+ favors strings)\r\n\"5\" + 3       // \"53\"   (3 is converted to string)\r\n\"5\" - 3       // 2      (- forces number conversion)\r\n\"5\" * \"2\"     // 10     (both converted to numbers)\r\n\r\n// Truthy / Falsy coercion (in if conditions)\r\n// Falsy values (convert to false):\r\nif (0)         { }  // false\r\nif (\"\")        { }  // false\r\nif (null)      { }  // false\r\nif (undefined) { }  // false\r\nif (NaN)       { }  // false\r\nif (false)     { }  // false\r\n// Everything else is truthy:\r\nif ([])        { }  // true! (empty array is truthy)\r\nif ({})        { }  // true! (empty object is truthy)\r\nif (\"0\")       { }  // true! (non-empty string is truthy)\r\n\r\n// Explicit conversion (safe)\r\nNumber(\"42\")   // 42\r\nString(42)     // \"42\"\r\nBoolean(1)     // true</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">06</span><span class=\"q-question\">What is null vs undefined in JavaScript?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>undefined</code> means a variable has been declared but no value was assigned — JS assigned it automatically. <code>null</code> means you, the developer, intentionally set something to \"empty\" or \"nothing\". Both represent absence of value but for different reasons.</div>\r\n<pre>let name;         // undefined — declared but not assigned\r\nconsole.log(name); // undefined\r\n\r\nlet user = null;  // null — intentionally set to \"no user\"\r\n\r\n// Function with no return value:\r\nfunction doSomething() { /* no return */ }\r\nconst result = doSomething(); // result is undefined\r\n\r\n// Common pattern — check for both with ==\r\nif (value == null) { }   // catches BOTH null and undefined\r\nif (value === null) { }  // catches ONLY null\r\nif (value === undefined) { } // catches ONLY undefined\r\n\r\n// Nullish coalescing ?? — use when you want a fallback for null/undefined:\r\nconst name = user?.name ?? \"Guest\";\r\n// Only triggers for null/undefined, NOT for 0 or \"\"</pre>\r\n<div class=\"example-box\"><span class=\"lbl\">🧩 Analogy</span>Imagine a parking spot. <code>undefined</code> = the spot exists but nobody told you if there's a car (you didn't check). <code>null</code> = you checked and deliberately confirmed the spot is empty.</div>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">07</span><span class=\"q-question\">What is NaN and how do you check for it?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>NaN</code> stands for \"Not a Number\" — it's the result of an invalid math operation. The weirdest thing about NaN: it's the only value in JavaScript that is NOT equal to itself. Use <code>Number.isNaN()</code> to check for it.</div>\r\n<pre>// NaN appears when math fails\r\nparseInt(\"hello\")  // NaN\r\nundefined * 2      // NaN\r\n0 / 0              // NaN\r\nMath.sqrt(-1)      // NaN\r\n\r\n// The weird part:\r\nNaN === NaN        // false (!!) — NaN is not equal to itself\r\nNaN == NaN         // also false\r\n\r\n// Wrong way to check:\r\nif (value === NaN) { } // ❌ never works!\r\n\r\n// Right ways:\r\nNumber.isNaN(NaN)      // ✅ true (only for actual NaN)\r\nNumber.isNaN(\"hello\")  // false (string, not NaN)\r\n\r\nisNaN(\"hello\")         // true — old function, converts first (confusing)\r\nisNaN(NaN)             // true\r\n\r\n// Or check by exploiting the self-inequality:\r\nfunction isItNaN(val) { return val !== val; }  // clever but use isNaN</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">08</span><span class=\"q-question\">What is hoisting in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Hoisting is JavaScript's behavior of moving variable and function declarations to the top of their scope before code runs. <code>var</code> declarations are hoisted (but initialized as <code>undefined</code>). Function declarations are fully hoisted. <code>let</code> and <code>const</code> are hoisted but NOT initialized (Temporal Dead Zone).</div>\r\n<pre>// Function declarations are FULLY hoisted\r\nsayHello(); // ✅ works! \"Hello\"\r\nfunction sayHello() { console.log(\"Hello\"); }\r\n\r\n// var is hoisted as undefined\r\nconsole.log(x); // undefined (not error — hoisted)\r\nvar x = 5;\r\nconsole.log(x); // 5\r\n\r\n// let/const — hoisted but in \"Temporal Dead Zone\"\r\nconsole.log(y); // ❌ ReferenceError — TDZ!\r\nlet y = 10;\r\n\r\n// Function EXPRESSIONS are not hoisted\r\ngreet(); // ❌ TypeError — greet is undefined (var) or ReferenceError (let)\r\nvar greet = function() { console.log(\"Hi\"); };</pre>\r\n<div class=\"note-box\"><span class=\"lbl\">💡 Temporal Dead Zone</span>The TDZ is the period between when a let/const variable is hoisted and when it's initialized. During this time, accessing the variable throws a ReferenceError. This is actually a feature — it prevents using variables before they're defined.</div>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 2: FUNCTIONS ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">⚙️</span><h2 class=\"section-title\">Functions & Scope</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">09</span><span class=\"q-question\">What are the different ways to declare a function?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>There are 4 main ways: Function Declaration (hoisted), Function Expression (not hoisted), Arrow Function (no own <code>this</code>), and Method shorthand (in objects). Each has different behavior with hoisting and <code>this</code>.</div>\r\n<pre>// 1. Function Declaration — fully hoisted\r\nfunction greet(name) { return `Hello, ${name}!`; }\r\n\r\n// 2. Function Expression — not hoisted\r\nconst greet = function(name) { return `Hello, ${name}!`; };\r\n\r\n// 3. Arrow Function — concise, no own 'this'\r\nconst greet = (name) => `Hello, ${name}!`;\r\nconst greet = name  =>  `Hello, ${name}!`; // single param: no parens\r\nconst add   = (a, b) => a + b;             // single expr: no braces\r\nconst square = n => ({ value: n * n });    // return obj: wrap in ()\r\n\r\n// 4. Method shorthand (inside objects)\r\nconst obj = {\r\n  greet(name) { return `Hello, ${name}!`; }\r\n};\r\n\r\n// Named function expression (useful for recursion + debugging)\r\nconst factorial = function fact(n) {\r\n  return n <= 1 ? 1 : n * fact(n - 1); // can call itself by name\r\n};</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">10</span><span class=\"q-question\">What is scope in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Scope determines where a variable is accessible. JavaScript has 3 types: <strong>Global</strong> (accessible everywhere), <strong>Function/Local</strong> (inside a function only), and <strong>Block</strong> (inside {} with let/const). Inner scopes can access outer, but not vice versa.</div>\r\n<pre>// Global scope — accessible everywhere\r\nconst globalVar = \"I'm global\";\r\n\r\nfunction outer() {\r\n  // Function scope — accessible inside outer()\r\n  const outerVar = \"I'm in outer\";\r\n\r\n  function inner() {\r\n    // Function scope — accessible inside inner()\r\n    const innerVar = \"I'm in inner\";\r\n    console.log(globalVar); // ✅ can access global\r\n    console.log(outerVar);  // ✅ can access outer (closure!)\r\n    console.log(innerVar);  // ✅ can access own\r\n  }\r\n\r\n  console.log(globalVar); // ✅\r\n  // console.log(innerVar); // ❌ cannot access inner's vars\r\n}\r\n\r\n// Block scope (let/const)\r\nif (true) {\r\n  const blockVar = \"block scoped\";\r\n  var funcVar = \"function scoped\";\r\n}\r\n// console.log(blockVar); // ❌ ReferenceError\r\nconsole.log(funcVar);     // ✅ var leaks out of blocks</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">11</span><span class=\"q-question\">What is a closure and why is it important?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A closure is when an inner function remembers and can access variables from its outer function even after the outer function has finished running. The function \"closes over\" those variables — they're captured in memory.</div>\r\n<pre>// Classic closure example\r\nfunction makeCounter() {\r\n  let count = 0;           // outer variable\r\n\r\n  return function() {      // inner function\r\n    count++;               // accesses outer variable\r\n    return count;\r\n  };\r\n}\r\n\r\nconst counter = makeCounter(); // outer function has returned!\r\nconsole.log(counter()); // 1\r\nconsole.log(counter()); // 2\r\nconsole.log(counter()); // 3\r\n// count is still alive because the inner function holds a reference to it\r\n\r\n// Practical use — private data (data hiding)\r\nfunction createUser(name) {\r\n  let role = 'user';  // private — can't access directly\r\n\r\n  return {\r\n    getName: () => name,\r\n    getRole: () => role,\r\n    promote: () => { role = 'admin'; }\r\n  };\r\n}\r\nconst user = createUser('Alice');\r\nuser.promote();\r\nconsole.log(user.getRole()); // 'admin'\r\n// user.role === undefined  — truly private!</pre>\r\n<div class=\"example-box\"><span class=\"lbl\">🧩 Analogy</span>A closure is like a backpack. When an inner function is created, it packs up (closes over) the variables it needs from its surroundings. Even when the function travels to a different place and the outer scope is gone, it still has its backpack with all those variables.</div>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">12</span><span class=\"q-question\">What is the classic closure bug in loops and how do you fix it?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>When using <code>var</code> in a loop with async callbacks, all callbacks share the same variable — by the time they run, the loop has finished and the variable holds the final value. Fix by using <code>let</code> (block-scoped) or an IIFE.</div>\r\n<pre>// ❌ The classic bug with var\r\nfor (var i = 0; i < 3; i++) {\r\n  setTimeout(() => console.log(i), 100);\r\n}\r\n// Prints: 3 3 3 — all share the same 'i', which is 3 after loop\r\n\r\n// ✅ Fix 1: Use let (creates a new 'i' for each iteration)\r\nfor (let i = 0; i < 3; i++) {\r\n  setTimeout(() => console.log(i), 100);\r\n}\r\n// Prints: 0 1 2 ✓\r\n\r\n// ✅ Fix 2: IIFE captures the current value\r\nfor (var i = 0; i < 3; i++) {\r\n  ((j) => {  // j is a new variable for each iteration\r\n    setTimeout(() => console.log(j), 100);\r\n  })(i);     // immediately invoked with current i\r\n}\r\n// Prints: 0 1 2 ✓</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">13</span><span class=\"q-question\">What is an IIFE (Immediately Invoked Function Expression)?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>An IIFE is a function that is defined and called at the same time. It creates a private scope — variables inside don't pollute the global scope. Before ES6 modules, this was the main pattern for encapsulation.</div>\r\n<pre>// IIFE syntax — wrap function in (), then call it with ()\r\n(function() {\r\n  const privateVar = \"nobody can access me from outside\";\r\n  console.log(\"IIFE ran!\");\r\n})();\r\n\r\n// Arrow function IIFE\r\n(() => {\r\n  console.log(\"Arrow IIFE\");\r\n})();\r\n\r\n// IIFE with parameters\r\n(function(name) {\r\n  console.log(`Hello, ${name}!`);\r\n})(\"Alice\");\r\n\r\n// Practical use: avoid polluting globals in scripts\r\n(function() {\r\n  const config = { apiKey: \"secret\" };  // stays private\r\n  // ... all your code here\r\n})();\r\n\r\n// Modern alternative: just use a block with let/const\r\n{\r\n  const privateVar = \"block scoped\";\r\n}\r\n// or ES6 modules — each file has its own scope automatically</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">14</span><span class=\"q-question\">What is the difference between arguments object and rest parameters?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The <code>arguments</code> object is an old array-like object available in regular functions that contains all passed arguments. <code>...rest</code> parameters are modern, real arrays that collect remaining arguments. Always prefer rest parameters.</div>\r\n<pre>// Old way — arguments object\r\nfunction sum() {\r\n  let total = 0;\r\n  for (let i = 0; i < arguments.length; i++) {\r\n    total += arguments[i];\r\n  }\r\n  return total;\r\n}\r\n// arguments is NOT available in arrow functions!\r\n// arguments is array-LIKE but not a real array (no .map, .filter, etc.)\r\n\r\n// Modern way — rest parameters\r\nfunction sum(...numbers) {  // real array!\r\n  return numbers.reduce((total, n) => total + n, 0);\r\n}\r\nsum(1, 2, 3, 4, 5); // 15\r\n\r\n// Rest collects the REMAINING arguments\r\nfunction first(a, b, ...rest) {\r\n  console.log(a);    // 1\r\n  console.log(b);    // 2\r\n  console.log(rest); // [3, 4, 5]\r\n}\r\nfirst(1, 2, 3, 4, 5);</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">15</span><span class=\"q-question\">What are default parameters in functions?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Default parameters let you specify a fallback value for a function argument if the caller doesn't provide one (or passes <code>undefined</code>). Introduced in ES6 — much cleaner than the old <code>param = param || default</code> pattern.</div>\r\n<pre>// ES6 default parameters\r\nfunction greet(name = \"World\", greeting = \"Hello\") {\r\n  return `${greeting}, ${name}!`;\r\n}\r\ngreet();              // \"Hello, World!\"\r\ngreet(\"Alice\");       // \"Hello, Alice!\"\r\ngreet(\"Bob\", \"Hi\");  // \"Hi, Bob!\"\r\ngreet(undefined, \"Hey\"); // \"Hey, World!\" — undefined triggers default\r\n\r\n// Old way (error-prone)\r\nfunction greet(name, greeting) {\r\n  name = name || \"World\";     // ❌ fails if name is 0 or \"\"\r\n  greeting = greeting !== undefined ? greeting : \"Hello\"; // verbose\r\n}\r\n\r\n// Default can be an expression or function call\r\nfunction createId(id = Math.random().toString(36).slice(2)) {\r\n  return id;\r\n}\r\n\r\n// Default from previous parameter\r\nfunction makeArray(value, length = value.length) {\r\n  return Array(length).fill(value);\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">16</span><span class=\"q-question\">What is the 'this' keyword and how does it work?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>this</code> refers to the object that is executing the current function. Its value depends on HOW the function is called, not where it's written. This is one of the most confusing parts of JavaScript.</div>\r\n<pre>// 1. Method call — this = the object before the dot\r\nconst user = {\r\n  name: \"Alice\",\r\n  greet() { console.log(this.name); }  // this = user\r\n};\r\nuser.greet(); // \"Alice\"\r\n\r\n// 2. Regular function — this = global (window) or undefined (strict mode)\r\nfunction showThis() { console.log(this); }\r\nshowThis(); // window (or undefined in strict mode)\r\n\r\n// 3. Arrow function — no own 'this', inherits from outer scope\r\nconst obj = {\r\n  name: \"Alice\",\r\n  greet: () => { console.log(this.name); } // this = outer (window!)\r\n};\r\nobj.greet(); // undefined — arrow doesn't have own 'this'!\r\n\r\n// 4. new — this = newly created object\r\nfunction Person(name) { this.name = name; }\r\nconst p = new Person(\"Bob\"); // this.name = \"Bob\"\r\n\r\n// 5. call/apply/bind — explicitly set this\r\nfunction greet() { return `Hi ${this.name}`; }\r\ngreet.call({ name: \"Carol\" });  // \"Hi Carol\"\r\ngreet.apply({ name: \"Dave\" }); // \"Hi Dave\"\r\nconst boundGreet = greet.bind({ name: \"Eve\" });\r\nboundGreet(); // \"Hi Eve\"</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">17</span><span class=\"q-question\">What is the difference between call(), apply(), and bind()?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>All three let you set <code>this</code> explicitly. <code>call()</code> invokes immediately with arguments listed. <code>apply()</code> invokes immediately with arguments as an array. <code>bind()</code> returns a new function with <code>this</code> permanently fixed — doesn't invoke immediately.</div>\r\n<pre>function introduce(greeting, punctuation) {\r\n  return `${greeting}, I'm ${this.name}${punctuation}`;\r\n}\r\nconst person = { name: \"Alice\" };\r\n\r\n// call() — args listed individually\r\nintroduce.call(person, \"Hello\", \"!\");   // \"Hello, I'm Alice!\"\r\n\r\n// apply() — args as array (useful when you have an array already)\r\nintroduce.apply(person, [\"Hi\", \"?\"]);   // \"Hi, I'm Alice?\"\r\n\r\n// bind() — returns NEW function, doesn't call it yet\r\nconst aliceIntro = introduce.bind(person, \"Hey\");\r\naliceIntro(\"...\");  // \"Hey, I'm Alice...\"\r\naliceIntro(\"!!!\");  // \"Hey, I'm Alice!!!\"\r\n\r\n// Practical use of bind:\r\nclass Timer {\r\n  constructor() { this.count = 0; }\r\n  start() {\r\n    // Without bind, 'this' inside callback = window\r\n    setInterval(this.tick.bind(this), 1000);\r\n  }\r\n  tick() { this.count++; console.log(this.count); }\r\n}</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 3: ARRAYS & OBJECTS ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">📦</span><h2 class=\"section-title\">Arrays & Objects</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">18</span><span class=\"q-question\">What are the most important array methods in JavaScript?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The most important array methods are: <code>map()</code> (transform each item), <code>filter()</code> (keep some items), <code>reduce()</code> (combine all into one value), <code>forEach()</code> (loop), <code>find()</code> (get first match), <code>some()</code>/<code>every()</code> (test conditions).</div>\r\n<pre>const nums = [1, 2, 3, 4, 5];\r\n\r\n// map — transform each element, returns NEW array\r\nnums.map(n => n * 2);          // [2, 4, 6, 8, 10]\r\n\r\n// filter — keep elements that pass the test\r\nnums.filter(n => n % 2 === 0); // [2, 4]  (evens only)\r\n\r\n// reduce — combine all into one value\r\nnums.reduce((sum, n) => sum + n, 0); // 15 (total sum)\r\n\r\n// find — first element that matches\r\nnums.find(n => n > 3);         // 4 (first match)\r\nnums.findIndex(n => n > 3);    // 3 (index of first match)\r\n\r\n// some — true if ANY element passes\r\nnums.some(n => n > 4);         // true (5 > 4)\r\n\r\n// every — true if ALL elements pass\r\nnums.every(n => n > 0);        // true (all positive)\r\n\r\n// includes — true if value exists\r\nnums.includes(3);              // true\r\n\r\n// flat + flatMap\r\n[[1,2],[3,4]].flat();           // [1,2,3,4]\r\nnums.flatMap(n => [n, n * 2]); // [1,2, 2,4, 3,6, ...]</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">19</span><span class=\"q-question\">What is the difference between map() and forEach()?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>map()</code> returns a NEW array with transformed values — use it when you need the results. <code>forEach()</code> returns <code>undefined</code> — use it only for side effects (logging, DOM updates) when you don't need the results.</div>\r\n<pre>const numbers = [1, 2, 3];\r\n\r\n// map — creates a new array, use the result\r\nconst doubled = numbers.map(n => n * 2);\r\nconsole.log(doubled);  // [2, 4, 6]\r\nconsole.log(numbers);  // [1, 2, 3] — original unchanged\r\n\r\n// forEach — returns undefined, use for side effects only\r\nconst result = numbers.forEach(n => n * 2); // returns undefined!\r\nconsole.log(result); // undefined\r\n\r\nnumbers.forEach(n => console.log(n));  // just logs: 1, 2, 3\r\n\r\n// Rule: need the transformed array? → map\r\n//       just want to do something with each element? → forEach\r\n\r\n// map is chainable, forEach is not:\r\nconst result2 = numbers\r\n  .map(n => n * 2)      // [2, 4, 6]\r\n  .filter(n => n > 3);  // [4, 6]\r\n// numbers.forEach(...).filter(...) — ❌ cannot chain</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">20</span><span class=\"q-question\">What is the spread operator and how do you use it?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The spread operator <code>...</code> expands an array or object into individual elements. It's perfect for copying arrays/objects, merging them, or passing array items as function arguments.</div>\r\n<pre>// Spread with arrays\r\nconst a = [1, 2, 3];\r\nconst b = [4, 5, 6];\r\n\r\nconst combined = [...a, ...b];  // [1, 2, 3, 4, 5, 6]\r\nconst copy = [...a];            // [1, 2, 3] — shallow copy\r\nconst withExtra = [...a, 7, 8]; // [1, 2, 3, 7, 8]\r\n\r\n// Spread with objects\r\nconst user = { name: \"Alice\", age: 25 };\r\nconst updatedUser = { ...user, age: 26, role: \"admin\" };\r\n// { name: \"Alice\", age: 26, role: \"admin\" }\r\n\r\n// Spread in function calls\r\nfunction sum(a, b, c) { return a + b + c; }\r\nconst nums = [1, 2, 3];\r\nsum(...nums);  // same as sum(1, 2, 3)\r\n\r\n// Practical: add item to array without mutation\r\nconst addItem = (arr, item) => [...arr, item];\r\n// Practical: update object without mutation\r\nconst update = (obj, key, val) => ({ ...obj, [key]: val });</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">21</span><span class=\"q-question\">What is destructuring in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Destructuring lets you unpack values from arrays or properties from objects into distinct variables in one line. Makes code cleaner and more readable — especially with function parameters.</div>\r\n<pre>// Array destructuring\r\nconst [first, second, ...rest] = [1, 2, 3, 4, 5];\r\n// first = 1, second = 2, rest = [3, 4, 5]\r\n\r\n// Skip elements\r\nconst [,, third] = [10, 20, 30];  // third = 30\r\n\r\n// Default values\r\nconst [a = 0, b = 0] = [5];  // a = 5, b = 0\r\n\r\n// Object destructuring\r\nconst user = { name: \"Alice\", age: 25, role: \"admin\" };\r\nconst { name, age } = user;\r\n// name = \"Alice\", age = 25\r\n\r\n// Rename while destructuring\r\nconst { name: userName, role: userRole } = user;\r\n// userName = \"Alice\", userRole = \"admin\"\r\n\r\n// Default values in objects\r\nconst { name, score = 100 } = { name: \"Bob\" };\r\n// score = 100 (default)\r\n\r\n// In function parameters (very common in React!)\r\nfunction greet({ name, age = 0 }) {\r\n  return `${name} is ${age} years old`;\r\n}\r\ngreet(user); // \"Alice is 25 years old\"</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">22</span><span class=\"q-question\">What is shallow copy vs deep copy in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A <strong>shallow copy</strong> copies the top-level properties only — nested objects/arrays still share the same reference. A <strong>deep copy</strong> recursively copies everything — fully independent. Spread and Object.assign() make shallow copies.</div>\r\n<pre>// Shallow copy — nested objects still shared\r\nconst original = { name: \"Alice\", address: { city: \"NYC\" } };\r\nconst shallow = { ...original };\r\n\r\nshallow.name = \"Bob\";              // ✅ doesn't affect original\r\nshallow.address.city = \"LA\";       // ❌ MUTATES original.address.city too!\r\n// original.address.city === \"LA\" — shared reference!\r\n\r\n// Deep copy options:\r\n\r\n// 1. JSON method (simple but has limits)\r\nconst deep1 = JSON.parse(JSON.stringify(original));\r\n// ❌ Loses: undefined, functions, Dates, RegExp, Map/Set, circular refs\r\n\r\n// 2. structuredClone (modern, best native option)\r\nconst deep2 = structuredClone(original);  // ✅ ES2022\r\n// Handles: nested objects, arrays, Dates, Map, Set, circular refs\r\n// ❌ Does NOT copy functions\r\n\r\n// 3. Lodash cloneDeep (handles everything)\r\nimport { cloneDeep } from 'lodash';\r\nconst deep3 = cloneDeep(original); // ✅ handles functions too</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">23</span><span class=\"q-question\">What are the methods to iterate over objects?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>You can't use array methods on plain objects, but there are several ways to loop over them. The most useful are <code>Object.keys()</code>, <code>Object.values()</code>, and <code>Object.entries()</code> — they return arrays you can work with.</div>\r\n<pre>const user = { name: \"Alice\", age: 25, role: \"admin\" };\r\n\r\n// Object.keys() — array of property names\r\nObject.keys(user);    // [\"name\", \"age\", \"role\"]\r\nObject.keys(user).forEach(key => console.log(key, user[key]));\r\n\r\n// Object.values() — array of values\r\nObject.values(user);  // [\"Alice\", 25, \"admin\"]\r\n\r\n// Object.entries() — array of [key, value] pairs (most useful!)\r\nObject.entries(user).forEach(([key, val]) => {\r\n  console.log(`${key}: ${val}`);\r\n});\r\n\r\n// for...in loop (also works, but includes inherited props)\r\nfor (const key in user) {\r\n  if (user.hasOwnProperty(key)) {  // filter inherited properties\r\n    console.log(key, user[key]);\r\n  }\r\n}\r\n\r\n// Convert object to array of entries, transform, convert back\r\nconst upperUser = Object.fromEntries(\r\n  Object.entries(user).map(([k, v]) => [k, String(v).toUpperCase()])\r\n);</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 4: ES6+ FEATURES ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🚀</span><h2 class=\"section-title\">ES6+ Modern JavaScript</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">24</span><span class=\"q-question\">What are template literals?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Template literals use backticks (`) instead of quotes and allow you to embed expressions with <code>${}</code> and write multi-line strings easily. Much cleaner than string concatenation.</div>\r\n<pre>const name = \"Alice\";\r\nconst age = 25;\r\n\r\n// Old way — string concatenation (messy)\r\nconst old = \"Hello, \" + name + \"! You are \" + age + \" years old.\";\r\n\r\n// Template literal — clean and readable\r\nconst modern = `Hello, ${name}! You are ${age} years old.`;\r\n\r\n// Multi-line strings\r\nconst html = `\r\n  &lt;div class=\"card\"&gt;\r\n    &lt;h2&gt;${name}&lt;/h2&gt;\r\n    &lt;p&gt;Age: ${age}&lt;/p&gt;\r\n  &lt;/div&gt;\r\n`;\r\n\r\n// Expressions inside ${}\r\nconst price = 9.99;\r\nconsole.log(`Total: $${(price * 1.1).toFixed(2)}`); // Total: $10.99\r\n\r\n// Tagged template literals (advanced)\r\nfunction highlight(strings, ...values) {\r\n  return strings.reduce((acc, str, i) =>\r\n    `${acc}${str}${values[i] ? `&lt;b&gt;${values[i]}&lt;/b&gt;` : ''}`, '');\r\n}\r\nhighlight`Hello, ${name}!`; // \"Hello, &lt;b&gt;Alice&lt;/b&gt;!\"</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">25</span><span class=\"q-question\">What are optional chaining (?.) and nullish coalescing (??)?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>?.</code> (optional chaining) safely accesses nested properties — if any part is <code>null</code>/<code>undefined</code>, it returns <code>undefined</code> instead of throwing an error. <code>??</code> (nullish coalescing) provides a fallback only for <code>null</code>/<code>undefined</code>, not for <code>0</code> or <code>\"\"</code>.</div>\r\n<pre>const user = { profile: { address: { city: \"NYC\" } } };\r\n\r\n// Old way — verbose and error-prone\r\nconst city = user && user.profile && user.profile.address && user.profile.address.city;\r\n\r\n// Optional chaining ?. — short and safe\r\nconst city = user?.profile?.address?.city; // \"NYC\"\r\nconst zip  = user?.profile?.address?.zip;  // undefined (no error!)\r\n\r\n// Works with methods too\r\nconst result = user?.getPermissions?.();   // undefined if method doesn't exist\r\n\r\n// Works with arrays\r\nconst first = users?.[0]?.name;           // undefined if array is empty\r\n\r\n// Nullish coalescing ??\r\nconst score = 0;\r\nscore || \"No score\"   // \"No score\" ❌ — 0 is falsy, wrong!\r\nscore ?? \"No score\"   // 0 ✅ — ?? only triggers for null/undefined\r\n\r\n// Combined\r\nconst username = user?.profile?.name ?? \"Anonymous\";\r\n\r\n// Nullish assignment ??=\r\nuser.name ??= \"Guest\"; // only sets if user.name is null/undefined</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">26</span><span class=\"q-question\">What are Map and Set in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>Map</code> is like an object but keys can be ANY type (including objects and functions), and it remembers insertion order. <code>Set</code> is a collection of unique values — duplicates are automatically removed.</div>\r\n<pre>// Map — key can be any type\r\nconst map = new Map();\r\nmap.set(\"name\", \"Alice\");\r\nmap.set(42, \"number key\");\r\nmap.set({ id: 1 }, \"object key\");  // objects as keys!\r\n\r\nmap.get(\"name\");      // \"Alice\"\r\nmap.has(\"name\");      // true\r\nmap.size;             // 3\r\nmap.delete(\"name\");\r\n\r\n// Iterate\r\nmap.forEach((value, key) => console.log(key, value));\r\nfor (const [key, value] of map) { }\r\n\r\n// Set — unique values only\r\nconst set = new Set([1, 2, 2, 3, 3, 3]);\r\nconsole.log(set); // Set {1, 2, 3} — duplicates removed!\r\n\r\nset.add(4);\r\nset.has(2);        // true\r\nset.delete(1);\r\nset.size;          // 3\r\n\r\n// Remove duplicates from array (classic trick!)\r\nconst unique = [...new Set([1, 2, 2, 3, 3])]; // [1, 2, 3]\r\n\r\n// WeakMap and WeakSet — hold weak references\r\n// (objects can be garbage collected even if they're in the WeakMap)\r\n// Good for: caching data associated with objects without preventing GC</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">27</span><span class=\"q-question\">What are symbols in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A <code>Symbol</code> is a guaranteed unique primitive value. No two symbols are ever equal, even if they have the same description. Useful for creating truly unique property keys that won't collide with any other code.</div>\r\n<pre>// Every Symbol is unique\r\nconst sym1 = Symbol(\"id\");\r\nconst sym2 = Symbol(\"id\");\r\nsym1 === sym2;  // false — always unique!\r\n\r\n// Use as object property keys (won't conflict)\r\nconst ID = Symbol(\"id\");\r\nconst user = {\r\n  name: \"Alice\",\r\n  [ID]: 12345    // square brackets needed for computed key\r\n};\r\n\r\nuser.name;  // \"Alice\"\r\nuser[ID];   // 12345\r\nuser.id;    // undefined — string \"id\" ≠ Symbol id!\r\n\r\n// Symbols are NOT enumerable — hidden from for...in and Object.keys()\r\nObject.keys(user);      // [\"name\"] — no symbol!\r\nObject.getOwnPropertySymbols(user); // [Symbol(id)]\r\n\r\n// Well-known symbols — customize built-in behavior\r\nclass MyArray {\r\n  [Symbol.iterator]() {  // make your object iterable!\r\n    let i = 0;\r\n    const data = [1, 2, 3];\r\n    return { next: () => ({ value: data[i++], done: i > data.length }) };\r\n  }\r\n}\r\nfor (const n of new MyArray()) { console.log(n); } // 1, 2, 3</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">28</span><span class=\"q-question\">What are generators in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A generator is a special function that can pause its execution (using <code>yield</code>) and resume later. It returns an iterator — you call <code>.next()</code> to get the next value. Perfect for lazy sequences and async flow control.</div>\r\n<pre>// Generator function — note the * after function\r\nfunction* counter() {\r\n  let i = 0;\r\n  while (true) {\r\n    yield i++;  // pause here, return i, then resume next time\r\n  }\r\n}\r\n\r\nconst gen = counter();\r\ngen.next().value; // 0\r\ngen.next().value; // 1\r\ngen.next().value; // 2\r\n// Never exhausted — lazy infinite sequence\r\n\r\n// Finite generator\r\nfunction* range(start, end) {\r\n  for (let i = start; i <= end; i++) {\r\n    yield i;\r\n  }\r\n}\r\n\r\nfor (const n of range(1, 5)) {\r\n  console.log(n); // 1, 2, 3, 4, 5\r\n}\r\n\r\n// Two-way communication — send values back in with .next(value)\r\nfunction* calculator() {\r\n  const x = yield \"Enter first number:\";\r\n  const y = yield \"Enter second number:\";\r\n  return x + y;\r\n}\r\nconst calc = calculator();\r\ncalc.next();         // ask for first number\r\ncalc.next(10);       // send 10\r\ncalc.next(20).value; // 30 — result!</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 5: ASYNC JS ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">⏳</span><h2 class=\"section-title\">Asynchronous JavaScript</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">29</span><span class=\"q-question\">What is the Event Loop and how does JavaScript handle asynchronous code?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>JavaScript is single-threaded — it can only do one thing at a time. The Event Loop manages async operations: when an async task completes, its callback is placed in a queue. The Event Loop continuously checks if the Call Stack is empty, then moves the next callback from the queue onto the stack.</div>\r\n<pre>console.log(\"1 — start\");      // synchronous, runs first\r\n\r\nsetTimeout(() => {\r\n  console.log(\"3 — timeout\");  // goes to callback queue\r\n}, 0);                          // even with 0ms, it waits!\r\n\r\nPromise.resolve().then(() => {\r\n  console.log(\"2 — promise\");  // goes to microtask queue\r\n});\r\n\r\nconsole.log(\"4 — end\");        // synchronous, runs now\r\n\r\n// Output order: 1, 4, 2, 3\r\n// Why? Microtask queue (promises) runs before callback queue (setTimeout)!\r\n\r\n// The order of priority:\r\n// 1. Synchronous code (Call Stack)\r\n// 2. Microtasks: Promises, queueMicrotask(), MutationObserver\r\n// 3. Macrotasks: setTimeout, setInterval, setImmediate, I/O</pre>\r\n<div class=\"example-box\"><span class=\"lbl\">🧩 Analogy</span>The Event Loop is like a restaurant waiter. The waiter can only serve one table at a time (single-threaded). When a customer orders (async task), the waiter gives it to the kitchen (Web APIs). When food is ready (async complete), it goes to the pickup counter (queue). The waiter checks the counter between serving each table (event loop).</div>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">30</span><span class=\"q-question\">What is a callback and what is \"callback hell\"?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A callback is a function passed as an argument to another function, to be called when an async operation completes. \"Callback hell\" is deeply nested callbacks that make code unreadable — solved by Promises and async/await.</div>\r\n<pre>// Callback — function passed to be called later\r\nsetTimeout(() => console.log(\"done\"), 1000);\r\n\r\n// Callback hell — the \"pyramid of doom\"\r\ngetUser(userId, (user) => {\r\n  getPosts(user.id, (posts) => {\r\n    getComments(posts[0].id, (comments) => {\r\n      getAuthor(comments[0].authorId, (author) => {\r\n        displayData(user, posts, comments, author, (result) => {\r\n          // By now you're 5 levels deep and completely lost\r\n        });\r\n      });\r\n    });\r\n  });\r\n});\r\n// Problems: hard to read, hard to handle errors, hard to maintain\r\n\r\n// Solution: Promises (then chains)\r\ngetUser(userId)\r\n  .then(user => getPosts(user.id))\r\n  .then(posts => getComments(posts[0].id))\r\n  .then(comments => getAuthor(comments[0].authorId))\r\n  .catch(error => console.error(error));\r\n\r\n// Best solution: async/await (looks synchronous!)\r\nasync function loadData() {\r\n  try {\r\n    const user = await getUser(userId);\r\n    const posts = await getPosts(user.id);\r\n    // ...\r\n  } catch (error) {\r\n    console.error(error);\r\n  }\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">31</span><span class=\"q-question\">What is a Promise and how does it work?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A Promise represents a value that will be available in the future. It has 3 states: <strong>pending</strong> (not done yet), <strong>fulfilled</strong> (success), or <strong>rejected</strong> (error). Use <code>.then()</code> for success, <code>.catch()</code> for errors, <code>.finally()</code> for cleanup.</div>\r\n<pre>// Creating a Promise\r\nconst fetchUser = (id) => new Promise((resolve, reject) => {\r\n  setTimeout(() => {\r\n    if (id > 0) {\r\n      resolve({ id, name: \"Alice\" }); // success\r\n    } else {\r\n      reject(new Error(\"Invalid ID\")); // failure\r\n    }\r\n  }, 1000);\r\n});\r\n\r\n// Consuming a Promise\r\nfetchUser(1)\r\n  .then(user => {\r\n    console.log(user);         // { id: 1, name: \"Alice\" }\r\n    return user.name;          // chain by returning\r\n  })\r\n  .then(name => console.log(name)) // \"Alice\"\r\n  .catch(err => console.error(err)) // handles any error\r\n  .finally(() => console.log(\"done\")); // always runs\r\n\r\n// Promise states:\r\n// pending  → waiting for resolve/reject\r\n// fulfilled → resolve() was called ✅\r\n// rejected  → reject() was called ❌\r\n// settled   → either fulfilled or rejected (not pending)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">32</span><span class=\"q-question\">What are Promise.all(), Promise.race(), Promise.allSettled(), Promise.any()?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>These run multiple promises in parallel with different behaviors when they resolve or reject.</div>\r\n<pre>const p1 = fetchUser(1);   // resolves in 1s\r\nconst p2 = fetchPost(1);   // resolves in 2s\r\nconst p3 = fetchData(1);   // rejects in 0.5s\r\n\r\n// Promise.all — wait for ALL, fail if ANY rejects\r\nPromise.all([p1, p2])\r\n  .then(([user, post]) => console.log(user, post)); // both results\r\n// ❌ If one rejects, the whole thing rejects\r\n\r\n// Promise.race — resolve/reject as soon as the FIRST one settles\r\nPromise.race([p1, p2])\r\n  .then(first => console.log(first)); // result of p1 (faster)\r\n\r\n// Promise.allSettled — wait for ALL, never rejects, gives status of each\r\nPromise.allSettled([p1, p2, p3])\r\n  .then(results => {\r\n    results.forEach(r => {\r\n      if (r.status === 'fulfilled') console.log(r.value);\r\n      else console.log(r.reason); // error\r\n    });\r\n  }); // ✅ never throws, always runs\r\n\r\n// Promise.any — first FULFILLED (ignores rejections, rejects if ALL fail)\r\nPromise.any([p1, p2, p3])\r\n  .then(first => console.log(first)); // p1 (p3 rejected, ignored)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">33</span><span class=\"q-question\">What is async/await and how does it work?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>async/await</code> is syntax sugar over Promises that makes async code look and behave like synchronous code. An <code>async</code> function always returns a Promise. <code>await</code> pauses execution inside the function until the Promise resolves.</div>\r\n<pre>// Promise version\r\nfunction loadUser(id) {\r\n  return fetch(`/api/users/${id}`)\r\n    .then(res => res.json())\r\n    .then(user => user)\r\n    .catch(err => console.error(err));\r\n}\r\n\r\n// async/await version — reads like normal code!\r\nasync function loadUser(id) {\r\n  try {\r\n    const res  = await fetch(`/api/users/${id}`); // pause here\r\n    const user = await res.json();                 // pause here\r\n    return user;\r\n  } catch (error) {\r\n    console.error(error);\r\n  }\r\n}\r\n\r\n// async function ALWAYS returns a Promise\r\nconst result = loadUser(1);  // Promise, not a user yet\r\nresult.then(user => console.log(user)); // or use await in another async fn\r\n\r\n// Run multiple awaits in parallel (don't do one then the other!)\r\n// ❌ Sequential — slow (1s + 2s = 3s total)\r\nconst user = await fetchUser(1);   // wait 1s\r\nconst posts = await fetchPosts(1); // THEN wait 2s\r\n\r\n// ✅ Parallel — fast (max of 1s and 2s = 2s total)\r\nconst [user, posts] = await Promise.all([fetchUser(1), fetchPosts(1)]);</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">34</span><span class=\"q-question\">What is the difference between microtasks and macrotasks?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Microtasks (Promises, MutationObserver) run immediately after the current task, before anything else. Macrotasks (setTimeout, setInterval, I/O) run in the next event loop iteration. The microtask queue is FULLY drained before the next macrotask starts.</div>\r\n<pre>console.log(\"1 — sync\");\r\n\r\nsetTimeout(() => console.log(\"4 — macrotask\"), 0); // macrotask queue\r\n\r\nPromise.resolve()\r\n  .then(() => console.log(\"2 — microtask 1\"))  // microtask queue\r\n  .then(() => console.log(\"3 — microtask 2\")); // added to microtask queue\r\n\r\nconsole.log(\"5 — sync end\");\r\n\r\n// Output: 1, 5, 2, 3, 4\r\n// Reason: sync runs first (1, 5)\r\n//         then ALL microtasks drain (2, 3)\r\n//         then macrotask runs (4)\r\n\r\n// Why this matters:\r\n// If you queue many microtasks (Promise chains), they all run\r\n// before the browser can repaint or handle setTimeout callbacks.\r\n// Too many microtasks = blocked UI!</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">35</span><span class=\"q-question\">What is the Fetch API and how do you handle errors properly?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The Fetch API is the modern way to make HTTP requests. It returns a Promise. Critical gotcha: <code>fetch()</code> does NOT reject on HTTP error status codes (like 404 or 500) — you must check <code>response.ok</code> manually.</div>\r\n<pre>// Basic fetch\r\nconst response = await fetch('https://api.example.com/users');\r\nconst data = await response.json();\r\n\r\n// ⚠️ fetch() ONLY rejects on network errors (no internet, DNS fail)\r\n// It does NOT reject for 404, 500 etc.\r\n\r\n// ✅ Correct error handling\r\nasync function fetchUsers() {\r\n  try {\r\n    const response = await fetch('/api/users');\r\n\r\n    if (!response.ok) {  // check for 4xx/5xx manually!\r\n      throw new Error(`HTTP Error: ${response.status}`);\r\n    }\r\n\r\n    const data = await response.json();\r\n    return data;\r\n  } catch (error) {\r\n    if (error.name === 'TypeError') {\r\n      console.error('Network error — no internet');\r\n    } else {\r\n      console.error('Request failed:', error.message);\r\n    }\r\n  }\r\n}\r\n\r\n// POST request\r\nconst response = await fetch('/api/users', {\r\n  method: 'POST',\r\n  headers: { 'Content-Type': 'application/json' },\r\n  body: JSON.stringify({ name: 'Alice' })\r\n});</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 6: PROTOTYPES & CLASSES ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🧬</span><h2 class=\"section-title\">Prototypes, Classes & OOP</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">36</span><span class=\"q-question\">What is the prototype chain in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Every JavaScript object has a hidden <code>[[Prototype]]</code> link to another object. When you access a property that doesn't exist on an object, JS looks up the chain — to the prototype, then to the prototype's prototype — until it reaches <code>null</code>.</div>\r\n<pre>const animal = { breathes: true };\r\nconst dog = { barks: true };\r\n\r\n// Set dog's prototype to animal\r\nObject.setPrototypeOf(dog, animal);\r\n\r\ndog.barks;    // true — own property\r\ndog.breathes; // true — found on prototype (animal)!\r\ndog.flies;    // undefined — not found anywhere in chain\r\n\r\n// How it works:\r\n// dog → animal → Object.prototype → null (end of chain)\r\n\r\n// Every array's chain:\r\n// [] → Array.prototype → Object.prototype → null\r\n// That's why arrays have .map, .filter etc. — they come from Array.prototype\r\n\r\nconst arr = [1, 2, 3];\r\narr.hasOwnProperty('length'); // true — from Object.prototype\r\narr.map;    // function — from Array.prototype\r\narr.map === Array.prototype.map; // true!\r\n\r\n// Check prototype\r\nObject.getPrototypeOf(dog) === animal; // true\r\ndog.__proto__ === animal;             // true (legacy, use getPrototypeOf)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">37</span><span class=\"q-question\">What are ES6 Classes and how do they relate to prototypes?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>ES6 classes are <strong>syntactic sugar</strong> over prototype-based inheritance — they make OOP code cleaner but underneath they still use prototypes. Classes add nothing new — they're just a nicer way to write constructor functions and set up prototypes.</div>\r\n<pre>// ES6 Class syntax\r\nclass Animal {\r\n  constructor(name) {\r\n    this.name = name;  // instance property\r\n  }\r\n\r\n  speak() {            // method on Animal.prototype\r\n    return `${this.name} makes a sound`;\r\n  }\r\n\r\n  static create(name) {  // static method — on the class itself, not instances\r\n    return new Animal(name);\r\n  }\r\n}\r\n\r\n// Inheritance with extends\r\nclass Dog extends Animal {\r\n  constructor(name, breed) {\r\n    super(name);           // MUST call super() first!\r\n    this.breed = breed;\r\n  }\r\n\r\n  speak() {                // override parent method\r\n    return `${this.name} barks!`;\r\n  }\r\n}\r\n\r\nconst dog = new Dog(\"Rex\", \"Labrador\");\r\ndog.speak();              // \"Rex barks!\"\r\ndog instanceof Dog;       // true\r\ndog instanceof Animal;    // true (inheritance chain)\r\n\r\n// Under the hood:\r\n// Dog.prototype → Animal.prototype → Object.prototype → null</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">38</span><span class=\"q-question\">What are private class fields and methods?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Private class fields (using <code>#</code> prefix) are truly private — they cannot be accessed or modified from outside the class. Even subclasses can't access them. This is real encapsulation in JavaScript (ES2022).</div>\r\n<pre>class BankAccount {\r\n  #balance = 0;       // private field — truly inaccessible outside\r\n  #owner;             // private field\r\n  #transactionHistory = [];\r\n\r\n  constructor(owner, initialDeposit) {\r\n    this.#owner = owner;  // can only set from inside\r\n    this.#balance = initialDeposit;\r\n  }\r\n\r\n  get balance() {         // public getter — controlled access\r\n    return `$${this.#balance}`;\r\n  }\r\n\r\n  deposit(amount) {\r\n    this.#balance += amount;\r\n    this.#log('deposit', amount);\r\n  }\r\n\r\n  #log(type, amount) {    // private method!\r\n    this.#transactionHistory.push({ type, amount });\r\n  }\r\n}\r\n\r\nconst account = new BankAccount(\"Alice\", 1000);\r\naccount.deposit(500);\r\nconsole.log(account.balance); // \"$1500\"\r\naccount.#balance;              // ❌ SyntaxError — truly private!\r\naccount.#log();                // ❌ SyntaxError</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">39</span><span class=\"q-question\">What is the difference between hasOwnProperty and the 'in' operator?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>hasOwnProperty()</code> checks if a property belongs directly to the object (not inherited). The <code>in</code> operator checks the entire prototype chain — including inherited properties.</div>\r\n<pre>const parent = { inherited: true };\r\nconst child = Object.create(parent);\r\nchild.own = \"mine\";\r\n\r\n// in — checks the entire prototype chain\r\n\"own\" in child;        // true  (own property)\r\n\"inherited\" in child;  // true  (inherited from parent)\r\n\"toString\" in child;   // true  (from Object.prototype)\r\n\r\n// hasOwnProperty — only own properties\r\nchild.hasOwnProperty(\"own\");        // true\r\nchild.hasOwnProperty(\"inherited\");  // false\r\nchild.hasOwnProperty(\"toString\");   // false\r\n\r\n// Modern replacement: Object.hasOwn() (ES2022 — preferred)\r\nObject.hasOwn(child, \"own\");        // true\r\nObject.hasOwn(child, \"inherited\");  // false\r\n// Object.hasOwn is safer — works even if hasOwnProperty is overridden\r\n\r\n// Practical use\r\nconst config = { debug: false };\r\nif (Object.hasOwn(config, 'debug')) {\r\n  // 'debug' was explicitly set (even as false/null/0)\r\n  // different from: if (config.debug) — would miss falsy values!\r\n}</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 7: DOM & BROWSER ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🌐</span><h2 class=\"section-title\">DOM & Browser APIs</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">40</span><span class=\"q-question\">What is the DOM?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The DOM (Document Object Model) is a tree representation of an HTML page. The browser creates it from your HTML. JavaScript can read and modify the DOM to dynamically change the page — add elements, change styles, handle events.</div>\r\n<pre>// Selecting elements\r\ndocument.getElementById('header');         // by id\r\ndocument.querySelector('.card');           // first matching CSS selector\r\ndocument.querySelectorAll('.card');        // all matching (NodeList)\r\ndocument.getElementsByClassName('btn');   // by class (live HTMLCollection)\r\n\r\n// Reading/writing content\r\nelement.textContent = \"New text\";          // text only, safe from XSS\r\nelement.innerHTML = \"&lt;strong&gt;Bold&lt;/strong&gt;\"; // parses HTML (XSS risk!)\r\n\r\n// Modifying styles\r\nelement.style.color = \"red\";\r\nelement.style.fontSize = \"18px\";\r\nelement.classList.add(\"active\");\r\nelement.classList.remove(\"hidden\");\r\nelement.classList.toggle(\"open\");\r\nelement.classList.contains(\"active\");     // true/false\r\n\r\n// Creating and adding elements\r\nconst div = document.createElement(\"div\");\r\ndiv.className = \"card\";\r\ndiv.textContent = \"Hello\";\r\nparent.appendChild(div);\r\nparent.append(div, \"text\");               // modern, appends multiple\r\nparent.prepend(div);\r\nparent.removeChild(div);\r\ndiv.remove();                             // modern</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">41</span><span class=\"q-question\">What is event delegation and why is it useful?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Event delegation attaches one event listener to a parent element instead of many listeners on each child. When a child is clicked, the event bubbles up to the parent, which handles it. Efficient and works for dynamically added elements.</div>\r\n<pre>// ❌ Without delegation — listener on every item (inefficient)\r\ndocument.querySelectorAll('.list-item').forEach(item => {\r\n  item.addEventListener('click', handleClick);\r\n  // 100 items = 100 listeners!\r\n  // Also: dynamically added items DON'T get the listener!\r\n});\r\n\r\n// ✅ With delegation — ONE listener on the parent\r\ndocument.querySelector('.list').addEventListener('click', (event) => {\r\n  // Check if the clicked element matches what we want\r\n  if (event.target.classList.contains('list-item')) {\r\n    handleClick(event.target);\r\n  }\r\n\r\n  // Or use closest() for nested elements\r\n  const item = event.target.closest('.list-item');\r\n  if (item) handleClick(item);\r\n});\r\n\r\n// Benefits:\r\n// 1. One listener instead of many → less memory\r\n// 2. Works for dynamically added elements (they bubble up too!)\r\n// 3. No need to add/remove listeners when items change</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">42</span><span class=\"q-question\">What is event bubbling and capturing?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>When you click a child element, the event travels in two phases: <strong>Capturing</strong> (from window down to target), then <strong>Bubbling</strong> (from target back up to window). By default, event listeners use the bubble phase. Use <code>stopPropagation()</code> to stop the travel.</div>\r\n<pre>// Bubbling (default) — event fires from child UP to parent\r\n&lt;div id=\"outer\"&gt;\r\n  &lt;button id=\"inner\"&gt;Click me&lt;/button&gt;\r\n&lt;/div&gt;\r\n\r\n// Clicking the button fires both handlers (inner → outer order)\r\nouter.addEventListener('click', () => console.log('outer'));\r\ninner.addEventListener('click', () => console.log('inner'));\r\n// Output: \"inner\", then \"outer\"\r\n\r\n// Stop bubbling\r\ninner.addEventListener('click', (e) => {\r\n  e.stopPropagation(); // stops event from bubbling up to outer\r\n  console.log('inner only');\r\n});\r\n\r\n// Capturing — listen in the capture phase (3rd argument = true)\r\nouter.addEventListener('click', () => console.log('outer capture'), true);\r\n// Fires BEFORE inner — outer capture, inner, outer bubble\r\n\r\n// preventDefault — stop browser's default action\r\nlink.addEventListener('click', (e) => {\r\n  e.preventDefault();  // stops link from navigating\r\n  handleCustomNavigation();\r\n});</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">43</span><span class=\"q-question\">What is localStorage, sessionStorage, and cookies?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Three ways to store data in the browser. <strong>localStorage</strong> persists forever until cleared. <strong>sessionStorage</strong> persists until the tab is closed. <strong>Cookies</strong> persist until expiry date, are sent with every HTTP request, and can be HttpOnly (server-only).</div>\r\n<pre>// localStorage — persists across sessions\r\nlocalStorage.setItem('user', JSON.stringify({ name: 'Alice' }));\r\nconst user = JSON.parse(localStorage.getItem('user'));\r\nlocalStorage.removeItem('user');\r\nlocalStorage.clear(); // removes ALL\r\n\r\n// sessionStorage — same API, but lost when tab closes\r\nsessionStorage.setItem('draft', 'my draft text');\r\nsessionStorage.getItem('draft');\r\n// Gone when user closes the tab\r\n\r\n// Cookies (more complex, read/write via document.cookie)\r\n// Setting:\r\ndocument.cookie = \"username=Alice; expires=Fri, 31 Dec 2025 00:00:00 UTC; path=/\";\r\n// Reading: document.cookie returns ALL cookies as one string\r\n\r\n// Comparison:\r\n// localStorage:   ~5MB, no expiry, JS only, same origin\r\n// sessionStorage: ~5MB, tab lifetime, JS only, same origin\r\n// Cookies:        ~4KB, custom expiry, sent with requests, works with server\r\n//                 HttpOnly flag = JS cannot read (safer for auth tokens)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">44</span><span class=\"q-question\">What is the Intersection Observer API?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The Intersection Observer watches when an element enters or leaves the viewport (or a parent element). It's efficient — no scroll event listeners needed. Perfect for lazy loading images, infinite scroll, and scroll-triggered animations.</div>\r\n<pre>// Create an observer\r\nconst observer = new IntersectionObserver((entries) => {\r\n  entries.forEach(entry => {\r\n    if (entry.isIntersecting) {\r\n      // Element is visible in the viewport!\r\n      entry.target.classList.add('visible');\r\n      observer.unobserve(entry.target); // stop watching after first trigger\r\n    }\r\n  });\r\n}, {\r\n  threshold: 0.1,    // trigger when 10% of element is visible\r\n  rootMargin: '0px 0px -50px 0px' // offset from viewport edges\r\n});\r\n\r\n// Watch elements\r\ndocument.querySelectorAll('.fade-in').forEach(el => observer.observe(el));\r\n\r\n// Lazy loading images\r\nconst imgObserver = new IntersectionObserver(entries => {\r\n  entries.forEach(entry => {\r\n    if (entry.isIntersecting) {\r\n      const img = entry.target;\r\n      img.src = img.dataset.src; // set real src when visible\r\n      imgObserver.unobserve(img);\r\n    }\r\n  });\r\n});\r\ndocument.querySelectorAll('img[data-src]').forEach(img => imgObserver.observe(img));</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 8: ERROR HANDLING ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🛡️</span><h2 class=\"section-title\">Error Handling & Debugging</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">45</span><span class=\"q-question\">How does try/catch/finally work?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>try</code> runs code that might fail. <code>catch</code> handles the error if one occurs. <code>finally</code> always runs — whether there was an error or not. Perfect for cleanup code like closing database connections.</div>\r\n<pre>try {\r\n  const data = JSON.parse(invalidJSON);  // throws SyntaxError\r\n  console.log(data);\r\n} catch (error) {\r\n  console.error(error.name);    // \"SyntaxError\"\r\n  console.error(error.message); // \"Unexpected token...\"\r\n  console.error(error.stack);   // full stack trace\r\n} finally {\r\n  console.log(\"This ALWAYS runs\");  // cleanup here\r\n  // db.close(); connection.release(); etc.\r\n}\r\n\r\n// Custom errors\r\nclass ValidationError extends Error {\r\n  constructor(message, field) {\r\n    super(message);\r\n    this.name = \"ValidationError\";\r\n    this.field = field;\r\n  }\r\n}\r\n\r\ntry {\r\n  throw new ValidationError(\"Too short\", \"username\");\r\n} catch (error) {\r\n  if (error instanceof ValidationError) {\r\n    console.log(`${error.field}: ${error.message}`);\r\n  } else {\r\n    throw error; // re-throw errors you can't handle\r\n  }\r\n}\r\n\r\n// async/await error handling\r\nasync function fetchData() {\r\n  try {\r\n    const res = await fetch('/api/data');\r\n    return await res.json();\r\n  } catch (error) {\r\n    console.error('Failed:', error);\r\n  }\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">46</span><span class=\"q-question\">What are the different types of errors in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>JavaScript has 7 built-in error types. Understanding them helps you quickly identify what went wrong from an error message.</div>\r\n<pre>// SyntaxError — invalid JS syntax (can't even parse the code)\r\neval(\"let = 5\");  // SyntaxError: Unexpected token '='\r\n\r\n// ReferenceError — accessing variable that doesn't exist\r\nconsole.log(undeclaredVar);  // ReferenceError: not defined\r\nlet x = y + 1;               // ReferenceError: y is not defined\r\n\r\n// TypeError — wrong type for operation\r\nnull.property;         // TypeError: Cannot read properties of null\r\nundefined();           // TypeError: undefined is not a function\r\n1 + {} toString();     // TypeError\r\n\r\n// RangeError — value outside allowed range\r\nnew Array(-1);         // RangeError: Invalid array length\r\n(1.5).toFixed(200);    // RangeError: too many decimals\r\n\r\n// URIError — bad URI encoding\r\ndecodeURIComponent('%');  // URIError\r\n\r\n// EvalError — error with eval() (rare)\r\n\r\n// AggregateError — multiple errors (Promise.any rejection)\r\nPromise.any([Promise.reject('a'), Promise.reject('b')])\r\n  .catch(e => e.errors); // ['a', 'b']</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 9: FUNCTIONAL PROGRAMMING ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🔢</span><h2 class=\"section-title\">Functional Programming Concepts</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">47</span><span class=\"q-question\">What are pure functions and why are they important?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A pure function always returns the same output for the same input, and has no side effects (doesn't change anything outside itself). Pure functions are predictable, easy to test, and can be safely used with memoization.</div>\r\n<pre>// Pure function — same input always = same output, no side effects\r\nfunction add(a, b) { return a + b; }\r\nadd(2, 3); // always 5, no matter what\r\n\r\n// Impure — depends on external state\r\nlet total = 0;\r\nfunction addToTotal(n) { total += n; return total; }\r\n// Different result each time depending on 'total'\r\n\r\n// Impure — side effect (modifies external thing)\r\nfunction addUser(user) {\r\n  database.users.push(user); // modifies external state\r\n  return user;\r\n}\r\n\r\n// Pure version — return new state instead of mutating\r\nfunction addUser(users, newUser) {\r\n  return [...users, newUser]; // doesn't touch original\r\n}\r\n\r\n// Why pure functions matter:\r\n// ✅ Easy to test (no setup/teardown needed)\r\n// ✅ Memoizable (cache results safely)\r\n// ✅ Parallelizable (no race conditions)\r\n// ✅ Referentially transparent (can replace call with result)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">48</span><span class=\"q-question\">What is currying in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Currying transforms a function that takes multiple arguments into a series of functions that each take one argument. Instead of <code>add(1, 2)</code>, you get <code>add(1)(2)</code>. Makes functions highly reusable and composable.</div>\r\n<pre>// Regular function\r\nfunction add(a, b) { return a + b; }\r\nadd(2, 3); // 5\r\n\r\n// Curried version\r\nfunction curriedAdd(a) {\r\n  return function(b) {\r\n    return a + b;\r\n  };\r\n}\r\nconst add5 = curriedAdd(5);\r\nadd5(3);  // 8\r\nadd5(10); // 15\r\n\r\n// Arrow function currying (concise)\r\nconst multiply = a => b => a * b;\r\nconst double = multiply(2);\r\nconst triple = multiply(3);\r\ndouble(5); // 10\r\ntriple(5); // 15\r\n\r\n// Practical example\r\nconst filter = predicate => array => array.filter(predicate);\r\nconst isEven = n => n % 2 === 0;\r\nconst getEvens = filter(isEven);\r\ngetEvens([1, 2, 3, 4, 5]); // [2, 4]\r\n\r\n// Generic curry function\r\nconst curry = fn => {\r\n  const curried = (...args) =>\r\n    args.length >= fn.length ? fn(...args) : (...more) => curried(...args, ...more);\r\n  return curried;\r\n};</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">49</span><span class=\"q-question\">What is function composition?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Function composition means combining small functions to build a bigger one. The output of one function becomes the input of the next. It's the building block of functional programming — small focused functions piped together.</div>\r\n<pre>// Small, focused functions\r\nconst double = x => x * 2;\r\nconst addTen = x => x + 10;\r\nconst square = x => x * x;\r\n\r\n// Manual composition\r\nconst result = square(addTen(double(5)));\r\n// 5 → double → 10 → addTen → 20 → square → 400\r\n\r\n// Compose helper — applies right to left (mathematical order)\r\nconst compose = (...fns) => x => fns.reduceRight((acc, fn) => fn(acc), x);\r\nconst transform = compose(square, addTen, double);\r\ntransform(5); // 400\r\n\r\n// Pipe helper — applies left to right (readable order)\r\nconst pipe = (...fns) => x => fns.reduce((acc, fn) => fn(acc), x);\r\nconst process = pipe(double, addTen, square);\r\nprocess(5); // 400 — same result, more readable\r\n\r\n// Real-world example\r\nconst processUser = pipe(\r\n  validateUser,\r\n  normalizeEmail,\r\n  hashPassword,\r\n  saveToDatabase\r\n);\r\nawait processUser(rawUserData);</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">50</span><span class=\"q-question\">What is memoization?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Memoization is caching the result of a function call so that subsequent calls with the same arguments return the cached result instantly instead of recomputing. Makes expensive pure functions fast.</div>\r\n<pre>// Without memoization — recalculates every time\r\nfunction fib(n) {\r\n  if (n <= 1) return n;\r\n  return fib(n - 1) + fib(n - 2);\r\n}\r\nfib(40); // extremely slow — 2^40 calls!\r\n\r\n// With memoization — cache results\r\nfunction memoize(fn) {\r\n  const cache = new Map();\r\n  return function(...args) {\r\n    const key = JSON.stringify(args);\r\n    if (cache.has(key)) {\r\n      return cache.get(key); // instant!\r\n    }\r\n    const result = fn.apply(this, args);\r\n    cache.set(key, result);\r\n    return result;\r\n  };\r\n}\r\n\r\nconst memoFib = memoize(function fib(n) {\r\n  if (n <= 1) return n;\r\n  return memoFib(n - 1) + memoFib(n - 2);\r\n});\r\n\r\nmemoFib(40); // fast! each value calculated once\r\nmemoFib(40); // instant — from cache\r\n\r\n// React's useMemo and useCallback are memoization!</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 10: MODULES & PATTERNS ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">📐</span><h2 class=\"section-title\">Modules & Design Patterns</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">51</span><span class=\"q-question\">What are ES Modules (import/export)?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>ES Modules are the official way to split JavaScript into files. <code>export</code> makes code available to other files. <code>import</code> brings it in. Each module has its own scope — no global pollution.</div>\r\n<pre>// math.js — named exports\r\nexport const PI = 3.14159;\r\nexport function add(a, b) { return a + b; }\r\nexport function multiply(a, b) { return a * b; }\r\n\r\n// Or export at the bottom\r\nconst helper = () => {};\r\nexport { helper };\r\n\r\n// math.js — default export (one per file)\r\nexport default function calculate(a, b) { return a + b; }\r\n\r\n// app.js — importing\r\nimport { PI, add, multiply } from './math.js';  // named imports\r\nimport calculate from './math.js';               // default import\r\nimport * as Math from './math.js';               // import all as namespace\r\nimport { add as sum } from './math.js';          // rename on import\r\n\r\n// Dynamic import — lazy load at runtime\r\nconst module = await import('./heavy-module.js');\r\n// OR\r\nbutton.addEventListener('click', async () => {\r\n  const { format } = await import('./formatter.js');\r\n  format(data);\r\n});</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">52</span><span class=\"q-question\">What is the Module pattern?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The Module pattern uses closures to create private state and expose a public API. It was the go-to pattern before ES6 modules existed and is still useful for understanding encapsulation.</div>\r\n<pre>// Module pattern using IIFE + closure\r\nconst CounterModule = (function() {\r\n  // Private — not accessible from outside\r\n  let count = 0;\r\n  const history = [];\r\n\r\n  function logChange(action) {\r\n    history.push({ action, value: count, time: Date.now() });\r\n  }\r\n\r\n  // Public API — returned object\r\n  return {\r\n    increment() {\r\n      count++;\r\n      logChange('increment');\r\n    },\r\n    decrement() {\r\n      count--;\r\n      logChange('decrement');\r\n    },\r\n    getCount() { return count; },\r\n    getHistory() { return [...history]; } // return copy, not reference\r\n  };\r\n})();\r\n\r\nCounterModule.increment();\r\nCounterModule.increment();\r\nCounterModule.getCount();   // 2\r\nCounterModule.count;        // undefined — truly private!\r\nCounterModule.getHistory(); // [{action:'increment',...}, ...]</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">53</span><span class=\"q-question\">What is the Observer pattern (publish/subscribe)?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The Observer pattern lets objects \"subscribe\" to events and be notified when something happens, without the publisher knowing who's listening. The foundation of event systems, React state, Redux, and most UI frameworks.</div>\r\n<pre>class EventEmitter {\r\n  #events = {};\r\n\r\n  on(event, listener) {\r\n    if (!this.#events[event]) this.#events[event] = [];\r\n    this.#events[event].push(listener);\r\n    return () => this.off(event, listener); // return unsubscribe fn\r\n  }\r\n\r\n  off(event, listener) {\r\n    this.#events[event] = this.#events[event]?.filter(l => l !== listener);\r\n  }\r\n\r\n  emit(event, ...args) {\r\n    this.#events[event]?.forEach(listener => listener(...args));\r\n  }\r\n\r\n  once(event, listener) {\r\n    const wrapper = (...args) => {\r\n      listener(...args);\r\n      this.off(event, wrapper);\r\n    };\r\n    this.on(event, wrapper);\r\n  }\r\n}\r\n\r\nconst emitter = new EventEmitter();\r\n\r\nconst unsubscribe = emitter.on('data', data => console.log('received:', data));\r\nemitter.on('error', err => console.error('error:', err));\r\n\r\nemitter.emit('data', { users: [1, 2, 3] }); // \"received: { users: [1,2,3] }\"\r\nunsubscribe(); // stop listening</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">54</span><span class=\"q-question\">What is the Singleton pattern?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The Singleton pattern ensures a class has only ONE instance and provides a global access point to it. Useful for shared resources like a database connection, configuration, or state store.</div>\r\n<pre>// Classic Singleton\r\nclass Database {\r\n  static #instance = null;\r\n  #connection;\r\n\r\n  constructor(url) {\r\n    if (Database.#instance) {\r\n      return Database.#instance; // return existing instance\r\n    }\r\n    this.#connection = url;\r\n    Database.#instance = this;\r\n  }\r\n\r\n  static getInstance(url) {\r\n    if (!Database.#instance) {\r\n      Database.#instance = new Database(url);\r\n    }\r\n    return Database.#instance;\r\n  }\r\n\r\n  query(sql) { /* ... */ }\r\n}\r\n\r\nconst db1 = Database.getInstance('mongodb://localhost');\r\nconst db2 = Database.getInstance('other-url');\r\ndb1 === db2; // true — same instance!\r\n\r\n// ES Module as Singleton (simplest approach)\r\n// config.js\r\nlet instance = null;\r\nexport function getConfig() {\r\n  if (!instance) instance = loadConfig();\r\n  return instance;\r\n}\r\n// Modules are cached after first load — effectively a singleton</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 11: ADVANCED CONCEPTS ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">🧪</span><h2 class=\"section-title\">Advanced JavaScript Concepts</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">55</span><span class=\"q-question\">What is a Proxy in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A <code>Proxy</code> wraps an object and lets you intercept and customize operations on it — like property reads, writes, function calls, and more. Vue 3's reactivity system is built with Proxies.</div>\r\n<pre>const handler = {\r\n  get(target, prop) {\r\n    console.log(`Getting ${prop}`);\r\n    return prop in target ? target[prop] : `Property ${prop} doesn't exist`;\r\n  },\r\n  set(target, prop, value) {\r\n    if (typeof value !== 'number') throw new TypeError('Only numbers!');\r\n    target[prop] = value;\r\n    console.log(`Set ${prop} = ${value}`);\r\n    return true; // must return true from set trap\r\n  },\r\n  has(target, prop) {\r\n    console.log(`Checking if ${prop} exists`);\r\n    return prop in target;\r\n  }\r\n};\r\n\r\nconst scores = new Proxy({}, handler);\r\nscores.alice = 95;        // \"Set alice = 95\"\r\nscores.bob = \"high\";      // ❌ TypeError: Only numbers!\r\nscores.alice;             // \"Getting alice\" → 95\r\nscores.charlie;           // \"Getting charlie\" → \"Property charlie doesn't exist\"\r\n'alice' in scores;        // \"Checking if alice exists\" → true\r\n\r\n// Reactive object (like Vue's data)\r\nfunction reactive(obj) {\r\n  return new Proxy(obj, {\r\n    set(target, prop, value) {\r\n      target[prop] = value;\r\n      render(); // re-render on any change!\r\n      return true;\r\n    }\r\n  });\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">56</span><span class=\"q-question\">What is Reflect in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>Reflect</code> is a built-in object with static methods that mirror Proxy traps. It lets you perform the default behavior inside a Proxy trap, and provides functional versions of operators like <code>in</code>, <code>delete</code>, and property access.</div>\r\n<pre>// Reflect mirrors Proxy traps exactly\r\nReflect.get(obj, 'name');           // same as obj.name\r\nReflect.set(obj, 'name', 'Alice');  // same as obj.name = 'Alice'\r\nReflect.has(obj, 'name');           // same as 'name' in obj\r\nReflect.deleteProperty(obj, 'name');// same as delete obj.name\r\nReflect.ownKeys(obj);               // all keys (including symbols)\r\n\r\n// Most useful: inside Proxy traps to call the default behavior\r\nconst handler = {\r\n  set(target, prop, value) {\r\n    // Do something extra\r\n    console.log(`Setting ${prop}`);\r\n    // Then do the default behavior\r\n    return Reflect.set(target, prop, value); // cleaner than target[prop] = value\r\n  },\r\n  get(target, prop) {\r\n    // Track access\r\n    trackAccess(prop);\r\n    // Default behavior\r\n    return Reflect.get(target, prop);\r\n  }\r\n};\r\n\r\n// Without Reflect you'd write: target[prop] = value; return true;\r\n// Reflect.set handles edge cases properly (prototype chains, etc.)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">57</span><span class=\"q-question\">What is garbage collection in JavaScript?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Garbage collection (GC) automatically frees memory when objects are no longer reachable (no references pointing to them). JavaScript's GC uses \"mark and sweep\" — it marks all reachable objects from root, then sweeps away (frees) everything else.</div>\r\n<pre>// Memory is freed when references are gone\r\nlet user = { name: \"Alice\" };  // object created in heap\r\nuser = null;  // reference removed → object eligible for GC\r\n\r\n// Memory leak — object kept alive unintentionally\r\nconst cache = new Map();\r\nfunction processUser(user) {\r\n  cache.set(user, computeExpensiveData(user));\r\n  // If 'user' objects are never removed from cache,\r\n  // they can never be garbage collected → memory leak!\r\n}\r\n\r\n// Fix: use WeakMap — keys are held weakly (don't prevent GC)\r\nconst weakCache = new WeakMap();\r\nfunction processUser(user) {\r\n  weakCache.set(user, computeExpensiveData(user));\r\n  // When 'user' has no other references, GC can collect it\r\n  // and the WeakMap entry is automatically removed!\r\n}\r\n\r\n// Other memory leak sources:\r\n// - Forgotten event listeners (always removeEventListener on cleanup)\r\n// - Closures holding large objects\r\n// - Global variables\r\n// - setInterval not cleared</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">58</span><span class=\"q-question\">What is debouncing and throttling?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><strong>Debounce</strong> waits until the user stops doing something for X ms, then fires once. <strong>Throttle</strong> fires at most once per X ms, no matter how often triggered. Debounce = wait for pause. Throttle = rate limit.</div>\r\n<pre>// Debounce — fires AFTER user stops for delay ms\r\nfunction debounce(fn, delay) {\r\n  let timerId;\r\n  return function(...args) {\r\n    clearTimeout(timerId);\r\n    timerId = setTimeout(() => fn.apply(this, args), delay);\r\n  };\r\n}\r\n\r\n// Search only after user stops typing for 300ms\r\nconst searchInput = document.querySelector('#search');\r\nsearchInput.addEventListener('input', debounce((e) => {\r\n  fetchResults(e.target.value); // API call only after pause\r\n}, 300));\r\n\r\n// Throttle — fires at most once per interval ms\r\nfunction throttle(fn, interval) {\r\n  let lastRun = 0;\r\n  return function(...args) {\r\n    const now = Date.now();\r\n    if (now - lastRun >= interval) {\r\n      lastRun = now;\r\n      fn.apply(this, args);\r\n    }\r\n  };\r\n}\r\n\r\n// Scroll handler — max once per 100ms\r\nwindow.addEventListener('scroll', throttle(() => {\r\n  updateScrollProgress();\r\n}, 100));\r\n\r\n// Debounce = search inputs, form validation, window resize\r\n// Throttle = scroll handlers, mousemove, resize, button clicks (prevent spam)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">59</span><span class=\"q-question\">What is WeakRef and FinalizationRegistry?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>WeakRef</code> holds a \"weak\" reference to an object — it doesn't prevent garbage collection. <code>FinalizationRegistry</code> lets you register a callback that runs when an object is garbage collected. Both are advanced GC-related features (ES2021).</div>\r\n<pre>// WeakRef — holds object weakly (doesn't prevent GC)\r\nlet heavyObject = { data: new Array(1000000).fill('data') };\r\nconst ref = new WeakRef(heavyObject);\r\n\r\n// Still has the object\r\nref.deref()?.data.length; // 1000000\r\n\r\nheavyObject = null; // remove strong reference\r\n// At some point, GC may collect it\r\n// ref.deref() will return undefined after GC\r\n\r\n// Caching with WeakRef — cache that doesn't prevent GC\r\nconst cache = new Map();\r\nfunction getCachedData(key, computeFn) {\r\n  const ref = cache.get(key);\r\n  const cached = ref?.deref();\r\n  if (cached) return cached;\r\n\r\n  const value = computeFn();\r\n  cache.set(key, new WeakRef(value));\r\n  return value;\r\n}\r\n\r\n// FinalizationRegistry — callback when object is GC'd\r\nconst registry = new FinalizationRegistry((heldValue) => {\r\n  console.log(`Object with token ${heldValue} was garbage collected`);\r\n});\r\n\r\nlet obj = { name: \"Alice\" };\r\nregistry.register(obj, \"alice-token\"); // register with a held value\r\nobj = null; // when GC collects obj, logs the token</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">60</span><span class=\"q-question\">What is tail call optimization?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A tail call is when a function's last action is calling another function. Tail call optimization (TCO) allows the engine to reuse the current stack frame instead of creating a new one — preventing stack overflows in deep recursion. Specified in ES6 but only Safari implements it fully.</div>\r\n<pre>// Regular recursion — creates new stack frame each call\r\nfunction factorial(n) {\r\n  if (n <= 1) return 1;\r\n  return n * factorial(n - 1); // ← NOT a tail call (multiplies after)\r\n  // Stack: factorial(5) → factorial(4) → factorial(3) → ...\r\n  // With large n, causes Stack Overflow!\r\n}\r\n\r\n// Tail-recursive — last action is just the call\r\nfunction factorialTCO(n, acc = 1) {\r\n  if (n <= 1) return acc;\r\n  return factorialTCO(n - 1, n * acc); // ← tail call! acc carries result\r\n}\r\n// Engine can reuse stack frame instead of adding a new one\r\n\r\n// Practical alternative — use a loop for deep recursion\r\nfunction factorialLoop(n) {\r\n  let result = 1;\r\n  while (n > 1) { result *= n--; }\r\n  return result;\r\n}</pre>\r\n</div></div>\r\n\r\n<!-- ══════════ SECTION 12: PERFORMANCE & MODERN JS ══════════ -->\r\n<div class=\"section-head\"><span class=\"section-icon\">⚡</span><h2 class=\"section-title\">Performance, Web Workers & Modern JS</h2><div class=\"section-line\"></div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">61</span><span class=\"q-question\">What are Web Workers?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Web Workers run JavaScript on a background thread — separate from the main thread. This means heavy computations don't block the UI. Workers communicate with the main thread via <code>postMessage()</code> and can't access the DOM.</div>\r\n<pre>// Main thread — main.js\r\nconst worker = new Worker('worker.js');\r\n\r\n// Send data to worker\r\nworker.postMessage({ numbers: [1, 2, 3, ..., 1000000] });\r\n\r\n// Receive results from worker\r\nworker.onmessage = (event) => {\r\n  console.log('Result:', event.data.result); // UI stays responsive!\r\n};\r\n\r\n// Worker thread — worker.js\r\nself.onmessage = (event) => {\r\n  const { numbers } = event.data;\r\n  // Heavy computation here — doesn't block UI!\r\n  const result = numbers.reduce((sum, n) => sum + n, 0);\r\n  self.postMessage({ result }); // send result back\r\n};\r\n\r\n// Worker types:\r\n// Dedicated Worker — one page only\r\n// Shared Worker — multiple pages/tabs can connect\r\n// Service Worker — intercepts network requests (offline/cache)</pre>\r\n<div class=\"note-box\"><span class=\"lbl\">💡 When to Use</span>Use Web Workers for: image/video processing, complex math (simulations, crypto), large data sorting/filtering, JSON parsing of huge files. Without workers, these operations freeze the entire browser tab.</div>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">62</span><span class=\"q-question\">What is the difference between setTimeout(fn, 0) and queueMicrotask()?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>setTimeout(fn, 0)</code> queues in the macrotask queue — runs after all microtasks and after the browser renders. <code>queueMicrotask(fn)</code> queues in the microtask queue — runs immediately after current code, before any renders or setTimeout callbacks.</div>\r\n<pre>console.log('1 sync');\r\n\r\nqueueMicrotask(() => console.log('3 microtask'));\r\nsetTimeout(() => console.log('5 macrotask'), 0);\r\nPromise.resolve().then(() => console.log('2 promise microtask'));\r\n\r\nconsole.log('4 sync end');\r\n\r\n// Order: 1, 4, 2, 3, 5\r\n// Because: sync → all microtasks (promises first, then queueMicrotask) → macrotasks\r\n\r\n// Use cases:\r\n// queueMicrotask: when you need to run AFTER current code but BEFORE rendering\r\n//                 useful for batching DOM updates\r\nqueueMicrotask(() => {\r\n  batchedDOMUpdates(); // all changes, then browser renders once\r\n});\r\n\r\n// setTimeout(0): defer to next \"tick\" — let browser paint first\r\nsetTimeout(() => {\r\n  renderLargeList(); // after current render cycle\r\n}, 0);</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">63</span><span class=\"q-question\">What is requestAnimationFrame?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>requestAnimationFrame()</code> tells the browser to run your function just before the next screen repaint. It syncs animations with the display refresh rate (usually 60fps) — much better than setInterval for animations.</div>\r\n<pre>// Bad — setInterval for animation\r\nsetInterval(() => {\r\n  element.style.left = position + \"px\";\r\n  position++;\r\n}, 16); // approximately 60fps, but can drift, and runs when hidden\r\n\r\n// Good — requestAnimationFrame\r\nfunction animate() {\r\n  position += 2;\r\n  element.style.left = position + \"px\";\r\n\r\n  if (position < 500) {\r\n    requestAnimationFrame(animate); // schedule next frame\r\n  }\r\n}\r\nrequestAnimationFrame(animate); // start!\r\n\r\n// Benefits vs setInterval:\r\n// ✅ Pauses when tab is hidden (saves battery/CPU)\r\n// ✅ Syncs with display refresh rate (no tearing)\r\n// ✅ No drift — always runs just before paint\r\n// ✅ Given a timestamp for precise timing\r\n\r\n// With timestamp for smooth speed-independent animation\r\nfunction animate(timestamp) {\r\n  const elapsed = timestamp - lastTimestamp;\r\n  position += speed * (elapsed / 1000); // speed in px/second\r\n  lastTimestamp = timestamp;\r\n  requestAnimationFrame(animate);\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">64</span><span class=\"q-question\">What is a Service Worker?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A Service Worker is a background script that acts as a network proxy — it can intercept requests, cache responses, and serve cached content when offline. It's the technology behind Progressive Web Apps (PWAs) and push notifications.</div>\r\n<pre>// Register a service worker (main.js)\r\nif ('serviceWorker' in navigator) {\r\n  navigator.serviceWorker.register('/sw.js').then(reg => {\r\n    console.log('SW registered:', reg);\r\n  });\r\n}\r\n\r\n// Service worker (sw.js)\r\nconst CACHE_NAME = 'my-app-v1';\r\nconst ASSETS = ['/', '/index.html', '/app.js', '/styles.css'];\r\n\r\n// Install — cache essential files\r\nself.addEventListener('install', (event) => {\r\n  event.waitUntil(\r\n    caches.open(CACHE_NAME).then(cache => cache.addAll(ASSETS))\r\n  );\r\n});\r\n\r\n// Fetch — serve from cache first, then network\r\nself.addEventListener('fetch', (event) => {\r\n  event.respondWith(\r\n    caches.match(event.request).then(cached => {\r\n      return cached || fetch(event.request); // cache or network\r\n    })\r\n  );\r\n});\r\n\r\n// Service workers:\r\n// - Run in background, even when page is closed\r\n// - No DOM access\r\n// - Communicate via postMessage or Push API\r\n// - Required for offline, push notifications, background sync</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">65</span><span class=\"q-question\">What are JavaScript iterators and the iterable protocol?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>An <strong>iterator</strong> is an object with a <code>.next()</code> method that returns <code>{value, done}</code>. An <strong>iterable</strong> is an object with <code>[Symbol.iterator]()</code> that returns an iterator. Arrays, Strings, Maps, Sets are built-in iterables. You can make anything iterable.</div>\r\n<pre>// Built-in iterables support for...of, spread, destructuring\r\nfor (const char of \"hello\") console.log(char); // h,e,l,l,o\r\nconst letters = [...'hello']; // ['h','e','l','l','o']\r\n\r\n// Manual iterator\r\nconst range = {\r\n  from: 1, to: 5,\r\n  [Symbol.iterator]() {           // makes this object iterable\r\n    let current = this.from;\r\n    const last = this.to;\r\n    return {\r\n      next() {\r\n        if (current <= last) {\r\n          return { value: current++, done: false };\r\n        }\r\n        return { value: undefined, done: true };\r\n      }\r\n    };\r\n  }\r\n};\r\n\r\nfor (const n of range) console.log(n); // 1, 2, 3, 4, 5\r\n[...range]; // [1, 2, 3, 4, 5]\r\nconst [a, b] = range; // a = 1, b = 2\r\n\r\n// Generator is the easiest way to create an iterator\r\nfunction* range2(from, to) {\r\n  for (let i = from; i <= to; i++) yield i;\r\n}\r\nfor (const n of range2(1, 5)) console.log(n);</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">66</span><span class=\"q-question\">What are async generators and async iterators?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Async generators combine generators and async/await — they produce values asynchronously with <code>yield</code> and use <code>await</code> inside. Consume them with <code>for await...of</code>. Perfect for streaming APIs and paginated data.</div>\r\n<pre>// Async generator — produces values asynchronously\r\nasync function* fetchPaginatedUsers() {\r\n  let page = 1;\r\n  while (true) {\r\n    const res  = await fetch(`/api/users?page=${page}`);\r\n    const data = await res.json();\r\n    if (!data.users.length) break;\r\n    yield data.users; // pause and yield each page\r\n    page++;\r\n  }\r\n}\r\n\r\n// Consume with for await...of\r\nasync function processAllUsers() {\r\n  for await (const users of fetchPaginatedUsers()) {\r\n    users.forEach(user => processUser(user));\r\n    // automatically fetches next page on each iteration\r\n  }\r\n}\r\n\r\n// Streaming file reading (Node.js)\r\nasync function* readFileChunks(path) {\r\n  const stream = fs.createReadStream(path, { highWaterMark: 1024 });\r\n  for await (const chunk of stream) {\r\n    yield chunk;\r\n  }\r\n}\r\n\r\n// ReadableStream in browsers is also an async iterable\r\nconst response = await fetch('/large-file');\r\nfor await (const chunk of response.body) {\r\n  processChunk(chunk);\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">67</span><span class=\"q-question\">What is Object.freeze() vs Object.seal()?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>Object.freeze()</code> makes an object completely immutable — no adding, removing, or changing properties. <code>Object.seal()</code> prevents adding or removing properties but allows changing existing values.</div>\r\n<pre>// Object.freeze — completely immutable\r\nconst config = Object.freeze({\r\n  apiUrl: 'https://api.example.com',\r\n  maxRetries: 3\r\n});\r\n\r\nconfig.apiUrl = 'other';    // silently fails (or throws in strict mode)\r\nconfig.newProp = 'hello';   // silently fails\r\ndelete config.apiUrl;       // silently fails\r\n// config remains unchanged!\r\n\r\n// Object.seal — can change, can't add/remove\r\nconst user = Object.seal({ name: 'Alice', age: 25 });\r\nuser.name = 'Bob';     // ✅ changing allowed\r\nuser.email = 'a@b.c'; // ❌ adding NOT allowed\r\ndelete user.age;       // ❌ deleting NOT allowed\r\n\r\n// Check status\r\nObject.isFrozen(config); // true\r\nObject.isSealed(user);   // true\r\n\r\n// ⚠️ Both are SHALLOW — nested objects are not frozen!\r\nconst nested = Object.freeze({ inner: { value: 42 } });\r\nnested.inner.value = 99; // ✅ still works! nested object not frozen\r\n// For deep freeze, recurse manually or use a library</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">68</span><span class=\"q-question\">What is the difference between == comparison with objects?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Objects are compared by <strong>reference</strong>, not by value. Two different objects with identical content are NOT equal. They're equal only if they point to the exact same object in memory.</div>\r\n<pre>// Objects — compared by REFERENCE (memory address)\r\nconst a = { name: \"Alice\" };\r\nconst b = { name: \"Alice\" };\r\nconst c = a;\r\n\r\na === b;  // false — different objects in memory\r\na === c;  // true  — same reference!\r\na == b;   // false — same result for objects\r\n\r\n// Primitive — compared by VALUE\r\n\"hello\" === \"hello\"; // true\r\n5 === 5;             // true\r\n\r\n// Deep equality — you must compare yourself\r\nfunction deepEqual(obj1, obj2) {\r\n  return JSON.stringify(obj1) === JSON.stringify(obj2);\r\n  // ⚠️ works for simple cases, but:\r\n  // fails for undefined values, functions, Dates, Map/Set\r\n  // key order matters\r\n}\r\n\r\n// Better: use lodash isEqual\r\nimport { isEqual } from 'lodash';\r\nisEqual({ a: 1, b: { c: 2 } }, { a: 1, b: { c: 2 } }); // true ✅\r\n\r\n// Practical impact:\r\nconst arrA = [1, 2, 3];\r\nconst arrB = [1, 2, 3];\r\narrA === arrB;  // false — different arrays!\r\nJSON.stringify(arrA) === JSON.stringify(arrB); // true</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">69</span><span class=\"q-question\">What is the Temporal API (modern date handling)?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The old <code>Date</code> API is notoriously broken. <code>Temporal</code> is the new proposal (stage 3) that fixes everything — immutable, timezone-aware, precise calendar support. Not yet in browsers, but coming soon.</div>\r\n<pre\">// Old Date API — broken in many ways\r\nconst d = new Date(2024, 0, 15); // January (0-indexed, confusing!)\r\nd.getMonth(); // 0 — January is 0?!\r\nnew Date(\"2024-01-15\") // parsed in UTC\r\nnew Date(\"01/15/2024\") // parsed in local time — different!\r\nd.setMonth(1); // mutates the date — unexpected!\r\n\r\n// Temporal API — fixes everything\r\nconst today = Temporal.Now.plainDateISO();\r\ntoday.year;  today.month;  today.day;  // 1-indexed months!\r\n\r\n// Immutable — always returns new objects\r\nconst date = Temporal.PlainDate.from('2024-01-15');\r\nconst nextMonth = date.add({ months: 1 });  // returns NEW date\r\ndate.toString(); // \"2024-01-15\" — unchanged\r\n\r\n// Time zones done right\r\nconst zonedDateTime = Temporal.ZonedDateTime.from(\r\n  '2024-01-15T10:00:00[America/New_York]'\r\n);\r\nconst inTokyo = zonedDateTime.withTimeZone('Asia/Tokyo');\r\n\r\n// Duration\r\nconst duration = Temporal.Duration.from({ hours: 2, minutes: 30 });\r\nconst meeting = start.add(duration);\r\n\r\n// Until polyfill lands: use date-fns or day.js</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">70</span><span class=\"q-question\">What are tagged template literals?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Tagged templates let you process a template literal with a function. The function receives the string parts and interpolated values separately, letting you transform the output. Used by styled-components, GraphQL (gql), and SQL libraries.</div>\r\n<pre\">// Tag function receives: (strings, ...values)\r\nfunction highlight(strings, ...values) {\r\n  return strings.reduce((result, str, i) => {\r\n    const value = values[i - 1];\r\n    return result + (value ? `&lt;mark&gt;${value}&lt;/mark&gt;` : '') + str;\r\n  });\r\n}\r\n\r\nconst name = \"Alice\";\r\nconst role = \"admin\";\r\nhighlight`User ${name} has ${role} access`;\r\n// \"User &lt;mark&gt;Alice&lt;/mark&gt; has &lt;mark&gt;admin&lt;/mark&gt; access\"\r\n\r\n// Real-world: GraphQL\r\nimport gql from 'graphql-tag';\r\nconst QUERY = gql`\r\n  query GetUser($id: ID!) {\r\n    user(id: $id) { name email }\r\n  }\r\n`;\r\n\r\n// Real-world: styled-components\r\nconst Button = styled.button`\r\n  background: ${props => props.primary ? 'blue' : 'white'};\r\n  padding: 8px 16px;\r\n`;\r\n\r\n// SQL safe queries (prevent injection)\r\nfunction sql(strings, ...values) {\r\n  return { query: strings.join('?'), params: values };\r\n}\r\nconst { query, params } = sql`SELECT * FROM users WHERE id = ${userId}`;</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">71</span><span class=\"q-question\">What is the structuredClone() function?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>structuredClone()</code> creates a deep clone of an object — fully independent copy with no shared references. It's the native deep clone function added in ES2022, replacing the hacky JSON.parse/JSON.stringify workaround.</div>\r\n<pre>const original = {\r\n  name: \"Alice\",\r\n  address: { city: \"NYC\" },\r\n  tags: [\"dev\", \"admin\"],\r\n  createdAt: new Date(),     // ✅ handles Dates\r\n  data: new Map([[1, 'a']]), // ✅ handles Map\r\n  scores: new Set([1, 2])    // ✅ handles Set\r\n};\r\n\r\n// Old hack — BROKEN\r\nconst broken = JSON.parse(JSON.stringify(original));\r\n// ❌ Dates become strings\r\n// ❌ Map/Set become empty objects\r\n// ❌ undefined values disappear\r\n// ❌ Functions disappear\r\n\r\n// structuredClone — proper deep clone\r\nconst clone = structuredClone(original);\r\nclone.address.city = \"LA\";   // ✅ original.address.city stays \"NYC\"\r\nclone.createdAt instanceof Date; // true ✅\r\n\r\n// Limitations:\r\n// ❌ Cannot clone functions\r\n// ❌ Cannot clone class instances (loses prototype)\r\n// ❌ Cannot clone DOM nodes\r\n\r\n// For functions/classes: use lodash.cloneDeep()</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">72</span><span class=\"q-question\">What is Array.from() and when is it useful?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>Array.from()</code> creates a real array from array-like or iterable objects (NodeList, arguments, string, Map, Set). Optionally applies a mapping function to each element.</div>\r\n<pre\">// Convert NodeList to array (so you can use .map, .filter etc.)\r\nconst elements = Array.from(document.querySelectorAll('.card'));\r\nelements.map(el => el.textContent); // ✅ now works!\r\n\r\n// Convert string to array of characters\r\nArray.from(\"hello\");  // ['h','e','l','l','o']\r\n// Same as: [...\"hello\"]\r\n\r\n// Convert Set to array\r\nArray.from(new Set([1, 2, 2, 3])); // [1, 2, 3]\r\n\r\n// Convert Map entries\r\nArray.from(new Map([['a',1],['b',2]])); // [['a',1], ['b',2]]\r\n\r\n// With a mapping function — Array.from(iterable, mapFn)\r\nArray.from({ length: 5 }, (_, i) => i * 2); // [0, 2, 4, 6, 8]\r\nArray.from({ length: 5 }, (_, i) => i + 1); // [1, 2, 3, 4, 5]\r\n\r\n// Create array of N items\r\nArray.from({ length: 10 }, () => Math.random());  // 10 random numbers\r\n\r\n// Convert arguments object to real array\r\nfunction toArray() {\r\n  return Array.from(arguments); // arguments is array-LIKE, not array\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">73</span><span class=\"q-question\">What is the logical assignment operators (&&=, ||=, ??=)?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>ES2021 added three assignment operators that combine logical operators with assignment. They're shorthand that only assigns if the left side meets a condition — avoiding unnecessary writes.</div>\r\n<pre\">// ||= — assign if left side is falsy\r\nlet title = \"\";\r\ntitle ||= \"Untitled\";  // title = \"Untitled\" (was falsy)\r\n\r\nlet count = 5;\r\ncount ||= 10; // count stays 5 (was truthy)\r\n\r\n// ??= — assign if left side is null/undefined (nullish)\r\nlet name = null;\r\nname ??= \"Guest\";    // name = \"Guest\"\r\n\r\nlet age = 0;\r\nage ??= 18;          // age stays 0 (not null/undefined!)\r\n// Use ??= over ||= when 0 and \"\" are valid values\r\n\r\n// &&= — assign if left side is truthy\r\nlet user = { name: \"Alice\", isAdmin: false };\r\nuser.isAdmin &&= checkPermissions(user); // only runs if isAdmin is truthy\r\n\r\n// Before (verbose):\r\ntitle = title || \"Untitled\";    // same as ||=\r\nname = name ?? \"Guest\";         // same as ??=\r\nif (user.isAdmin) user.isAdmin = checkPermissions(user); // same as &&=</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">74</span><span class=\"q-question\">What are the new Array methods in modern JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Recent JavaScript versions added several useful array methods: <code>at()</code>, <code>toSorted()</code>, <code>toReversed()</code>, <code>toSpliced()</code>, <code>with()</code>, and <code>findLast()</code>. The \"to\" methods return new arrays — they don't mutate the original.</div>\r\n<pre\">const arr = [1, 2, 3, 4, 5];\r\n\r\n// at() — access by index, supports negative (from end)\r\narr.at(0);    // 1  (same as arr[0])\r\narr.at(-1);   // 5  (last element!)\r\narr.at(-2);   // 4  (second to last)\r\n// arr[-1] would be undefined (objects don't have negative keys)\r\n\r\n// findLast() — find last matching element\r\narr.findLast(n => n % 2 === 0);      // 4 (last even)\r\narr.findLastIndex(n => n % 2 === 0); // 3 (index of last even)\r\n\r\n// toSorted() — new sorted array (original unchanged)\r\nconst sorted = arr.toSorted((a, b) => b - a); // [5,4,3,2,1]\r\n// arr.sort() would MUTATE arr — toSorted() doesn't!\r\n\r\n// toReversed() — new reversed array (original unchanged)\r\nconst reversed = arr.toReversed(); // [5,4,3,2,1]\r\n\r\n// toSpliced() — new array with elements replaced\r\nconst spliced = arr.toSpliced(2, 1, 99); // [1,2,99,4,5]\r\n\r\n// with() — new array with one element replaced\r\nconst withNew = arr.with(2, 99); // [1,2,99,4,5]</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">75</span><span class=\"q-question\">What is Object.create() and how is it different from new?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>Object.create(proto)</code> creates a new object with the specified object as its prototype. Unlike <code>new</code>, it doesn't call a constructor function. Useful for creating clean prototype chains.</div>\r\n<pre\">// Object.create — direct prototype chain control\r\nconst animal = {\r\n  breathe() { console.log(`${this.name} breathes`); }\r\n};\r\n\r\nconst dog = Object.create(animal); // animal is dog's prototype\r\ndog.name = \"Rex\";\r\ndog.breathe(); // \"Rex breathes\" — found on animal prototype\r\n\r\n// Create with null prototype (no inherited properties!)\r\nconst cleanObj = Object.create(null);\r\ncleanObj.key = \"value\";\r\ncleanObj.hasOwnProperty; // undefined — no Object.prototype!\r\n// Useful for dictionaries where you want NO inherited properties\r\n\r\n// Object.create vs new:\r\nfunction Person(name) { this.name = name; }\r\nPerson.prototype.greet = function() { return `Hi, I'm ${this.name}`; };\r\n\r\n// new — calls constructor, sets prototype\r\nconst alice = new Person(\"Alice\");\r\n\r\n// Object.create — no constructor, you set properties manually\r\nconst bob = Object.create(Person.prototype);\r\nbob.name = \"Bob\"; // must set manually\r\nbob.greet(); // \"Hi, I'm Bob\" — prototype chain works\r\n\r\n// Both: alice.__proto__ === bob.__proto__ === Person.prototype</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">76</span><span class=\"q-question\">What is the typeof operator and its quirks?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>typeof</code> returns a string describing the type of a value. It has a few famous quirks that trip up developers — especially <code>typeof null === \"object\"</code> which is a historic bug.</div>\r\n<pre\">typeof \"hello\"      // \"string\"\r\ntypeof 42           // \"number\"\r\ntypeof 3.14         // \"number\"  (no separate float type)\r\ntypeof true         // \"boolean\"\r\ntypeof undefined    // \"undefined\"\r\ntypeof Symbol()     // \"symbol\"\r\ntypeof 42n          // \"bigint\"\r\ntypeof {}           // \"object\"\r\ntypeof []           // \"object\"  ← arrays are objects!\r\ntypeof null         // \"object\"  ← FAMOUS BUG! null is not an object\r\ntypeof function(){} // \"function\" ← special case for functions\r\n\r\n// Check for null safely:\r\nvalue === null       // ✅ correct way\r\n\r\n// Check for array:\r\nArray.isArray([]);    // ✅ true\r\nArray.isArray({});    // false\r\n\r\n// Check for object (not null, not array):\r\ntypeof value === 'object' && value !== null && !Array.isArray(value)\r\n\r\n// typeof on undeclared variables (unique — doesn't throw ReferenceError)\r\ntypeof undeclaredVar; // \"undefined\" — safe!\r\nundeclaredVar;        // ❌ ReferenceError</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">77</span><span class=\"q-question\">What is the instanceof operator and how does it work?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>instanceof</code> checks whether an object has a constructor's prototype anywhere in its prototype chain. It walks up the chain until it finds a match or reaches <code>null</code>.</div>\r\n<pre\">class Animal {}\r\nclass Dog extends Animal {}\r\n\r\nconst rex = new Dog();\r\n\r\nrex instanceof Dog;     // true — Dog.prototype in chain\r\nrex instanceof Animal;  // true — Animal.prototype in chain\r\nrex instanceof Object;  // true — Object.prototype always in chain\r\nrex instanceof Array;   // false — Array.prototype not in chain\r\n\r\n// How it works:\r\n// rex.__proto__ === Dog.prototype      → match → true\r\n// rex.__proto__.__proto__ === Animal.prototype → match → true\r\n\r\n// instanceof with arrays:\r\n[] instanceof Array;    // true\r\n[] instanceof Object;   // true (arrays are objects)\r\n\r\n// Gotcha: fails across different windows/frames (iframes)\r\n// because they have different Array constructors\r\n// Use Array.isArray() instead for arrays\r\n\r\n// Custom instanceof with [Symbol.hasInstance]\r\nclass Even {\r\n  static [Symbol.hasInstance](num) {\r\n    return Number.isInteger(num) && num % 2 === 0;\r\n  }\r\n}\r\n2 instanceof Even;  // true\r\n3 instanceof Even;  // false</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">78</span><span class=\"q-question\">What are getters and setters in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Getters and setters are special methods accessed like properties. They let you add logic when a property is read (<code>get</code>) or written (<code>set</code>) — for validation, computed values, or logging — without changing the calling syntax.</div>\r\n<pre\">class Temperature {\r\n  #celsius;\r\n\r\n  constructor(celsius) { this.#celsius = celsius; }\r\n\r\n  get celsius() { return this.#celsius; }\r\n\r\n  set celsius(value) {\r\n    if (value < -273.15) throw new RangeError('Below absolute zero!');\r\n    this.#celsius = value;\r\n  }\r\n\r\n  get fahrenheit() {      // computed property — no stored value\r\n    return this.#celsius * 9/5 + 32;\r\n  }\r\n\r\n  set fahrenheit(value) { // convert and store as celsius\r\n    this.celsius = (value - 32) * 5/9;\r\n  }\r\n}\r\n\r\nconst temp = new Temperature(25);\r\ntemp.celsius;      // 25  (calls getter)\r\ntemp.fahrenheit;   // 77  (computed!)\r\ntemp.celsius = -300; // ❌ RangeError: Below absolute zero!\r\ntemp.fahrenheit = 212; // sets celsius to 100\r\n\r\n// Object literal syntax\r\nconst circle = {\r\n  radius: 5,\r\n  get area() { return Math.PI * this.radius ** 2; },\r\n  get circumference() { return 2 * Math.PI * this.radius; }\r\n};</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">79</span><span class=\"q-question\">What are the most common JavaScript interview coding challenges?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Common Tasks in Interviews</span></div>\r\n<pre\">// 1. Flatten a nested array\r\nconst flat = arr => arr.reduce((acc, val) =>\r\n  Array.isArray(val) ? [...acc, ...flat(val)] : [...acc, val], []);\r\nflat([1, [2, [3, [4]]]]); // [1,2,3,4]\r\n// Or: arr.flat(Infinity)\r\n\r\n// 2. Remove duplicates from array\r\nconst unique = arr => [...new Set(arr)];\r\n\r\n// 3. Deep clone an object\r\nconst clone = obj => structuredClone(obj);\r\n\r\n// 4. Flatten an object\r\nfunction flattenObj(obj, prefix = '') {\r\n  return Object.keys(obj).reduce((acc, key) => {\r\n    const val = obj[key];\r\n    const newKey = prefix ? `${prefix}.${key}` : key;\r\n    return typeof val === 'object' && val !== null\r\n      ? { ...acc, ...flattenObj(val, newKey) }\r\n      : { ...acc, [newKey]: val };\r\n  }, {});\r\n}\r\nflattenObj({a:{b:{c:1}}}); // {'a.b.c': 1}\r\n\r\n// 5. Group array of objects by a key\r\nconst groupBy = (arr, key) =>\r\n  arr.reduce((groups, item) => ({\r\n    ...groups, [item[key]]: [...(groups[item[key]] || []), item]\r\n  }), {});\r\n\r\n// 6. Implement Promise.all\r\nconst promiseAll = promises =>\r\n  new Promise((resolve, reject) => {\r\n    const results = [];\r\n    let done = 0;\r\n    promises.forEach((p, i) =>\r\n      Promise.resolve(p).then(val => {\r\n        results[i] = val;\r\n        if (++done === promises.length) resolve(results);\r\n      }).catch(reject));\r\n  });</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">80</span><span class=\"q-question\">What are the differences between CommonJS and ES Modules?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><strong>CommonJS</strong> (require/module.exports) is Node.js's original module system — synchronous, dynamic, loaded at runtime. <strong>ES Modules</strong> (import/export) is the official JavaScript standard — static, async, loaded at parse time. Modern Node.js and browsers both support ESM.</div>\r\n<pre\">// CommonJS (CJS) — Node.js original, .js or .cjs\r\nconst fs = require('fs');\r\nconst { add } = require('./math');\r\nmodule.exports = { myFunction };\r\nmodule.exports.default = mainFunction;\r\n\r\n// Characteristics:\r\n// - Synchronous (require blocks)\r\n// - Dynamic (can require() inside if/loops)\r\n// - Module code runs when first required\r\n// - exports is a reference (can change it)\r\n\r\n// ES Modules (ESM) — standard, .js with \"type\":\"module\" or .mjs\r\nimport fs from 'fs';\r\nimport { add } from './math.js';  // .js extension required in Node ESM!\r\nexport { myFunction };\r\nexport default mainFunction;\r\n\r\n// Characteristics:\r\n// - Asynchronous (loaded in parallel)\r\n// - Static (imports hoisted, analyzed at parse time)\r\n// - Tree-shakeable (unused exports removed by bundlers)\r\n// - import.meta.url — current module's URL\r\n// - Top-level await available\r\n\r\n// Key difference:\r\n// CJS: require() anywhere, even in functions\r\n// ESM: import must be at top level (static)\r\n// ESM: supports dynamic import() function for lazy loading</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">81</span><span class=\"q-question\">What is tree shaking in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Tree shaking is when a bundler (like Webpack or Rollup) removes unused JavaScript code from the final bundle. It works with ES Module static imports — bundlers can analyze exactly what's used and exclude the rest.</div>\r\n<pre\">// utils.js — exports 3 functions\r\nexport function add(a, b) { return a + b; }\r\nexport function subtract(a, b) { return a - b; }\r\nexport function multiply(a, b) { return a * b; }  // 100KB complex function\r\n\r\n// app.js — only imports add\r\nimport { add } from './utils.js';\r\nadd(1, 2);\r\n\r\n// After tree shaking:\r\n// multiply and subtract are NOT in the final bundle!\r\n// Result: smaller bundle = faster loading\r\n\r\n// Requirements for tree shaking:\r\n// ✅ ES Modules (static imports)\r\n// ✅ Named exports (not \"export default { everything }\")\r\n// ✅ Pure functions (no side effects)\r\n// ✅ Bundler configured correctly (Webpack, Rollup, Vite)\r\n\r\n// Why CommonJS can't be tree shaken:\r\nconst utils = require('./utils'); // which parts? unknown until runtime\r\nutils.add(1, 2); // bundler must include ALL of utils\r\n\r\n// Writing tree-shakeable libraries:\r\n// ❌ export default { add, subtract, multiply } — whole object bundled\r\n// ✅ export { add } from './add.js'; — individual exports</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">82</span><span class=\"q-question\">What is a JavaScript engine and how does V8 work?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A JavaScript engine parses and executes JS code. V8 (Chrome, Node.js) compiles JS to machine code using JIT (Just-In-Time) compilation — hot code is optimized on the fly for near-native speed.</div>\r\n<div class=\"example-box\"><span class=\"lbl\">📋 V8 Execution Pipeline</span><strong>1. Parse</strong>: Source code → AST (Abstract Syntax Tree)<br><strong>2. Ignition</strong>: AST → Bytecode (interpreted, fast startup)<br><strong>3. Profiling</strong>: Track \"hot\" functions (called many times)<br><strong>4. TurboFan</strong>: Hot code → optimized machine code (fast execution)<br><strong>5. Deoptimize</strong>: If assumptions fail (type changed), fall back to bytecode</div>\r\n<pre\">// V8 optimization tips:\r\n// 1. Don't change object shapes (add properties in constructor)\r\nfunction Person(name, age) {\r\n  this.name = name;  // ✅ consistent shape\r\n  this.age = age;\r\n}\r\n// ❌ Adding properties after construction creates different \"hidden class\"\r\nconst p = new Person(\"Alice\", 25);\r\np.email = \"alice@example.com\"; // shape changed — deoptimizes\r\n\r\n// 2. Don't change types of variables\r\nlet x = 1;     // V8 treats as int\r\nx = \"hello\";   // ❌ type changed — deoptimize!\r\n\r\n// 3. Use typed arrays for numeric data\r\nconst data = new Float64Array(1000); // V8 optimizes heavily\r\n\r\n// 4. Functions called with consistent argument types are faster</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">83</span><span class=\"q-question\">What is JSON and how do you work with it in JavaScript?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>JSON (JavaScript Object Notation) is a text format for storing and transporting data. <code>JSON.stringify()</code> converts a JS value to a JSON string. <code>JSON.parse()</code> converts a JSON string back to a JS value.</div>\r\n<pre\">const user = { name: \"Alice\", age: 25, active: true };\r\n\r\n// Object → JSON string\r\nconst json = JSON.stringify(user);\r\n// '{\"name\":\"Alice\",\"age\":25,\"active\":true}'\r\n\r\n// Pretty print\r\nJSON.stringify(user, null, 2);\r\n// {\r\n//   \"name\": \"Alice\",\r\n//   ...\r\n// }\r\n\r\n// Filter properties (replacer)\r\nJSON.stringify(user, ['name', 'age']); // only these keys\r\n\r\n// JSON string → Object\r\nconst parsed = JSON.parse(json);\r\nparsed.name; // \"Alice\"\r\n\r\n// Limitations — these are NOT valid JSON:\r\n// undefined, Functions, Symbols → omitted/lost\r\n// Date objects → become strings\r\n// Map/Set → become {}\r\n// NaN, Infinity → become null\r\n\r\n// Always wrap in try-catch!\r\ntry {\r\n  const data = JSON.parse(untrustedInput);\r\n} catch (e) {\r\n  console.error(\"Invalid JSON:\", e.message);\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">84</span><span class=\"q-question\">What is the Intl API for internationalization?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The <code>Intl</code> API provides locale-sensitive formatting for dates, numbers, currencies, and relative time — no external library needed. Works for any language and region.</div>\r\n<pre\">// Number formatting\r\nnew Intl.NumberFormat('en-US').format(1234567.89);\r\n// \"1,234,567.89\"\r\nnew Intl.NumberFormat('de-DE').format(1234567.89);\r\n// \"1.234.567,89\" (German uses dots for thousands, commas for decimal)\r\n\r\n// Currency\r\nnew Intl.NumberFormat('en-US', {\r\n  style: 'currency', currency: 'USD'\r\n}).format(99.5); // \"$99.50\"\r\n\r\n// Date formatting\r\nnew Intl.DateTimeFormat('en-US', {\r\n  dateStyle: 'long', timeStyle: 'short'\r\n}).format(new Date()); // \"April 3, 2026 at 2:30 PM\"\r\n\r\n// Relative time — \"3 days ago\", \"in 2 hours\"\r\nconst rtf = new Intl.RelativeTimeFormat('en', { numeric: 'auto' });\r\nrtf.format(-3, 'day');  // \"3 days ago\"\r\nrtf.format(2, 'hour');  // \"in 2 hours\"\r\nrtf.format(-1, 'day');  // \"yesterday\"\r\n\r\n// Pluralization\r\nconst pr = new Intl.PluralRules('en');\r\npr.select(1); // \"one\" → use \"1 item\"\r\npr.select(5); // \"other\" → use \"5 items\"</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">85</span><span class=\"q-question\">What is eval() and why should you avoid it?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>eval()</code> executes a string as JavaScript code. It's considered extremely dangerous and should never be used in production. It opens XSS vulnerabilities and prevents JavaScript engine optimizations.</div>\r\n<pre\">// eval() executes a string as code\r\neval(\"console.log('hello')\"); // prints \"hello\"\r\neval(\"1 + 2\"); // 3\r\n\r\n// Why it's dangerous:\r\nconst userInput = \"alert('hacked!')\";\r\neval(userInput); // ❌ executes user input as code — XSS attack!\r\n\r\n// Why it's slow:\r\n// V8 can't optimize code inside eval — unknown at parse time\r\n// eval has access to local scope — entire function can't be optimized\r\n\r\n// Common mistake — using eval for JSON:\r\neval('(' + jsonString + ')'); // ❌ DANGEROUS + slow\r\nJSON.parse(jsonString);       // ✅ safe and fast\r\n\r\n// Almost always, eval can be replaced:\r\n// Dynamic property access:\r\neval(\"obj.\" + key);   // ❌\r\nobj[key];             // ✅\r\n\r\n// Dynamic function call:\r\neval(funcName + \"()\"); // ❌\r\nwindow[funcName]();    // ✅ (browser) or\r\nfunctionMap[name]();   // ✅ (use a map)\r\n\r\n// \"eval is evil\" — never use it. If you think you need it,\r\n// there's always a better, safer way.</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">86</span><span class=\"q-question\">What is strict mode in JavaScript?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Strict mode (<code>'use strict'</code>) enables a stricter variant of JavaScript that catches common coding mistakes and throws errors instead of silently ignoring them. ES6 modules are always in strict mode automatically.</div>\r\n<pre\">'use strict'; // at top of file or function\r\n\r\n// 1. Variables must be declared\r\nx = 5; // ❌ ReferenceError — without strict: creates global!\r\n\r\n// 2. Can't delete non-deletable properties\r\ndelete Object.prototype; // ❌ TypeError\r\n\r\n// 3. Duplicate parameter names are forbidden\r\nfunction add(a, a) {} // ❌ SyntaxError\r\n\r\n// 4. 'this' is undefined in regular functions (not window)\r\nfunction test() { console.log(this); } // undefined (not window)\r\n\r\n// 5. No octal literals\r\nconst n = 010; // ❌ SyntaxError (0-prefix octal)\r\nconst n = 0o10; // ✅ modern octal syntax\r\n\r\n// 6. No writing to read-only properties\r\nconst obj = {};\r\nObject.defineProperty(obj, 'x', { value: 42, writable: false });\r\nobj.x = 100; // ❌ TypeError (silently fails without strict mode)\r\n\r\n// 7. eval has its own scope\r\neval(\"var evalVar = 1\");\r\ntypeof evalVar; // \"undefined\" in strict mode\r\n// In non-strict: evalVar would leak into outer scope!\r\n\r\n// ES6 modules are ALWAYS strict — no need to add 'use strict'</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">87</span><span class=\"q-question\">What is the difference between for...of and for...in?</span><span class=\"badge b-easy\">Easy</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span><code>for...of</code> iterates over the <strong>values</strong> of any iterable (arrays, strings, Maps, Sets). <code>for...in</code> iterates over the <strong>keys/property names</strong> of an object (including inherited ones — a pitfall).</div>\r\n<pre\">// for...of — values (works with any iterable)\r\nfor (const value of [1, 2, 3]) console.log(value); // 1, 2, 3\r\nfor (const char of \"hello\") console.log(char);      // h,e,l,l,o\r\nfor (const [key, val] of new Map([['a',1]])) {}     // entries\r\nfor (const item of new Set([1,2,3])) {}             // unique values\r\n\r\n// for...in — property NAMES (keys)\r\nconst obj = { a: 1, b: 2, c: 3 };\r\nfor (const key in obj) console.log(key); // \"a\", \"b\", \"c\"\r\n\r\n// for...in DANGER — also iterates inherited properties!\r\nfunction Person(name) { this.name = name; }\r\nPerson.prototype.species = \"human\"; // inherited property\r\n\r\nconst alice = new Person(\"Alice\");\r\nfor (const key in alice) console.log(key); // \"name\", \"species\" ← unwanted!\r\n\r\n// Fix — filter with hasOwnProperty\r\nfor (const key in alice) {\r\n  if (Object.hasOwn(alice, key)) console.log(key); // only \"name\"\r\n}\r\n\r\n// Don't use for...in on arrays — use for...of or forEach instead\r\n// for...in on array gives string indices and may include prototype properties</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"medium\">\r\n<div class=\"q-top\"><span class=\"q-num\">88</span><span class=\"q-question\">What is the difference between synchronous and asynchronous iteration?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>Synchronous iteration processes items one at a time immediately. Asynchronous iteration (with <code>for await...of</code>) waits for each item's Promise to resolve before moving to the next — perfect for paginated APIs, streams, and async data sources.</div>\r\n<pre\">// Sync iteration — immediate values\r\nfor (const item of [1, 2, 3]) {\r\n  process(item); // each item available now\r\n}\r\n\r\n// Async iteration — wait for each value\r\nasync function processStream() {\r\n  for await (const chunk of readableStream) {\r\n    process(chunk); // waits for each chunk\r\n  }\r\n}\r\n\r\n// When to use for await...of:\r\n// 1. AsyncGenerator results\r\nasync function* getUsers() {\r\n  for (const id of userIds) {\r\n    yield await fetchUser(id); // each user fetched one by one\r\n  }\r\n}\r\nfor await (const user of getUsers()) { display(user); }\r\n\r\n// 2. ReadableStream (fetch body, File reader)\r\nconst response = await fetch('/large-file.json');\r\nfor await (const chunk of response.body) { accumulate(chunk); }\r\n\r\n// 3. Database cursors (Mongoose, etc.)\r\nfor await (const doc of User.find().cursor()) { process(doc); }</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">89</span><span class=\"q-question\">What are the most important JavaScript performance tips?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Performance Best Practices</span></div>\r\n<div class=\"example-box\"><span class=\"lbl\">📋 Top Performance Tips</span>\r\n<strong>1. Avoid layout thrashing</strong> — batch DOM reads and writes separately. Alternating read/write forces reflow each time.<br><br>\r\n<strong>2. Use DocumentFragment for bulk DOM inserts</strong> — one reflow instead of N..<br><br>\r\n<strong>3. Debounce/throttle event handlers</strong> — scroll, resize, input events fire hundreds of times per second.<br><br>\r\n<strong>4. Use virtual scrolling for long lists</strong> — only render visible items (React Window, TanStack Virtual).<br><br>\r\n<strong>5. Lazy load code</strong> — dynamic import() to load code only when needed.<br><br>\r\n<strong>6. Memoize expensive computations</strong> — don't recalculate what hasn't changed.<br><br>\r\n<strong>7. Use Web Workers for heavy computation</strong> — keep the main thread free for UI.<br><br>\r\n<strong>8. Avoid memory leaks</strong> — removeEventListener, cancel timers, clear WeakMap entries.\r\n</div>\r\n<pre\">// Layout thrashing — BAD\r\nelements.forEach(el => {\r\n  const height = el.offsetHeight; // READ (triggers reflow)\r\n  el.style.height = height + 10 + 'px'; // WRITE (invalidates layout)\r\n}); // N reflows!\r\n\r\n// Batched — GOOD\r\nconst heights = elements.map(el => el.offsetHeight); // all reads\r\nelements.forEach((el, i) => el.style.height = heights[i] + 10 + 'px'); // all writes\r\n// 1 reflow!\r\n\r\n// DocumentFragment for bulk inserts\r\nconst fragment = document.createDocumentFragment();\r\nitems.forEach(item => {\r\n  const li = document.createElement('li');\r\n  li.textContent = item;\r\n  fragment.appendChild(li); // no reflow yet\r\n});\r\nlist.appendChild(fragment); // 1 reflow!</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">90</span><span class=\"q-question\">What are the common JavaScript security vulnerabilities?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Key Security Risks</span></div>\r\n<div class=\"example-box\"><span class=\"lbl\">📋 XSS, Injection & More</span>\r\n<strong>1. XSS (Cross-Site Scripting)</strong> — injecting scripts via user input. Never use innerHTML with user data. Use textContent instead.\r\n</div>\r\n<pre\">// XSS vulnerability\r\nel.innerHTML = userInput; // ❌ if input is \"&lt;script&gt;evil()&lt;/script&gt;\"\r\nel.textContent = userInput; // ✅ always safe (renders as text, not HTML)\r\n\r\n// Content Security Policy header also prevents XSS\r\n// Content-Security-Policy: script-src 'self'\r\n\r\n// Prototype pollution\r\nconst obj = {};\r\nJSON.parse('{\"__proto__\": {\"polluted\": true}}');\r\n// Some merge utilities set obj.__proto__.polluted = true\r\n// Fix: use Object.create(null) for merge targets\r\n// or explicitly block __proto__, constructor, prototype keys\r\n\r\n// CSRF — use SameSite cookies and CSRF tokens\r\n// Avoid storing sensitive data in localStorage (XSS accessible)\r\n// Use HttpOnly, Secure, SameSite=Strict cookies for auth tokens\r\n\r\n// eval and new Function with user input — NEVER\r\n// JSON.parse is safe, eval() is not\r\n\r\n// ReDoS (Regex Denial of Service)\r\n// /^(a+)+$/ on \"aaaaaaaaab\" takes exponential time\r\n// Test regexes with redos-checker tools</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"easy\">\r\n<div class=\"q-top\"><span class=\"q-num\">91</span><span class=\"q-question\">What are common JavaScript coding interview patterns?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Patterns You Must Know</span></div>\r\n<pre\">// 1. Two-pointer technique\r\nfunction isPalindrome(str) {\r\n  let left = 0, right = str.length - 1;\r\n  while (left < right) {\r\n    if (str[left++] !== str[right--]) return false;\r\n  }\r\n  return true;\r\n}\r\n\r\n// 2. Sliding window\r\nfunction maxSubArraySum(arr, k) {\r\n  let sum = arr.slice(0, k).reduce((a, b) => a + b, 0);\r\n  let max = sum;\r\n  for (let i = k; i < arr.length; i++) {\r\n    sum = sum - arr[i - k] + arr[i]; // slide the window\r\n    max = Math.max(max, sum);\r\n  }\r\n  return max;\r\n}\r\n\r\n// 3. Frequency map\r\nfunction twoSum(nums, target) {\r\n  const seen = new Map();\r\n  for (const [i, num] of nums.entries()) {\r\n    if (seen.has(target - num)) return [seen.get(target - num), i];\r\n    seen.set(num, i);\r\n  }\r\n}\r\n\r\n// 4. Recursive tree traversal\r\nfunction sumTree(node) {\r\n  if (!node) return 0;\r\n  return node.value + sumTree(node.left) + sumTree(node.right);\r\n}\r\n\r\n// 5. BFS with queue\r\nfunction bfs(root) {\r\n  const queue = [root], result = [];\r\n  while (queue.length) {\r\n    const node = queue.shift();\r\n    result.push(node.value);\r\n    if (node.left) queue.push(node.left);\r\n    if (node.right) queue.push(node.right);\r\n  }\r\n  return result;\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">92</span><span class=\"q-question\">How does JavaScript handle numbers and what is floating point precision?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>JavaScript uses 64-bit floating point (IEEE 754) for all numbers. This means some decimal operations produce unexpected results. It also means there's a maximum safe integer beyond which precision is lost.</div>\r\n<pre\">// The famous floating point problem\r\n0.1 + 0.2 === 0.3; // false!\r\n0.1 + 0.2;  // 0.30000000000000004\r\n\r\n// Fix: round to reasonable precision\r\n(0.1 + 0.2).toFixed(2) === \"0.30\"; // \"0.30\" (string!)\r\nMath.abs(0.1 + 0.2 - 0.3) < Number.EPSILON; // true ✅\r\n\r\n// For money — use integers (cents) or a library like decimal.js\r\nconst price = 9.99; // ❌ floating point issues\r\nconst priceInCents = 999; // ✅ integer arithmetic is exact\r\n\r\n// Safe integer limit\r\nNumber.MAX_SAFE_INTEGER; // 9007199254740991 (2^53 - 1)\r\n9007199254740991 + 1 === 9007199254740991 + 2; // true (precision lost!)\r\n\r\n// For large integers — use BigInt\r\nconst big = 9007199254740992n;   // BigInt\r\nbig + 1n; // exactly 9007199254740993n\r\n\r\n// Useful number constants\r\nNumber.MAX_VALUE;       // largest positive number\r\nNumber.MIN_VALUE;       // smallest positive number (near 0)\r\nNumber.POSITIVE_INFINITY;\r\nNumber.NEGATIVE_INFINITY;\r\nNumber.EPSILON;         // smallest difference (2.2e-16)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">93</span><span class=\"q-question\">What is the Revealing Module pattern?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The Revealing Module pattern is a variation of the Module pattern where all functions are defined privately and only selected ones are exposed in the returned object. Makes it very clear what's public vs private.</div>\r\n<pre\">const Calculator = (function() {\r\n  // All private\r\n  let history = [];\r\n\r\n  function add(a, b) {\r\n    const result = a + b;\r\n    history.push(`${a} + ${b} = ${result}`);\r\n    return result;\r\n  }\r\n\r\n  function subtract(a, b) {\r\n    const result = a - b;\r\n    history.push(`${a} - ${b} = ${result}`);\r\n    return result;\r\n  }\r\n\r\n  function getHistory() {\r\n    return [...history]; // return copy\r\n  }\r\n\r\n  function clearHistory() {\r\n    history = [];\r\n  }\r\n\r\n  // REVEAL — only expose what should be public\r\n  return { add, subtract, getHistory, clearHistory };\r\n  // multiply (if it existed) would stay private by not including it here\r\n})();\r\n\r\nCalculator.add(5, 3);      // 8\r\nCalculator.getHistory();   // [\"5 + 3 = 8\"]\r\nCalculator.history;        // undefined — private!</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">94</span><span class=\"q-question\">What are the differences between Promise and Observable?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>A Promise handles ONE future value and is eager (starts immediately). An Observable handles MULTIPLE values over time, is lazy (starts only when subscribed), and can be cancelled. Observables are from the RxJS library, popular in Angular.</div>\r\n<pre\">// Promise — single value, eager, not cancellable\r\nconst promise = new Promise(resolve => {\r\n  console.log(\"Starts immediately!\"); // runs NOW\r\n  setTimeout(() => resolve(\"done\"), 1000);\r\n});\r\n// Already started — can't cancel\r\npromise.then(val => console.log(val));\r\n\r\n// Observable — multiple values, lazy, cancellable\r\nimport { Observable } from 'rxjs';\r\nconst observable = new Observable(subscriber => {\r\n  console.log(\"Starts when subscribed!\"); // NOT yet\r\n  const id = setInterval(() => subscriber.next(Date.now()), 1000);\r\n  return () => clearInterval(id); // cleanup/cancel function\r\n});\r\n// Nothing has started yet!\r\nconst subscription = observable.subscribe(val => console.log(val));\r\n// Now it starts\r\nsubscription.unsubscribe(); // cancel! cleanup runs\r\n\r\n// Comparison:\r\n// Promise: one value, eager, .then()/.catch(), native\r\n// Observable: many values, lazy, .subscribe(), cancellable, needs RxJS\r\n\r\n// Observables shine for: WebSockets, DOM events, polling, streams</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">95</span><span class=\"q-question\">What are the common JavaScript mistakes and how to avoid them?</span><span class=\"badge b-medium\">Medium</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Top Mistakes to Avoid</span></div>\r\n<div class=\"example-box\"><span class=\"lbl\">📋 Common Pitfalls</span>\r\n❌ <strong>Using == instead of ===</strong> — always use strict equality.<br><br>\r\n❌ <strong>Mutating function arguments (objects/arrays)</strong> — always work with copies.<br><br>\r\n❌ <strong>Not handling Promise rejections</strong> — always add .catch() or try/catch with await.<br><br>\r\n❌ <strong>Missing await on async functions</strong> — you get a Promise, not a value.<br><br>\r\n❌ <strong>Synchronous loops with async operations</strong> — forEach doesn't await; use for...of instead.<br><br>\r\n❌ <strong>var in loops</strong> — use let. var creates closure bugs.<br><br>\r\n❌ <strong>Comparing objects with ===</strong> — objects are compared by reference.<br><br>\r\n❌ <strong>Swallowing errors silently</strong> — catch blocks that log nothing hide bugs.\r\n</div>\r\n<pre\">// forEach doesn't await — BUG\r\nconst results = [];\r\nawait items.forEach(async (item) => {  // ❌ forEach can't await\r\n  results.push(await fetchItem(item));\r\n});\r\n// results is still empty!\r\n\r\n// Fix: use for...of or Promise.all\r\nfor (const item of items) {\r\n  results.push(await fetchItem(item)); // ✅ awaits each\r\n}\r\n// Or in parallel:\r\nconst results2 = await Promise.all(items.map(fetchItem)); // ✅\r\n\r\n// Mutating arguments — surprise!\r\nfunction addRole(user, role) {\r\n  user.roles.push(role); // ❌ mutates the original!\r\n}\r\n// Fix:\r\nfunction addRole(user, role) {\r\n  return { ...user, roles: [...user.roles, role] }; // ✅ new object\r\n}</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">96</span><span class=\"q-question\">What is the difference between call stack overflow and heap out of memory?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>The <strong>call stack</strong> is fixed-size memory for function calls. Overflow = too much recursion (function calls itself without stopping). The <strong>heap</strong> is dynamic memory for objects. Out of memory = too many objects created without being freed (memory leak).</div>\r\n<pre\">// Stack overflow — too deep recursion\r\nfunction infinite() {\r\n  return infinite(); // calls itself forever\r\n}\r\ninfinite(); // ❌ RangeError: Maximum call stack size exceeded\r\n\r\n// Fix: iterative or tail recursion\r\nfunction iterative(n, acc = 0) {\r\n  while (n > 0) { acc += n--; }\r\n  return acc;\r\n}\r\n\r\n// Heap out of memory — too many objects\r\nconst data = [];\r\nwhile (true) {\r\n  data.push(new Array(10000).fill(0)); // keeps growing\r\n}\r\n// ❌ FATAL ERROR: JavaScript heap out of memory (Node.js)\r\n\r\n// Common heap leak — event listeners not removed\r\nfunction attachListeners() {\r\n  const bigData = new Array(100000).fill(\"data\");\r\n  document.addEventListener('click', () => {\r\n    console.log(bigData.length); // closes over bigData!\r\n  });\r\n  // bigData can NEVER be GC'd as long as listener exists!\r\n}\r\n// Fix: store reference and removeEventListener later\r\n\r\n// Stack size is typically ~10,000-15,000 frames in V8</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">97</span><span class=\"q-question\">What is the TC39 process and how do JavaScript features progress?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Simple Answer</span>TC39 is the committee that evolves JavaScript. New features go through 5 stages (0-4) before becoming official. Stage 4 = in the spec (ship it!). Browsers often implement stage 3 features early.</div>\r\n<div class=\"example-box\"><span class=\"lbl\">📋 The 5 Stages</span>\r\n<strong>Stage 0 (Strawperson)</strong>: Just an idea, anyone can propose.<br>\r\n<strong>Stage 1 (Proposal)</strong>: Champion identified, problem statement, initial solution.<br>\r\n<strong>Stage 2 (Draft)</strong>: Formal spec text written, intent to standardize.<br>\r\n<strong>Stage 3 (Candidate)</strong>: Spec complete, browsers start implementing, feedback gathered.<br>\r\n<strong>Stage 4 (Finished)</strong>: Two implementations, ready for spec — it's now official JS!<br><br>\r\nRecent additions: Optional chaining (?.) = Stage 4 in 2020. Array.at() = Stage 4 in 2022. structuredClone() = 2022. Array.toSorted() = 2023. Temporal = Stage 3 (coming soon).\r\n</div>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">98</span><span class=\"q-question\">What are the latest ES2024 JavaScript features?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 ES2024 (ES15) New Features</span></div>\r\n<pre\">// 1. Object.groupBy() — group array by key (ES2024)\r\nconst people = [\r\n  { name: \"Alice\", dept: \"Engineering\" },\r\n  { name: \"Bob\", dept: \"Design\" },\r\n  { name: \"Carol\", dept: \"Engineering\" }\r\n];\r\nObject.groupBy(people, p => p.dept);\r\n// { Engineering: [{Alice...}, {Carol...}], Design: [{Bob...}] }\r\n// Also: Map.groupBy() for Map output\r\n\r\n// 2. Promise.withResolvers() — expose resolve/reject outside\r\nconst { promise, resolve, reject } = Promise.withResolvers();\r\n// More ergonomic than wrapping in new Promise()\r\nsetTimeout(() => resolve(\"done\"), 1000);\r\nconst result = await promise;\r\n\r\n// 3. ArrayBuffer.resize() — resize a buffer\r\nconst buffer = new ArrayBuffer(8, { maxByteLength: 16 });\r\nbuffer.resize(12); // expand to 12 bytes\r\n\r\n// 4. Regex /v flag — unicode sets\r\nconst regex = /[\\p{Decimal_Number}--[0-9]]/v; // set difference\r\n\r\n// 5. String.prototype.isWellFormed() / toWellFormed()\r\n\"\\uD800\".isWellFormed();    // false (lone surrogate)\r\n\"\\uD800\".toWellFormed();    // \"?\" (replace with replacement char)</pre>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">99</span><span class=\"q-question\">What makes excellent JavaScript architecture for large applications?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Architecture Principles</span></div>\r\n<div class=\"example-box\"><span class=\"lbl\">📋 Large-Scale JS Best Practices</span>\r\n🏗️ <strong>Module boundaries</strong>: Each module has one clear responsibility. Avoid circular imports.<br><br>\r\n🔒 <strong>Immutability</strong>: Functions return new values instead of mutating. Prevents hard-to-find state bugs.<br><br>\r\n🧪 <strong>Pure functions by default</strong>: Side effects only at the edges (API calls, DOM). Makes code testable.<br><br>\r\n🎯 <strong>Explicit error handling</strong>: Never swallow errors. Create custom error classes. Use Result type pattern.<br><br>\r\n📝 <strong>TypeScript</strong>: Type errors at compile time > runtime crashes in production.<br><br>\r\n⚡ <strong>Async boundaries</strong>: Keep async/await at the \"edges\" — data access layer, not deep in business logic.<br><br>\r\n🔄 <strong>Dependency injection</strong>: Pass dependencies as parameters instead of importing them directly — makes testing easy.<br><br>\r\n📦 <strong>Barrel files carefully</strong>: index.js re-exports can hurt tree shaking. Be intentional.\r\n</div>\r\n</div></div>\r\n\r\n<div class=\"q-card\" data-difficulty=\"hard\">\r\n<div class=\"q-top\"><span class=\"q-num\">100</span><span class=\"q-question\">What are the key differences between JavaScript runtime environments?</span><span class=\"badge b-hard\">Hard</span><span class=\"chevron\">▾</span></div>\r\n<div class=\"q-body\">\r\n<div class=\"simple-box\"><span class=\"lbl\">💡 Browser vs Node vs Deno vs Bun</span></div>\r\n<div class=\"example-box\"><span class=\"lbl\">📋 Runtime Comparison</span>\r\n🌐 <strong>Browser</strong>: DOM, Web APIs (fetch, localStorage, WebGL, WebRTC), sandboxed, security restrictions, window as global.<br><br>\r\n⚙️ <strong>Node.js</strong>: File system, HTTP servers, native modules (fs, path, crypto, child_process), CommonJS + ESM, global as global object.<br><br>\r\n🦕 <strong>Deno</strong>: Built-in TypeScript, ESM only, secure by default (must grant permissions), compatible with many browser APIs, built-in test runner.<br><br>\r\n🍞 <strong>Bun</strong>: Extremely fast (JavaScriptCore engine), drop-in Node replacement, native TypeScript, fast package manager, fast test runner.\r\n</div>\r\n<pre\">// Feature availability by environment:\r\n// fetch() — browser ✅ | Node 18+ ✅ | Deno ✅ | Bun ✅\r\n// fs module — browser ❌ | Node ✅ | Deno (with permission) | Bun ✅\r\n// localStorage — browser ✅ | Node ❌ | Deno ❌ | Bun ❌\r\n// window — browser ✅ | Node ❌ | Deno ❌ | Bun ❌\r\n// process.env — browser ❌ | Node ✅ | Deno (Deno.env) | Bun ✅\r\n// WebSockets — browser ✅ | Node (ws library) | Deno ✅ | Bun ✅\r\n\r\n// Environment detection:\r\nconst isBrowser = typeof window !== 'undefined';\r\nconst isNode    = typeof process !== 'undefined' && process.versions?.node;\r\nconst isDeno    = typeof Deno !== 'undefined';\r\n\r\n// Write isomorphic code — runs in both:\r\nconst baseUrl = isBrowser\r\n  ? window.location.origin\r\n  : process.env.API_URL || 'http://localhost:3000';</pre>\r\n</div></div>\r\n\r\n</main>\r\n\r\n<footer>\r\n  <p>🎉 All 100 JavaScript concepts covered — from basics to ES2024, async patterns, performance, and everything in between. Go ace that interview! ⚡</p>\r\n</footer>\r\n\r\n" }} />
    </>
  );
}